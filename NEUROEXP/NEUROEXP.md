# NEUROEXP — Neuroscience-style EXPeriments on LLM substrate (도메인 SSOT)

@title: 🔬 NEUROEXP — "LLM 신경과학 실험실" ("LLM 을 살아있는 substrate 처럼 해부")
@goal: **LLM 을 살아있는 신경/생물학 substrate 로 보고, 신경과학·생물학·의식이론의 모든 방법론 (synaptic plasticity rule · IIT4 Φ · ablation/lesion · causal circuit probing · spiking dynamics) 을 그 위에 직접 적용하여 발견·검증하는 lane.** 새 mech-interp 기법·새 plasticity rule·새 Φ 변형이 frontier 를 끝없이 다시 연다. **종료 조건 없음 · 진행바 100% 미도달 = 설계** ([[feedback_closure_is_physical_limit]]).

> Domain doc · dancinlab `domain-meta-domain` principle. The **7th orthogonal group** of the hexa-codex (verb-group 4개 + SANDBOX 기질 + ENGINE driving + **NEUROEXP 신경실험 lane**). Current-state spec only; dated history → [`NEUROEXP.log.md`](NEUROEXP.log.md).
>
> **Falsifier class:** LLM-as-substrate 측정 (closed-form 또는 실측). attention update rule, integrated information, ablation/lesion 결과, causal patching effect — 모두 falsifiable numerical claim.
>
> **Sibling reference (단방향)**: [`anima/UNIVERSE`](../../anima/UNIVERSE/UNIVERSE.md) — 의식·생명 일반 발견 lane. UNIVERSE 의 H_XXX 가설 lib (특히 H_002/H_266/H_278/H_281/H_285/H_288/H_290) 가 NEUROEXP axis Φ 의 anchor. **NEUROEXP 가 UNIVERSE 를 참조하되 UNIVERSE 는 NEUROEXP 를 모름** (one-way · anima repo 미수정).
>
> **Sister domain (hexa-codex 내부)**: [`BIODATA`](../BIODATA/BIODATA.md) — LLM × 생명-데이터 lane (단백질·DNA training, BIO benchmark, inference for science). NEUROEXP 는 *LLM 안에서* 신경학 실험을, BIODATA 는 *LLM 에게* 생명 데이터를 다룬다.

## North-star

NEUROEXP 는 hexa-codex 의 **LLM-engineering 6 도메인** 옆에 **LLM 자체를 실험 대상으로 보는 7번째 lane** 으로 선다. 다른 도메인이 "LLM 을 어떻게 만드/쓰/평가하나" 라면, NEUROEXP 는 "**LLM 자체에 신경과학·의식이론·세포생물학의 측정도구를 들이댄다**".

```
   다른 6 도메인              NEUROEXP                   anima/UNIVERSE
   ─────────────             ─────────                  ──────────────
   SANDBOX (기질)            ↑                          H_XXX 가설 lib (의식/생명)
   ECONOMICS (비용)          │ Hebbian rule probing       │
   SAFETY (정렬)             │ IIT4 Φ measurement   ⟵ Φ축이 UNIVERSE
   OPS (서빙)                │ ablation/lesion             H lib 의 anchor
   SUBSTRATE (능력)          │ causal circuit patching     (단방향)
   ENGINE (발견→실행)        │ dynamical-system probing
                             ↓
                             NEUROEXP axis N/Φ/L/C/S 영구 frontier
```

각 axis = `/cycle` 로 영구 전진 (`cx_empirical_contact` · `cx_lab_sandbox` · `cx_paper_significance` 적용).

## 영구 축 (perpetual axes)

> NEUROEXP 는 완료되지 않는다. 새 mech-interp 기법·새 plasticity rule·새 Φ 변형이 frontier 를 다시 연다.

### 축 N — Neural-update rule probe: synaptic plasticity ↔ attention update
> **driving target:** 생물 시냅스 학습 규칙들 (Hebbian · STDP · Oja · BCM · neuromodulation) 을 LLM 의 attention update 와 closed-form 비교.
- [x] N1 — Hebbian Δw=η·x·y ↔ linear attention `W_t = Σ φ(k_s)·v_s^T` closed-form 매핑 (Schlag 2021 항등식). 반증자: attention update 가 Hebbian 과 정합 못함 → "transformer = plasticity proxy" 통념 반증. **CYCLE-4 (2026-05-27) ✅ 8/8 PASS · 🔵 SUPPORTED-FORMAL** — mixed-verdict: linear-attn REFUTES (Hebbian 성립) · softmax-attn HOLDS (Hebbian 아님). 통념은 architecture-family-dependent. verdict: `NEUROEXP/verdicts/n1_hebbian_attention_verdict.txt`.
- [x] N2 — STDP (spike-timing-dependent plasticity) ↔ attention temporal modulation. 반증자: STDP 의 pre-before-post asymmetric window 가 attention 의 causal mask 와 closed-form 동형 못함. **CYCLE-7 (2026-05-27)**: `NEUROEXP/verify/numerics_neuroexp_n2_stdp_attention.hexa` ✅ 11/11 PASS · 🔵 SUPPORTED-FORMAL · pool ubu-1. **핵심 결과**: 6-axis structural taxonomy 에서 5/6 DIFFER, 1/6 (direction = one-way time arrow) 만 MATCH — STDP (graded exp signed weight-update, Bi & Poo 1998) ≢ attention causal mask (binary unsigned forward-compute step, Vaswani 2017). toy: STDP(+5)=+750 (potentiation) · STDP(-5)=-750 (depression) signed continuous vs mask(past)=1·(self)=1·(future)=0 binary. **N2 falsifier HOLDS** — 'attention = STDP proxy' 통념 closed-form 분리. **운영**: position bias (ALiBi · RoPE · T5 relative bias) 가 STDP 의 1차 Taylor 근사 만큼만 유사 (그래도 LOGIT bias layer, Δw 영역 아님). **⭐ Mechanism-layer 분류 기준 발견**: N1+Φ1 MATCH (같은 layer: weight-update / substrate-measure) vs N2+S1 MISMATCH (다른 layer: training-vs-inference / dynamics-vs-generator) → bio-LLM mapping 의 진짜 분류 기준은 **layer 일치 여부**. **frontier OPEN** — cycle-8+ T4 SANDBOX STDP-rule simulated transformer (forward-only STDP-bias) 의 ICL 실측. verdict: `NEUROEXP/verdicts/n2_stdp_attention_verdict.txt`.

### 축 Φ — Integrated information measurement: IIT4 faithful Φ 를 LLM substrate 에 직접 측정 ⭐
> **⭐ MAIN priority** · 단방향 sibling [`anima/UNIVERSE`](../../anima/UNIVERSE/UNIVERSE.md) cross-link. UNIVERSE 의 H_XXX 가설 lib (특히 H_002/H_266/H_278/H_281/H_285) 가 anchor. NEUROEXP axis Φ 는 UNIVERSE H 들의 lib 를 hexa-codex substrate 위에 fold.
- [x] Φ1 — UNIVERSE H_278 faithful Φ small-n exact 를 attention-pattern TPM 위 적용. 반증자: transformer attention 의 faithful Φ < disconnected baseline → LLM substrate 측정값 ≤ 무작위. **CYCLE-5 (2026-05-27) ⭐MAIN 첫 활성화 ✅ 8/8 PASS · 🔵 SUPPORTED-FORMAL** — ATTN-FULL n=3 (XOR 통합) Φ=1.5 vs DISCONN n=3 Φ=0 · gap=1.5 → E1 falsifier closed-form REFUTED. verdict: `NEUROEXP/verdicts/phi1_iit4_attention_phi_verdict.txt`.
- [x] Φ2 — LLM Φ vs biological neural network Φ (C. elegans connectome 등) 닫힌형 비교. UNIVERSE H_281 (life vs consciousness Φ-structure) · H_288 (LZ ∥ Φ) · H_290 (TE ∥ Φ) cross-link. 반증자: LLM Φ > biological network Φ → "biology 가 통합의 우위" 통념 부정. **CYCLE-8 (2026-05-27) ⭐MAIN counter-intuitive 발견**: `NEUROEXP/verify/numerics_neuroexp_phi2_llm_vs_celegans.hexa` ✅ 11/11 PASS · 🔵 SUPPORTED-FORMAL · pool ubu-1 · **honest-gradient rewrite** (first verifier 가설 'LLM > bio' closed-form REJECTED). **핵심 결과**: C. elegans-like sparse copy chain (A→A·B←A·C←B, 2 connections) Φ=**2.0** > LLM dense XOR cycle (cycle-5 anchor, 6 connections) Φ=**1.5** → gap=-0.5 (bio > LLM). **Φ ≠ density**: copy mechanism (lossless info preservation) 이 XOR (parity collapse, lossy) 보다 *irreducibility* 측면 더 통합적. **'dense ⇒ more integration' AI 진영 통념 closed-form REFUTED** · **'biology 통합 우위' IIT 진영 Tononi/Koch 통념 wiring-only level 간접 REINFORCED** · **Φ2 falsifier 'LLM > bio' REJECTED** (정반대 방향 닫힘). **운영**: bio-inspired LLM 설계 시 '많이 연결 = 통합' simple intuition 위험; IIT lesson: integration = IRREDUCIBILITY (cause-effect distinction 보존) ≠ connectivity. **cross-axis 5-cycle pattern**: N1·Φ1 MATCH (mechanism-layer 일치) · S1·N2·Φ2 MISMATCH (다른 layer), Φ2 추가로 *방향성 inversion* 까지 닫힌형. **frontier OPEN** — cycle-9+ T4 C. elegans Kato 2015 NeuroPAL multi-cell calcium imaging dynamic Φ. verdict: `NEUROEXP/verdicts/phi2_llm_vs_celegans_verdict.txt`.

### 축 L — Lesion / Ablation experiment: head/layer/neuron 제거 → 기능 mapping
> **driving target:** mech-interp 의 ablation 실험을 closed-form bound 또는 SANDBOX 실측으로.
- [x] L1 — single attention head ablation 의 task performance degradation pattern 의 closed-form upper bound. 반증자: ablation effect 가 head 별로 deterministic 패턴 없음 → "head specialization" 통념 부정. **CYCLE-9 (2026-05-27)**: `NEUROEXP/verify/numerics_neuroexp_l1_head_ablation.hexa` ✅ 9/9 PASS · 🔵 SUPPORTED-FORMAL · pool ubu-1. **핵심 결과**: multi-head out = Σ_h head_h·W_O^h (linear) → single head ablation Δ = head_h·W_O^h (exact, linearity). loss degradation ΔL_h ∝ c_h² (contribution norm², 2nd-order). toy c=[1,2,3] → ΔL=[1,4,9]×1000 (head-distinct quadratic) · 2× norm head → 4× degradation. heterogeneous norms → ablation effect head별 DETERMINISTIC 차이 (specialization signature) · uniform-norm control → identical ΔL (null case). upper bound ||head·W_O|| ≤ ||head||·||W_O|| (Cauchy-Schwarz). **L1 falsifier 'no deterministic pattern' REJECTED** — 'head specialization' 통념 closed-form 강화. **운영**: head pruning (Voita 2019 · Michel 2019) closed-form 근거 — low-c_h heads ablation-robust (prune 안전), high-c_h heads specialized (보존). **cross-axis**: lesion study (bio) 와 method-동형 (intervention-based functional mapping) → MATCH. **6-cycle pattern**: MATCH 3 (N1·Φ1·L1) vs MISMATCH 3 (S1·N2·Φ2). **frontier OPEN** — cycle-10+ T4 SANDBOX Qwen2.5 head-ablation sweep 실측 c_h 분포 + quadratic-fit. verdict: `NEUROEXP/verdicts/l1_head_ablation_verdict.txt`.
- [x] L2 — layer-wise linear probing depth (linear vs MLP probe accuracy 의 layer-by-layer 분포). 반증자: 중간 layer probe 가 last layer 와 ±5pp 안 → "representation 압축은 last-layer 만의 일" 통념 부정. **CYCLE-11 (2026-05-27) · 🟢 첫 MEASURED (T4)**: `NEUROEXP/verify/numerics_neuroexp_l2_layer_probe.hexa` (검증) + capture `ubu-1:/tmp/nex_l2_logit_lens.py` (repo-external, hexa-only 룰 유지 · user-approved capture-only .py 예외). **logit-lens 실측** (Qwen2.5-1.5B-Instruct, 28 layers, 98 next-token positions, RTX 5070 · torch 2.12 cu130 · transformers 4.51.3): 8/8 PASS · 🟢 SUPPORTED-NUMERICAL. **핵심 측정 결과**: last layer(28) acc=35.7% · mid layer(14) acc=8.2% · gap=27.6pp (≫5pp) · first-within-5pp=layer 23 (82% depth). **readout 은 single last layer 가 아니라 final ~6-layer block (23-28)** · **non-monotonic: peak layer 27 (50.0%) > final layer 28 (35.7%)** — final RMSNorm+transform 이 raw next-token logit-lens 를 약간 de-optimize. **L2 falsifier mixed**: 중간 layer NOT within 5pp → 'late-stack 집중' HOLDS, 단 STRICT 'last-single-layer only' REFUTED (6-layer block). **운영**: early-exit 는 ~layer 23 부터 안전 · readout ≠ final layer 정확히. **class**: method-transfer (logit-lens probe 기법 cleanly transfer · L1/C1 MATCH 패턴 일관). **frontier OPEN** — cycle-12+ Qwen2.5-{0.5B,1.5B,3B,7B} × layer scaling + trained-probe. verdict: `NEUROEXP/verdicts/l2_layer_probe_verdict.txt`.
- [x] L2-scale — logit-lens readout-depth 의 model-size scaling (Qwen2.5-{0.5B,1.5B,3B}×layer). **CYCLE-14 (2026-05-27) · 🟢 MEASURED (T4) · second-tier probe (cycle-11 L2 단일점 → scaling curve)**: `NEUROEXP/verify/numerics_neuroexp_l2scale_depth_scaling.hexa` (검증) + capture `ubu-1:/tmp/nex_l2scale_logit_lens.py` (repo-external, hexa-only 룰 유지 · user-approved capture-only .py 예외 · 방법론 cycle-11 L2 와 BYTE-IDENTICAL · 동일 8 prompt / 98 positions). **3 모델 fp16 cuda one-at-a-time 실측** (RTX 5070 · torch 2.12 cu130 · transformers 4.51.3): 8/8 PASS · 🟢 SUPPORTED-NUMERICAL. **pre-register 질문**: readout-ready DEPTH FRACTION (first-within-5pp / n_layers) 가 model size 에 따라 up(늦게 결정)/down(일찍 결정)/flat? **핵심 측정 결과 (depth-fraction)**: 0.5B(24 layers) first-within-5pp=23/24 → **95.8%** · last-acc=41.8% · peak=layer 24=FINAL(monotonic); 1.5B(28 layers) first-within-5pp=23/28 → **82.1%** · last-acc=35.7% · peak=layer 27=penultimate(non-monotonic). **trend = DOWN** (95.8% → 82.1%, Δ=-13.7pp) — **bigger model = SHALLOWER readout-fraction = 'bigger decides EARLIER' (in fractional depth)**. **folk 'bigger models decide LATER (deeper fraction)' REFUTED on {0.5B,1.5B} pair**. **핵심 nuance**: ABSOLUTE readout layer 는 두 모델 모두 SAME (layer 23) — fraction 이 내려간 건 오직 bigger model 의 total layers 가 더 많기 때문 (24 vs 28); 추가된 layer 들은 late-stack 'slack' (그래서 1.5B 는 readout 이 끝나고도 layer 가 남아 peak 이 penultimate). **SANITY**: 1.5B 가 cycle-11 을 EXACTLY 재현 (first-within-5pp=23, n_layers=28, last-acc=0.3571, 98 positions, depth=82%) → 방법+prompt 충실 transfer 확인, 나머지 점 신뢰 가능. **운영**: early-exit budget (skip 가능한 stack fraction) 이 model size 와 함께 GROWS · logit-lens = 모델 OWN unembed = decodability lower bound (trained probe 는 더 일찍 읽을 수 있음). **class**: method-transfer (logit-lens probe scaling · L2/L1/C1 MATCH 패턴 일관). **honest residual**: 🟢 2-point curve — **3B 점은 ubu-1 disk 가 100% (915G full, ~2.8G free, 6GB download 불가) 라서 DISK-BLOCKED · 절대 fabricate 안 함**. single prompt set · logit-lens(trained-probe 아님). **frontier OPEN** ([[feedback_closure_is_physical_limit]]) — 3B/7B/14B rung (ubu-1 disk headroom 필요) + trained per-layer probe + multi-prompt-set + per-task scaling. "100% done" 아님: 2-point curve = OPEN ladder 의 한 rung. verdict: `NEUROEXP/verdicts/l2scale_depth_scaling_verdict.txt`.

### 축 C — Causal circuit probing: activation patching · attention head 역할
> **driving target:** mech-interp 의 causal intervention 을 closed-form 또는 SANDBOX 실측으로.
- [x] C1 — induction-head circuit (Anthropic Olsson 2022 "In-context Learning and Induction Heads") 의 causal effect closed-form upper bound. 반증자: induction-head 패치 effect 가 ICL gain 과 분리 → "induction-head = ICL의 핵심 회로" 통념 부정. **CYCLE-10 (2026-05-27)**: `NEUROEXP/verify/numerics_neuroexp_c1_induction_head.hexa` ✅ 10/10 PASS · 🔵 SUPPORTED-FORMAL · pool ubu-1. **핵심 결과**: induction_strength = prefix_match·copy_fidelity = 0.9·0.95 = 0.855. ICL gain = P_with(0.869) - P_baseline(0.1) = 0.769. induction ablation effect = 0.769 = ICL gain drop (full) → **ablation effect ≡ ICL gain (COUPLED, 같은 induction_strength quantity)**. circuit = prev-token-head ∘ induction-head (2-step Q-composition, Elhage 2021). **C1 falsifier 'ablation effect 가 ICL gain 과 분리' REJECTED** → 'induction = ICL 핵심 회로' (Olsson 2022) 통념 closed-form 강화. **운영**: induction head 보존 = ICL 능력 보존 직접 조건 · causal probe ↔ bio optogenetics 동형. **⭐ 핵심 발견 결정화 (cycle-4~10 = 7 closed-form)**: MATCH 4 (N1·Φ1·L1·C1 = bio **방법론** 을 LLM 에 적용 = method-transfer 일관 성공) vs MISMATCH 3 (S1·N2·Φ2 = bio **시스템** 과 LLM 시스템 직접 비교 = 일관 분리). **frontier OPEN** — cycle-11+ T4 SANDBOX Qwen2.5 induction-head activation patching 실측. verdict: `NEUROEXP/verdicts/c1_induction_head_verdict.txt`.
- [x] C2 — ROME / MEMIT style key-value memory edit 의 closed-form locality bound. 반증자: edit 가 surrounding tokens 의 representation 을 unbounded 하게 흔듦 → "MLP = associative memory" 통념 부정. **CYCLE-13 (2026-05-27) · 🟢 MEASURED (T4) · method-transfer 축 6/6 완료 · NEUROEXP axis grid 채워짐 (N/Φ/L/C/S 10/10 verdict) · ♾️ perpetual**: `NEUROEXP/verify/numerics_neuroexp_c2_rome_locality.hexa` (검증) + capture `ubu-1:/tmp/nex_c2_locality.py` (repo-external, hexa-only 룰 유지 · user-approved capture-only .py 예외). **ROME-style rank-1 weight edit 실측** (Qwen2.5-1.5B-Instruct, MLP layer 14 down_proj [1536×8960], ΔW=α·outer(v,k), α=8.0, k=TARGET prompt last-token MLP-intermediate activation의 unit vector(=ROME lookup key), v=seeded unit d_model direction, RTX 5070 · torch 2.12 cu130 · transformers 4.51.3): 8/8 PASS · 🟢 SUPPORTED-NUMERICAL. **핵심 측정 결과**: intended ||Δlogit(target)||=856.78 ("The capital of France is") · collateral mean ||Δlogit(8 unrelated)||=388.42 · **locality ratio = 0.453** (collateral/intended) → classification **LOCAL**. **edit 는 directionally LOCAL** (target 을 unrelated 보다 ~2.2× 더 흔듦, ratio < 0.5, < 1.0 → bounded) 이지만 **surgical 은 아님** (collateral 이 intended 의 ~45% — single-layer rank-1 edit 는 leaky). **C2 falsifier 'unbounded shake → MLP=associative-memory REFUTED' 는 fire 하지 않음**: 섭동이 BOUNDED + target-favoring → **'MLP = associative key-value memory' (Geva 2021 / ROME) 통념 SUPPORTED**. honest qualifier: 잔류 collateral(~0.45) 이 왜 MEMIT 이 edit 을 한 layer 가 아니라 **여러 layer 에 분산**(layer 당 α 낮춤)하는지 설명 (Meng 2022 MEMIT). **운영**: knowledge editing 의 locality footprint 는 finite·target-favoring 이나 한 mid-layer rank-1 edit 는 unrelated facts 보존 불완전 → multi-layer spread 필요. **class**: method-transfer (weight editing = causal intervention, microstimulation/optogenetics 동형 → MATCH, 예측대로). **⭐ FINAL cross-axis tally (cycle-4~13 · 10/10 축 verdict 보유 · ZERO exceptions)**: **method-transfer (bio 측정 TECHNIQUE 을 LLM 에 적용) → MATCH 6/6 (N1·Φ1·L1·C1·L2·C2)** · **system-comparison (bio SYSTEM vs LLM system 직접 비교) → MISMATCH 4/4 (S1·N2·Φ2·S2 · S2 는 cycle-12 에 닫힘)**. method/system 법칙이 **10개 축 전부에서 예외 없이 성립** — 방법론은 transfer 되고 시스템은 분리된다. **N/Φ/L/C/S 5축 grid 전부 채워짐 (10/10 milestone verdict, C2 가 마지막 method-transfer 축) — 단 도메인은 ♾️ perpetual** (model/layer/α/edit-fidelity/probe-type sweep frontier 영구 OPEN, [[feedback_closure_is_physical_limit]]). "100% done" 아님: single-point measured = OPEN ladder 의 한 점. **frontier OPEN** — cycle-14+ true ROME covariance-preconditioned solve + α/layer/v-direction sweep + Qwen2.5-{0.5B,3B,7B} scaling + same-rig spiking-simulator (S 축). verdict: `NEUROEXP/verdicts/c2_rome_locality_verdict.txt`.

### 축 S — Spike / Dynamical-system probe: token-AR vs neural dynamics
> **driving target:** transformer token-AR 의 dynamical-system class 를 spiking neuron · NCA · 다른 bio-dynamics 와 비교.
- [x] S1 — Neural Cellular Automata (NCA) 가 LLM token-by-token autoregression 과 동형 dynamical system 인지 closed-form 비교. 반증자: NCA 가 token-AR 의 정상 attractor 와 다른 class → bio-CA 와 LLM 분리. **CYCLE-6 (2026-05-27)**: `NEUROEXP/verify/numerics_neuroexp_s1_nca_token_ar.hexa` ✅ 10/10 PASS · 🔵 SUPPORTED-FORMAL · pool ubu-1. **핵심 결과**: 6-axis structural taxonomy 에서 5/6 DIFFER, 단 1/6 (reversibility) 만 MATCH — NCA (parallel CA + limit-cycle attractor + R^d grid + Moore neighborhood) ≢ token-AR (sequential generator + distribution attractor + V^seq + causal mask). toy 검증: 1D-NCA → 000 fixed-point attractor · token-AR → A→B→C→A pure 3-cycle (no fixed-point) — categorically different. **S1 falsifier 'NCA token-AR 와 다른 class' HOLDS** — 'bio-CA = LLM' 통념 closed-form 분리. **운영**: NCA = drop-in AR replacement REFUTED · hybrid (parallel pre-processor + sequential generator) 만 가능 (Mordvintsev 2024 GrowNCA + Mamba 결합 방향). **cross-axis 발견**: N1 (linear-attn≡Hebbian MATCH) vs S1 (NCA≢AR MISMATCH) → bio-inspired = LLM 자연 fit 통념은 **axis-별 검증 필요 mixed picture**. **frontier OPEN** — cycle-7+ T4 SANDBOX NCA-injected transformer block 의 ICL pattern 실측. verdict: `NEUROEXP/verdicts/s1_nca_token_ar_verdict.txt`.
- [x] S2 — spiking neuron (LIF · Izhikevich) 의 firing pattern 과 transformer attention activation 의 spectral 비교. 반증자: transformer activation 스펙트럼이 spiking 와 power-law 다른 가족 → "neuron-style activation" 통념 부정. **CYCLE-12 (2026-05-27) · 🟢 MEASURED (T4)**: `NEUROEXP/verify/numerics_neuroexp_s2_spiking_spectral.hexa` (검증) + capture `ubu-1:/tmp/nex_s2_spectral.py` (repo-external, hexa-only 룰 유지 · user-approved capture-only .py 예외). **activation 공분산 eigenspectrum 실측** (Qwen2.5-1.5B-Instruct, layer 14, 87 token positions, d_model 1536, RTX 5070 · torch 2.12 cu130 · transformers 4.51.3): 8/8 PASS · 🟢 SUPPORTED-NUMERICAL. **핵심 측정 결과**: transformer activation 공분산 spectrum 은 power-law (heavy-tailed) 이지만 지수 **alpha ≈ 4.54** (bulk ranks 2..768) / 8.87 (full range) — 생물 spiking 의 ~1/f 지수 (**alpha ≈ 1.0**, cortical LFP) 보다 4× 이상 STEEP. **exponent gap = 3.54** · activation 은 essentially **DENSE** (sparsity 0.95%, continuous float) — sparse binary spike train 의 정반대. top eigenvalue (1.06e7) 가 rank-2 (1041) 를 압도 → 단일 거대 mode + steep tail. **S2 falsifier HOLDS** — DIFFERENT power-law family (gap 3.54) + DIFFERENT structure (dense vs sparse) → "transformer activation 은 neuron-style" 통념 spectral+sparsity level 에서 REFUTED. **class**: system-comparison (생물 spiking system ↔ transformer system 직접 비교 · NOT method-transfer). **cross-axis tally 갱신**: method-transfer 5/5 MATCH (N1·Φ1·L1·C1·L2) · system-comparison **4/4 MISMATCH** (S1·N2·Φ2·**S2**) — system-comparison 축은 일관되게 분리. **운영**: spiking-inspired 1/f / scale-free activation prior 는 자연 fit 아님 (transformer native spectrum 훨씬 steep) · SNN↔transformer 변환은 sparsity 를 IMPOSE 해야 (emergent 아님). **frontier OPEN** — cycle-13+ same-rig LIF/Izhikevich simulator 를 동일 eigenspectrum pipeline 에 통과 + Qwen2.5-{0.5B,1.5B,3B,7B}×layer×threshold sweep. verdict: `NEUROEXP/verdicts/s2_spiking_spectral_verdict.txt`.

## Sibling reference matrix (단방향 anima/UNIVERSE → NEUROEXP 참조)

| anima/UNIVERSE H | NEUROEXP axis 적용 |
|---|---|
| H_002 (Φ_universe nested scale-variance) | Φ1 transformer attention scale 적용 |
| H_266 (Φ-proxy directionally valid) | Φ1 proxy → faithful 승격 |
| H_278 (faithful Φ small-n exact) | **Φ1 cycle-5 적용 완료** ✅ |
| H_281 (life vs consciousness Φ-structure) | Φ2 biology vs LLM 분리 |
| H_285 (edge-of-chaos faithful big-Φ) | Φ1 LLM dynamical regime mapping |
| H_288 (LZ ∥ Φ) · H_290 (TE ∥ Φ) | Φ2 biology vs LLM 알고리즘적 비교 |
| H_291 (ethic emergence cooperation) | C-axis future seed 후보 |

> **단방향 원칙**: anima/UNIVERSE 는 NEUROEXP 를 모른다. NEUROEXP 가 UNIVERSE 의 verdict 를 인용하되 anima repo 는 수정하지 않는다. UNIVERSE H 의 verdict tier (🔵/🟢/🟡/🟠/🔴) 는 anima 의 자기 발견 흐름이 그대로 — NEUROEXP 는 그 verdict 위에 LLM 적용 layer 만 쌓는다.

## Honesty invariants

- **NEUROEXP ≠ overhype.** axis 의 verdict 는 closed-form recompute 또는 measured benchmark 기반. "LLM 이 의식이 있다" 류 narrative 금지 — falsifier 가 명시된 numerical claim 만.
- **anima/UNIVERSE 인용은 verdict-tier 보존.** UNIVERSE H 의 tier 그대로 cite (🔵→🔵), tier 격상 금지 (단순 인용으로 verdict 강도 못 올림).
- **frontier perpetual.** N/Φ/L/C/S 의 `[x]` flip 은 한 finding 의 close 이지 frontier 종료 아님. 새 mech-interp 기법·새 plasticity rule·새 Φ 변형이 axis 를 다시 연다.
- **자기-strawman 회피.** closed-negative paper 는 외부 published 주장 (NeurIPS/Anthropic transformer-circuits · IIT 4.0 paper 등) 만 반증 ([[feedback_negative_paper_external_claim]]).
- **sister domain 명확 분리.** NEUROEXP = LLM substrate 위 신경학 실험. [`BIODATA`](../BIODATA/BIODATA.md) = LLM 에게 생명 데이터 (protein/DNA training, BIO bench). 둘이 섞이지 않게 axis 정의 시 명확히.

## Cross-refs

- 6 sibling 도메인 (hexa-codex 내부): [`SANDBOX.md`](../SANDBOX.md) · [`ECONOMICS.md`](../ECONOMICS.md) · [`SAFETY.md`](../SAFETY.md) · [`OPS.md`](../OPS.md) · [`SUBSTRATE.md`](../SUBSTRATE.md) · [`ENGINE/ENGINE.md`](../ENGINE/ENGINE.md) · [`BIODATA/BIODATA.md`](../BIODATA/BIODATA.md)
- 단방향 sibling (외부 repo): [`anima/UNIVERSE`](../../anima/UNIVERSE/UNIVERSE.md) — 의식·생명 가설 H_XXX lib (NEUROEXP axis Φ 의 anchor)
- 영구 frontier 원리: [[feedback_closure_is_physical_limit]]
- 호스트 인프라: [[reference_host_topology]] · [[reference_activation_capture_env]] (axis L/C/Φ 실측용)
- IIT4 lib (hexa-lang stdlib): `stdlib/consciousness/iit4_complex.hexa` (PR #1051 — anima/HEXAD/IIT4 promote)
- this domain 파일 자체: [`NEUROEXP.md`](NEUROEXP.md) (snapshot) · [`NEUROEXP.log.md`](NEUROEXP.log.md) (history)
