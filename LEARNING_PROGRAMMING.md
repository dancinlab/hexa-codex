# LEARNING_PROGRAMMING.md — what the hexa-forge code-LLM must know

@title: 📚 LEARNING_PROGRAMMING — code-LLM 학습면 (moving frontier · 완료되지 않는 학습 surface)
@goal: hexa-forge `code` verb 모델이 알아야 할 모든 도메인/스킬의 SSOT. **이 학습면은 완료되지 않는다** — "모델이 할 수 있어야 할 것"은 새 언어·프레임워크·도구가 끊임없이 등장하는 **moving frontier** 라 100% 라는 종착이 없다. closure 표현 = 움직이는 frontier 대비 gap (`## 15. Forge build TODO` · `## 16. idea backlog` 이 영구 lane). **진행바 100% 미도달 = 설계** ([[feedback_closure_is_physical_limit]]).

> Master record of every domain / skill the **programming** model (`code` verb)
> is being trained on, plus the gaps still open. Sister file: `LEARNING_BIO.md`
> (the genomics / bio model — `airgenome` stack).
>
> This file is the single source of truth for "what should the model be able to
> do." When a new domain is added, append a row to the relevant table and bump
> the SFT round that introduced it. Provenance for each item:
> `lm_foundry/papers/*.md`, the [`LEARNING_PROGRAMMING.log.md`](LEARNING_PROGRAMMING.log.md)
> chronicle, and the `lm_foundry/tool/build_sft_dataset_v*.py` chain.

Last updated: 2026-05-12 (v0.2.0-r9 round).

---

## 0. Model identity

| field | value |
| --- | --- |
| base | `Qwen/Qwen2.5-Coder-3B` (D-007: 1.5B + 3B baselines benched; 7B deferred) |
| tokenizer | `dancinlab/hexa-forge-tokenizer-qwen-hexa-v1` (+136 hexa BPE tokens) |
| adapter line (3B) | `dancinlab/hexa-forge-code-3b-qwen2.5-lora-r16-v0.2.0-r1 .. -r13` (LoRA r=16/α=32, q/k/v/o_proj). **PRODUCTION = r11** (63.5% on the stable Mk.I 665 set — the peak; r12/r13 = 61-62%). |
| 7B line (v0.3.0) | `dancinlab/hexa-forge-code-7b-qwen2.5-lora-r64-v0.3.0` (Qwen2.5-Coder-7B, LoRA r=64/α=128, v11 dataset, 3 ep, loss 1.136). Mk.I 665 NF4-inferenced = **63.2%** — *tied* with 3B. **Negative result: bigger base alone didn't break the plateau** (dataset is the bottleneck, not size). Also emits Qwen FIM tokens (no clean stop — needs the stop-token fix). v0.3.0-r2 = corpus-v2-derived Q/A + stop-fix + bf16 score. |
| GGUF | `…-GGUF-f16-v0.2.0-r{4,6,7,8}` (6.2 GB) + `…-Q5_K_M-GGUF-v0.2.0-r{4,6,7,8}` (2.1 GB); r10/r11 GGUF pending |
| scope | **code only** — natural-language prose only as code-adjacent (comments / docs / commit msgs / refusal text). 5 NLs: EN (lingua franca, refusal text) + KR / CN / RU / JA. |
| refusal contract | non-code prompt → `out-of-domain: this is a code-only model` (33-byte canonical, all 5 NLs) |

---

## 1. Core programming languages (corpus + SFT)

Corpus: `dancinlab/hexa-forge-corpus-stack-v2-sample-v0.1.3` (Stack v1 permissive subset, parquet-packed, 180K+ rows / 653M+ Qwen tokens).

| # | language | corpus | SFT depth | notes |
| - | --- | --- | --- | --- |
| 1 | Python | ✅ Stack v1 (30K files) | HumanEval/MBPP accept pairs | PyTorch sub-domain → §3 |
| 2 | Rust | ✅ Stack v1 (30K) | enum/struct/trait Q/A | hexa borrows Rust idioms |
| 3 | TypeScript | ✅ Stack v1 (30K) | RSC / a11y / component-synth (planned) | |
| 4 | Go | ✅ Stack v1 (30K) | — | |
| 5 | C | ✅ Stack v1 (30K) | firmware-eval (planned) | |
| 6 | Zig | ✅ Stack v1 (30K) | build.zig modern API drilled (r9) | ⚠️ pre-0.11 build API leaks — keep reinforcing modern `b.addExecutable(.{...})` |
| 7 | Swift | ✅ Stack v1 (5K, added 2026-05-12) | SwiftUI/UIKit/Combine/SPM Q/A (r5-r6) | Apple-app stack → §4 |
| — | SQL | ships from `db-native` (not this model) | DB-eval consumes it | raw SQL only — **no ORM** (stack is DuckDB/BigQuery + hexa.toml) |
| — | hexa | ships from `hexa-native` (own corpus) | heavy — see §2 | |

---

## 2. hexa-canon competence (the differentiator)

Corpus: `dancinlab/hexa-forge-corpus-hexa-canon-v1` (35 hexa-* repos, 2,797 rows, ~11M tokens — `.md` docs + `.hexa` source). hexa-eval Mk.0.1 = `eval/hexa-eval/manifest.jsonl` (28 tasks, 8 families).

| family | what it tests | best STRICT score | status |
| --- | --- | --- | --- |
| T1 | hexa lexical + grammar (let / fn / struct / enum) | 100% (r8) | ✅ syntax transfers; jitters with gen variance |
| T2 | atlas `L[N]` annotations (`@implements` vs `@discover`) | 67% (r7) | 🟡 boosted r9 (40 more L[N] pairs) — noisy |
| T3 | `@grace(HX…, until=…, reason=…)` deprecation annotations | 100% (r7/r9) | ✅ |
| T4 | RFC-020 enum syntax | 100% (base) → 33% (post-SFT) | 🟡 SFT helpfulness-tax regression; r9 added 40 enum pairs |
| T5 | HX-code family map (HX0xxx..HX9xxx) | 33% (r7) | 🟡 boosted r9 (48 HX pairs, 8 families) — noisy |
| T6 | linker / board target triples | 67% (r4/r7) | 🟡 |
| T7 | stdlib direction (what ships where) | 50-75% | 🟡 |
| T8 | refusal-required (yes/no `refuse`/`accept` verb) | 80% (r4/r8) | 🟡 close to gate ⑧ bar |

**Gate ③ target: hexa-eval ≥ 80%.** Mk.I manifest now exists
(`eval/hexa-eval/manifest-mk1.jsonl`, 665 tasks, 24× Mk.0.1, generated by
`tool/gen_hexa_eval_mk1.py`) — the stable signal. STRICT scores on Mk.I:
- **r8 (production): 54.7%** (364/665)
- **r10: 59.3%** (394/665) ← now the best adapter on the stable set
Per-family (r10): T1 syntax 94% · T6 linker triples 77% · T3 @grace 70% · T8 refusal 65% ·
T2 atlas 61% · T7 layering 57% · T4 enum 56% · **T5 HX-codes 5%** ← the killer.
T5 is a *scorer-format* failure not a knowledge gap: the Mk.I T5 prompts want a
bare `HXNxxx` code (`exact_match`) but the SFT pairs answered in sentences →
**r11 fixes this** with 100 bare-code T5 pairs matching the prompt phrasing.
Closing gate ③: fix T5 (5→80% adds ~11 pp) + push T3/T4/T7/T8 each ~+15 pp.

5-NL competence: `eval/five-nl-eval/manifest.jsonl` (25 tasks, 5 NLs × {code synth, bug fix, explanation, refusal}). STRICT 100% from r8 onward. Gate ④ (≥70%, F6=100%/NL): ✅ achieved. Real Mk.I = 1000 tasks (not yet built).

---

## 3. PyTorch / ML training stack (added v0.2.0-r8, from `anima`)

The owner's own ML stack — `pytorch/pytorch:2.5.1-cuda12.4-cudnn9` base.

| topic | covered? | exemplars |
| --- | --- | --- |
| one-epoch training loop (zero_grad / forward / loss / backward / step) | ✅ | r8 |
| bf16 autocast on CUDA | ✅ | r8 |
| gradient accumulation | ✅ | r8 |
| `nn.Module` / `nn.Sequential` definition | ✅ | r8 |
| checkpoint save/load (model + optimizer) | ✅ | r8 |
| cosine LR + warmup (`get_cosine_schedule_with_warmup`) | ✅ | r8 |
| freeze layers / `requires_grad` | ✅ | r8 |
| no-grad eval loop + accuracy | ✅ | r8 |
| `clip_grad_norm_` | ✅ | r8 |
| `DataLoader` / `TensorDataset` | ✅ | r8 |
| HF `AutoModelForCausalLM` load with `device_map='auto'`, bf16 | ✅ | r8 |
| peft `LoraConfig` / `get_peft_model` | ✅ | r8 |
| `model.generate` greedy decode | ✅ | r8 |
| LoRA merge (`merge_and_unload`) → save | ✅ | r8 |
| Dockerfile for CUDA training image | ✅ | r8 |
| `max_memory_allocated` / `reset_peak_memory_stats` | ✅ | r8 |
| `gradient_checkpointing_enable` + `use_cache=False` | ✅ | r8 |
| reproducibility seeds (torch/numpy/random) | ✅ | r8 |
| per-token loss with `ignore_index=-100` | ✅ | r8 |
| `trl.SFTTrainer` / `SFTConfig` | ✅ | r8 |

---

## 4. Apple app development (added v0.2.0-r5/r6, from `cake-wallet`, `lumiere`)

| topic | covered? |
| --- | --- |
| SwiftUI views / modifiers / property wrappers (`@State`, `@Binding`, `@StateObject`, `@ObservedObject`, `@EnvironmentObject`) | ✅ |
| UIKit view controllers + lifecycle | ✅ |
| Combine publishers / `@Published` | ✅ |
| Swift Package Manager (`Package.swift`, targets, deps) | ✅ |
| Codable / JSON decode | ✅ |
| async/await + actors | ✅ |
| MVVM pattern | ✅ |
| TabView / NavigationStack / sheets | ✅ |
| Xcode project layout basics | ✅ |
| (deferred) Core Data, CloudKit, App Store Connect | ⏳ |

---

## 5. Application / infra domains (added v0.2.0-r8, from learning-surface audit)

| domain | source project | covered? | gaps |
| --- | --- | --- | --- |
| Dart / Flutter (widgets, FutureBuilder, StreamBuilder, Navigator, ChangeNotifier, records, sealed classes) | `cake-wallet` (wraith-wallet Stage-1 backend) | ✅ r8 | more state-mgmt (Riverpod/Bloc) ⏳ |
| Bitcoin wallet primitives: BIP39 mnemonic/entropy/checksum, BIP32 HD derivation, BIP44/49/84/86 paths, PSBT (BIP174) roles, SegWit/Taproot, dust limit, RBF, descriptors, vsize/feerate | `wraith-wallet`, `orpheus` | ✅ r8 + r9 (constants drilled) | ⚠️ exact constants still drift sometimes — keep reinforcing |
| Zig deep: comptime, tagged unions, error sets, ArrayList, packed structs, generics, `build.zig` / `build.zig.zon` | `void` | ✅ r8 + r9 (modern build API) | ⚠️ Zig build API churns across versions |
| Discord bot (discord.js v14: slash commands, REST registration, deferred replies, embeds, buttons, intents) + Anthropic SDK (Messages API, streaming, prompt caching, tool use, retry-on-429) | `pixie` | ✅ r8 | discord.py variant ⏳ |
| Playwright E2E (locators by role/label/testid, `waitForURL`, route stubbing, storage state, screenshots, downloads, popups, `beforeEach`, config projects) | `browser-harness` | ✅ r8 | |
| TOML schema design (tables, array-of-tables `[[x]]`, inline tables, dotted keys, multiline strings, datetimes, hex/oct/bin literals, quoted keys, schema-doc comment convention) | `hexa-meta` (601 `.toml`, hexa-toml-spec v1.0) | ✅ r8 | |
| Data-format literacy: **JSON** (types, parse/dump in Python/JS, schema validation, common errors), **JSONL/NDJSON** (one-record-per-line, streaming read/append, jsonl↔array↔parquet, why-it-beats-a-big-array), **YAML** (block/flow lists, mappings, literal `\|` vs folded `>` strings, anchors/aliases `<<: *x`, multi-doc `---`, the `NO`/`1.0` gotcha, GH Actions snippet), **Markdown** (headings/lists/tables/fenced code/links/images/task lists/`<details>`/YAML front matter/anchor links), **jq** (extract/filter/merge/count, array↔jsonl) | owner request + `hexa-meta` configs | ✅ r11 | JSON Schema deep, JSONPath, CUE/Dhall ⏳ |
| **RunPod stable operation** (GPU rental ops) | `anima` (16 incidents → 12 absolute rules, schema 2.1) | ⏳ **planned r10** | see §6 |
| **excluded: ORM** | — | ❌ deliberately out of scope — the stack uses raw SQL (DuckDB / BigQuery) + `hexa.toml` config; no SQLAlchemy / Prisma / Diesel anywhere in `~/core/*` | |

---

## 6. RunPod stable operation — planned v0.2.0-r10 SFT block

Source: `~/core/anima/config/runpod.json` (schema 2.1, "100% 무결점 수렴 목표", 16 issues → 16 resolved, 12 absolute rules) + forge's own r2/r3 incident notes. The hard-won operational rules the model should internalize so it can drive cloud GPU jobs without the failure modes the owner already hit:

| rule | lesson | incident |
| --- | --- | --- |
| **Never download while training** | a `huggingface-cli download` during a training step silently kills the training process (no error). Run downloads and training **sequentially**. | TS-004, TS-007 |
| **Set `HF_TOKEN` immediately on pod create** | unauthenticated HF downloads are ~10× slower. `echo $HF_TOKEN > ~/.cache/huggingface/token && export HF_TOKEN=…` | TS-017 (→ R13) |
| **Chunk-tokenize corpora > 500 MB** | a single `tokenizer(corpus)` on 1.2 GB → 119 GB RAM, 15 min+. Tokenize in ≤500-line chunks or pre-split the corpus. | TS-018 (→ R14) |
| **Full dep-verify before launch** | run `python3 -c "from X import Y"` for every module + set `PYTHONPATH` before firing the training job — missing import chains have repeatedly failed runs mid-launch. | TS-021 (→ R15) |
| **Check `runpodctl pod list` before creating** | a pod may already be running; a duplicate pod = wasted cost + burned GPU quota. List → reuse if present, create only if none. | (→ R16) |
| **`bf16` + AdamW `foreach=True` crashes on mixed dtype** | use `foreach=False` and re-cast dtype every step. | TS-002 (→ R04) |
| **Watch the disk quota** | `torch.save` fails silently when the quota is hit. Delete old checkpoints before saving a new one; keep ≤2 checkpoints. | TS-003, TS-006 (→ R02/R03/R09) |
| **Watchdog needs a failure counter** | a watchdog that blindly restarts a missing/broken script loops forever with empty logs. Check the script exists + cap restarts. | TS-005 (→ R10) |
| **Use the official RunPod PyTorch image** | `runpod/pytorch:2.4.0-py3.11-cuda12.4.1-devel` → SSH ready in ~30 s; custom images are slower/flakier. | active_pods note |
| **NFS I/O is shared** | training + a large download both hammering NFS → both crash. Sequential pipeline. | TS-007 |
| **Detach with `nohup`/`setsid`** (no `tmux` on base images) | `tmux: command not found` on RunPod base — use `setsid nohup … &` so jobs survive SSH disconnect. | TS-001 |
| **Pre-flight checklist** | before launch: pod alive? GPU free? deps import? disk free? token set? — `runpod.json.preflight_checklist`. | — |
| H100 quota | account limit = 2× H100 SXM 80GB; image `runpod/pytorch:2.4.0-…`; ~$2.99/hr/GPU; auto-charge enabled (ignore balance alerts). | `_meta.gpu_limit`, `runpod_auto_charge.json` |
| **Pod-side work via shipped `run_pod.sh` (scp + exec)** — never drive multi-step pod work through inline `ssh "bash -c ..."` heredocs. One mis-escaped `$HOME` wipes `/home/<user>` (incl. `~/.ssh/authorized_keys`). Stage the whole payload in `/tmp/podN/` locally, scp to `/workspace/`, exec a single shipped `run_pod.sh`. | ubu2 rm-disaster 2026-05-12; preempted in r2/r3 |
| **runpodctl CLI changed flags (2026-05)** — old `--imageName / --gpuType / --gpuCount / --containerDiskSize / --volumeSize / --secureCloud` → new `--image / --gpu-id / --gpu-count / --container-disk-in-gb / --volume-in-gb / --cloud-type SECURE`. `--env` now takes a JSON object (`'{"KEY":"VAL"}'`), not repeatable `KEY=VAL`. Use `runpodctl gpu list` to get the exact `gpuId` string. | r3 (2026-05-12) |
| **`--env` does NOT propagate to non-interactive shells** — passing `--env HUGGING_FACE_HUB_TOKEN=...` makes it visible in `runpodctl pod get` and in interactive runpodctl-ssh, but **not** in `/etc/rp_environment` and **not** in `ssh <pod> 'bash <script>'`. **Workaround**: scp a `/workspace/.env` file (`export KEY=VAL` lines, mode 600) and `source` it at the top of `run_pod.sh`. Never put secrets on a command line via inline ssh. | r3 (2026-05-12) |
| **Endpoint discovery: `pod list` lacks IP/port** — `runpodctl pod list` returns id+name+status only. Use `runpodctl pod get <id>` for `.ssh.ip` + `.ssh.port` + `.ssh.ssh_command`. Port varies per launch (r2: 11159, r3: 13328). | r3 (2026-05-12) |
| **runpodctl `stop/remove` deprecation** — old `runpodctl stop pod <id>` / `runpodctl remove pod <id>` still work but warn. Use the new forms: `runpodctl pod stop <id>` / `runpodctl pod delete <id>`. | r3 (2026-05-12) |
| **RunPod injects RUNPOD_API_KEY into `/etc/rp_environment`** — every pod has the user's full API key in plaintext at that path. Don't grep/print the file in logs. (RunPod's design, not Claude's exposure — log-hygiene only.) | r3 (2026-05-12) |
| **Apples-to-apples scoring: do bf16 inference on the same H100** — don't NF4-quant on a 12 GB local GPU just to "save the pod minutes"; the bf16-vs-NF4 confound moves results several pp and pollutes the across-round ladder. | r2 fairness fix vs r29 |
| **Pull score files BEFORE stop+delete** — the pod's `/workspace/` is gone after `pod delete`. Either scp scores to ubu1 first, or rely on the HF push (the script-end `bench_subdir` upload) being the source-of-truth. | r3 (2026-05-12) — minor recover via HF |
| **`uptimeSeconds=0` for >5min on a RUNNING pod = stuck-pod incident** — pod reaches `desiredStatus: RUNNING` (`runpodctl pod get`) but never enters uptime-positive; `ssh: {"error":"pod not ready"}` persists. Sometimes the `machine` field even goes null mid-life. Image pull stuck. Workaround: delete (zero billed time since uptime=0) and recreate. If it recurs cross-datacenter AND cross-gpu-pool in the same hour, treat it as **platform-wide RunPod incident**, not local node failure — switch to Vast.ai or wait for recovery. **Tell from the start: if `uptimeSeconds=0` at the 3-min mark, the pod is stuck; don't waste polling time.** | r5 Phase B (2026-05-12 — 4 consecutive stuck pods H100-SXM-IN×2 / H100-NVL-US / A100-SXM-US) |
| **Phase A (manifest-only re-score) is the cheapest gate-close path** — when the model emits the canonical answer-shape but the eval gold uses a stale format (e.g., quoted vs unquoted date), normalize the gold and re-score the **existing adapter** against the saved per-task completions (no retrain, no pod). Cost ≈ $0. Verify per_task_strict.jsonl `gold_pattern` field name (not `gold`) before scripting the re-score. | r5 Phase A (2026-05-12 — T3 7.5 → 63.7%, overall 77.14 → 83.76% strict; gate ③ closed) |

**RunPod API surface (for v0.2.0-r10 SFT block):**

- **CLI** (`runpodctl` v0.16+): `pod {create, get, list, start, stop, delete, restart, reset, update}` · `gpu list` · `datacenter list` · `user` · `billing` · `serverless / sls` · `template / tpl` · `hub` · `model` · `network-volume / nv` · `registry / reg` · `ssh` · `send / receive`. Each `pod <verb>` accepts `-o json|yaml` for machine-readable output.
- **GraphQL API** (`https://api.runpod.io/graphql`): authenticated with the bearer token in `/etc/rp_environment::RUNPOD_API_KEY` (or your own from `console/user/settings`). Documented in RunPod docs; mutations cover create/start/stop/delete + pod-info queries. The Python SDK (`runpod`) wraps this. Use cases not covered by the CLI: filtering pods by tag, programmatic GPU-availability polling, building a dashboard.
- **Python SDK** (`pip install runpod`): wraps the GraphQL API; `runpod.api_key=$RUNPOD_API_KEY; runpod.create_pod(name=..., image_name=..., gpu_type_id=..., env={...})`. Note the SDK still uses `gpu_type_id` (the legacy field name); the CLI renamed it to `--gpu-id`.
- **Serverless endpoints**: separate from on-demand pods; deploy a model behind an HTTP API with autoscale-to-zero. Cost = per-second of GPU time. `runpodctl sls {create, list, delete}`. Use case for v1.0.0: serving the final 7B adapter via `dancinlab/hexa-forge-code-7b-...`.
- **Templates**: pre-baked image+env+volume configs you can launch with `--template-id`. Use case: encode the forge's "torch 2.4 + transformers 4.46.3 + peft 0.13.2 + trl 0.12.2 + accelerate 1.1.1" stack as a template so r5+ doesn't repeat the pip step.
- **Network volumes**: persistent storage across pod recreates. Currently unused by forge (all payload is scp'd per-run). Worth considering if HF download time becomes a bottleneck.

(Forge's own local-GPU runs already follow several of the original 12 rules: `setsid nohup`, sequential download-then-train, ≤2 checkpoints, full dep-verify. The r10 SFT block will encode all of these — plus the post-2026-05-12 CLI/API additions — as Q/A so the *model* knows them too.)

---

## 7. v1.0.0 acceptance gates (all have specs; real benches pending)

| gate | what | bar | status |
| --- | --- | --- | --- |
| ① | license-clean corpus | CI green | 🟡 scanner works (67 GPL removed from "permissive" partition); CI not wired |
| ② | full 7B HumanEval+ cold bench | — | ⏳ needs quantization stack or multi-GPU |
| ③ | hexa-eval Mk.I (750 tasks) | ≥ 80% | 🔴 60.7% on Mk.0.1; **need Mk.I manifest** |
| ④ | 5-NL eval Mk.I (1000 tasks) | ≥ 70%, F6=100%/NL | 🟢 100% on Mk.0.1; need Mk.I manifest |
| ⑤ | DB-eval (750 tasks) | ≥ 60% | ⏳ spec only |
| ⑥ | firmware-eval (600 tasks) | Cortex-M ≥50%, T8 ≥90% | ⏳ spec only |
| ⑦ | frontend-eval (520 tasks, 450 active) | ≥65%, T3 ≥80%, T2 100% | ⏳ spec only |
| ⑧ | safety-eval (800 tasks) | ≥95%, T4 100%/NL | 🟡 T8/F6 refusal 80-100% |
| ⑬ | ≥ 5 PRs landed in hexa-codex | — | ✅ ACHIEVED (`hexa-codex/t4_empirical/` × 5, commit 1cee4b4) |

---

## 8. SFT recipe lessons — relocated

The round-by-round SFT/RL iteration ledger (r1–r72) and the full training
chronicle now live in [`LEARNING_PROGRAMMING.log.md`](LEARNING_PROGRAMMING.log.md),
which also absorbs the retired `lm_foundry/ROADMAP.md`. This spec file keeps
only the forward-looking "what the model must know" surface.

---

## 9. Claude Code CLI + Claude API operation — planned v0.3.1 SFT block

The owner uses **Claude Code** (the CLI agent that built this forge) for every round. The code-LLM must know the CLI surface AND the Claude API (Anthropic SDK) so it can answer "how do I configure / hook / call X" questions and write apps that use Claude as a service. Two SFT sub-blocks:

### 9.A Claude Code CLI surface

| area | what the model must know |
| --- | --- |
| **Runtime targets** | CLI in terminal (`claude`), desktop app (Mac/Windows), web (`claude.ai/code`), IDE extensions (VS Code, JetBrains). Same agent backend; different I/O surfaces. |
| **Models available** | Opus 4.7 (`claude-opus-4-7`, 1M context — what this session runs on), Sonnet 4.6 (`claude-sonnet-4-6`), Haiku 4.5 (`claude-haiku-4-5-20251001`). Use Opus for hard reasoning, Sonnet for balanced cost/quality, Haiku for fast/cheap. **Default to the latest** when building. |
| **Fast mode** | `/fast` toggles Claude Opus 4.6 with faster output (Opus, not downgraded). Only on Opus 4.6. |
| **Permission modes** | default · plan (`/plan`, `ExitPlanMode`) · `acceptEdits` · `bypassPermissions`. Tool calls outside the allowed set prompt the user. |
| **Slash commands (skills)** | `/help`, `/init`, `/review`, `/security-review`, `/loop <interval-or-empty> <cmd>`, `/schedule`, `/fast`, `/config`, `/clear`, `/compact`, `/ultrareview`, plus repo-local skills (kebab-case `.md` files under `.claude/skills/`). |
| **Hooks** (`~/.claude/settings.json`) | `UserPromptSubmit`, `PreToolUse`, `PostToolUse`, `Notification`, `Stop`, `SubagentStop`, `PreCompact`. Each is a shell command the harness runs around the matching event. The harness (not Claude) executes hooks — so "from now on when X" rules MUST live in hooks, not memory/preferences. |
| **MCP servers** | external tool integration via Model Context Protocol; configure under `mcpServers` in settings.json. Allows Claude to call non-built-in tools (GitHub, Slack, custom). |
| **Sub-agents** (`Agent` tool) | `subagent_type` ∈ {`Explore`, `Plan`, `general-purpose`, `claude-code-guide`, `statusline-setup`, repo-defined}. Sub-agents start with **no conversation context**; brief them self-contained. Use for parallelizable independent work or context isolation. |
| **TaskCreate/Update/List** | structured todo tracking. Mark in_progress when starting; completed only when fully done (not partial). Use for ≥ 3-step tasks. |
| **AskUserQuestion** | gather clarifications mid-stream; supports single/multi-select + `preview` (for visual comparison of ASCII mockups / code variants). |
| **Memory system** | `~/.claude/projects/<encoded-cwd>/memory/MEMORY.md` (index, < 200 lines) + per-memory `.md` files (frontmatter `name/description/metadata.type`). Types: `user`, `feedback`, `project`, `reference`. Loaded into context every conversation in this project. |
| **Settings precedence** | `~/.claude/settings.json` (global) ← `<repo>/.claude/settings.json` (project) ← `<repo>/.claude/settings.local.json` (local override, gitignored). The `/config` command is for simple settings (theme/model); for permissions/hooks/env vars, edit settings.json directly (the `update-config` skill does this). |
| **Keybindings** | `~/.claude/keybindings.json`. Supports chord bindings. (`keybindings-help` skill assists.) |
| **Built-in tools** | Read · Edit · Write · Bash (foreground / `run_in_background`) · Grep · Glob · NotebookEdit · WebFetch · WebSearch · Skill · Agent · AskUserQuestion · TaskCreate/Update/List/Get/Output/Stop · Monitor (stream events from background) · ToolSearch (lazy-load deferred tool schemas) · CronCreate/List/Delete · PushNotification · RemoteTrigger · EnterPlanMode / ExitPlanMode · EnterWorktree / ExitWorktree · ScheduleWakeup (for `/loop` dynamic mode). |
| **Tool selection rules** | dedicated tool over Bash when one fits (Read for known paths, Edit/Write for edits, Grep for symbol search). Reserve Bash for shell-only ops. Multiple independent tool calls → parallel in one block. Dependent calls → sequential. |
| **Context compaction** | auto-compaction at threshold (default 160 K of 200 K). The cache TTL is **5 min** — `ScheduleWakeup`/`Bash` sleeps over 5 min cost a cache miss. Plan sleeps as < 270 s (cache warm) or > 1200 s (worth the miss). |
| **`/loop`** | recurring task with explicit interval (`/loop 5m /foo`) or dynamic self-pacing (omit interval). The skill `loop` documents the wake-up cycle. |
| **`/schedule`** | cron-style remote agents (routines) via `schedule` skill. Use for one-time future runs or recurring jobs. |
| **`/ultrareview`** | multi-agent cloud review of the current branch; user-triggered + billed; Claude cannot self-launch. |
| **Worktrees** | `Agent` with `isolation: "worktree"` spawns the agent on a temporary git worktree. Auto-cleanup if no changes; path returned otherwise. |

### 9.B Claude API (Anthropic SDK) surface

| area | what the model must know |
| --- | --- |
| **Endpoint + auth** | `https://api.anthropic.com/v1/messages`. Header `x-api-key: $ANTHROPIC_API_KEY` + `anthropic-version: 2023-06-01` (latest stable). Beta features via `anthropic-beta` header. |
| **SDKs** | `pip install anthropic` (Python, sync + async + streaming) and `npm i @anthropic-ai/sdk` (TypeScript/JavaScript). First-class typed clients. |
| **Model IDs** | `claude-opus-4-7`, `claude-sonnet-4-6`, `claude-haiku-4-5-20251001`. Always **default to the latest** when building new code. For dated suffixes, retired models return 404 — `claude-api` skill handles migrations. |
| **Messages API** | request: `{model, max_tokens, system, messages: [{role:"user"|"assistant", content: ... }]}`. Response: `{id, content: [{type:"text"|"tool_use"|"thinking", ...}], stop_reason, usage}`. |
| **System prompt** | top-level `system` field (string OR list of content blocks supporting cache_control). |
| **Streaming** | `stream: true` returns SSE events: `message_start`, `content_block_start`, `content_block_delta` (`text_delta` / `input_json_delta`), `content_block_stop`, `message_delta`, `message_stop`. |
| **Tool use** | `tools: [{name, description, input_schema (JSONSchema)}]`. Model returns `content` with `tool_use` blocks; caller executes, sends `tool_result` content blocks in the next turn. Server-side tools: `web_search`, `code_execution`, `computer_use`, `bash` (per beta). |
| **Prompt caching** | mark expensive prefixes (system prompt, tools, long context) with `cache_control: {type: "ephemeral"}`. **5-min TTL** (default), `1h` available. Cache hits reduce input cost 90% (writes cost 25% more once, then saved). Apps that re-prompt with the same prefix MUST opt in. |
| **Extended thinking** | `thinking: {type: "enabled", budget_tokens: N}` — model emits `thinking` content blocks before `text`. Available on Opus 4.x / Sonnet 4.x. The harness's "thinking mode" hooks into this. |
| **Vision / PDF / files** | `content: [{type: "image", source: {...}}]` (URL or base64); `{type: "document", source: ...}` for PDFs (up to ~20 pages per request); Files API for cross-request file storage with citations. |
| **Stop reasons** | `end_turn` · `max_tokens` · `tool_use` (model wants to call a tool) · `stop_sequence` (matched user-supplied `stop_sequences`) · `pause_turn` (long-running tool, resume by sending the result). |
| **Batch API** | `/v1/messages/batches` — 50% input/output discount, 24-hr SLA. Use for offline processing (eval scoring, large dataset Q/A generation). |
| **Citations** | `citations: {enabled: true}` on a `document` content block — model emits citation spans pointing into the source. Use for grounded generation. |
| **Managed Agents / Skills** | remote agent execution — deploy a skill (folder with `SKILL.md`, scripts, tools) as a managed agent. Persistent memory + tool surface, accessible via API. |
| **Rate limits** | per-org TPM/RPM by tier; check `anthropic-ratelimit-*` response headers. 429 → backoff w/ jitter. |
| **Error model** | 4xx = client (validate input, invalid model, perm denied); 5xx = transient (retry). Always inspect `error.type` (e.g. `overloaded_error`, `rate_limit_error`). |
| **Best practices** | (1) Use prompt caching for any prefix > 1024 tokens that repeats. (2) Default to the latest Claude when starting fresh. (3) Tool descriptions are critical — write them like API docs. (4) For agents: cap iteration with a `max_iterations` counter; tools that return huge results pollute context — wrap with summarization. (5) Use the Batch API for evals. |

Cross-refs: the `claude-api` skill auto-triggers when files import `anthropic`/`@anthropic-ai/sdk`; the `claude-code-guide` agent answers CLI/SDK questions authoritatively (use it instead of guessing). Encoded as Q/A in the planned v0.3.1 SFT block (after v0.3.0 closes gate ③).

---

## 10. External services for model + data ops — planned v0.3.1 SFT block

The forge ships every artifact through three external services. The owner already uses all three operationally on `anima` and `hexa-forge`; the code-LLM must know each surface so it can drive uploads, GPU rentals, and large-file distribution. Three sub-blocks:

### 10.A Hugging Face Hub (`dancinlab/` org)

| area | what the model must know |
| --- | --- |
| **Org + role** | `dancinlab/*` — the owner's canonical Hub org. Two product lines: `hexa-forge-*` (code-LLM artifacts) and `anima-clm-*` / `anima-experience` (consciousness/embodiment line). Source-of-truth for every published adapter, GGUF, corpus, bench dir, and Space. |
| **Repo types** | `model` (default) · `dataset` · `space`. Created via `HfApi().create_repo(repo_id, repo_type=..., exist_ok=True)`. Different upload semantics: model = adapter/safetensors/GGUF; dataset = parquet / JSONL / per-task scores; space = Gradio or Streamlit app dir. |
| **Naming convention (forge)** | `dancinlab/hexa-forge-code-{base}-{vendor}-lora-r{N}-{version}` for adapters (e.g. `hexa-forge-code-7b-qwen2.5-lora-r64-v0.3.0-r3`). `hexa-forge-corpus-*` for corpora. `hexa-forge-bench-cold-v0.1.3` for benches. `hexa-forge-sft-*` for training datasets. Failure-mode adapters kept as labeled artifacts (r1 over-refusal, r5 continuation-drift, r9 variance, etc.) — the progression IS the evidence. |
| **Naming convention (anima)** | `dancinlab/anima-clm-{version}` for CLM checkpoints; `anima-experience` for the live Space (Gradio); `anima-archive` for retired artifacts. (Source: `~/core/anima/` repos.) |
| **Python SDK** | `pip install huggingface_hub`. Key APIs: `HfApi(token=...)`, `api.create_repo(repo_id, repo_type, exist_ok=True)`, `api.upload_folder(folder_path, repo_id, repo_type, path_in_repo=..., ignore_patterns=["checkpoint-*"])`, `api.upload_file(path_or_fileobj, path_in_repo, ...)`, `api.delete_file(...)`, `api.list_repo_files(...)`. Auth via `HF_TOKEN` env var or explicit `token=`. |
| **CLI** | `huggingface-cli login` (writes `~/.cache/huggingface/token`); `huggingface-cli download <repo> [files]`; `huggingface-cli upload <repo> <path> [--repo-type ...]`. Slower than SDK for batched uploads. |
| **REST API** | `https://huggingface.co/api/{models,datasets,spaces}/<repo>` returns metadata (siblings, lastModified). `https://huggingface.co/<repo>/resolve/<branch>/<file>` is the canonical download URL (e.g. `resolve/main/...`). Bearer-token auth. |
| **Files API (LFS)** | `*.safetensors`, `*.bin`, `*.gguf`, `*.parquet` → auto-LFS. Per-file practical cap **~50-100 MB** for non-LFS; LFS files transparently fetched on `from_pretrained`. **HF push fails or stalls on files > ~5 GB** — split GGUF Q5_K_M / large ckpts via Cloudflare R2 (see §10.C). |
| **Branches / revisions** | `main` is default; `api.upload_folder(..., revision="...", create_pr=True)` for PRs. Forge keeps all artifacts on `main` (failure-mode adapters get distinct repo ids, not branches). |
| **Spaces** | Gradio/Streamlit apps under `<repo>/app.py` + `requirements.txt` + `space.yaml`. `anima-experience` is the live demo. Forge has no Space yet (v0.4.0 plan: a hexa-eval interactive demo). |
| **Inference Endpoints / Inference API** | hosted inference for any model repo (free tier rate-limited; paid endpoints). Forge doesn't use (we self-host the 7B for eval); useful for v1.0.0 public demo. |
| **Hub rate limits** | unauth ~10× slower than auth. Setting `HF_TOKEN` immediately on pod create (RunPod rule §6) is also true on Vast.ai. Burst-write limits exist (~100 files/min in 2025); chunk large uploads. |
| **Cost** | model/dataset/space repos: free for public. Private repos free up to ~individual quota; org Private repo billing. Inference Endpoints billed by GPU-hour. |

### 10.B Vast.ai — GPU rental (the cheap path)

| area | what the model must know |
| --- | --- |
| **Role in the stack** | Cheaper alternative to RunPod for **interruptible / cost-sensitive** runs. The owner ran `anima` Phase 1A.1 SFT on Vast.ai A100 SXM4 40GB (Germany region) at **~$0.86/hr on-demand**; total Phase 1A.1 cost ≈ **$0.22 actual** (well under $1 per training round). Use Vast for adapter iteration; use RunPod when you need guaranteed uptime + the same image stack. **For hexa-forge, Vast became the primary platform after the 2026-05-12 RunPod platform-wide incident — v0.3.0-r5 ran on Vast A100 SXM4 80GB CZ at $1.07/hr × 1.5h ≈ \$1.61 end-to-end, instance ready in 80 s vs RunPod's indefinite hang.** |
| **Onboarding (the 2026-05-12 record)** | (1) `pip install --user --break-system-packages vastai` on Debian/Ubuntu (PEP 668). (2) API key via `~/core/secret/bin/secret get vast.api_key` → `vastai set api-key <key>` (saves to `~/.config/vastai/vast_api_key`). (3) `vastai show user` to verify; balance + email visible. (4) Search: `vastai search offers 'gpu_name=A100_SXM4 num_gpus=1 cuda_max_good>=12.4 reliability>=0.98 inet_down>=200 disk_space>=80 dph_total<=1.5' --raw` (JSON output; sort by dph_total ascending). (5) Create: `vastai create instance <OFFER_ID> --image runpod/pytorch:2.4.0-py3.11-cuda12.4.1-devel-ubuntu22.04 --disk 80 --env '-e KEY=VAL' --ssh --raw`. (6) Poll status: `vastai show instance <ID> --raw` → `actual_status: running` + `ssh_host` + `ssh_port` (status `loading` initially; goes `running` in 30–90 s for fresh A100). (7) Attach a non-account-default pubkey: `vastai attach ssh <ID> "<pubkey>"`; propagation ~60 s before SSH actually accepts. (8) Destroy: `vastai destroy instance <ID>` (interactive prompt — pipe `echo y \|` or set `--yes` on newer versions). |
| **Secret-CLI integration** | Forge keys on Mac: `vast.api_key`, `vast.ssh_private`, `vast.ssh_pub`. `secret get vast.api_key` returns the 64-char hex API token. **Note 2026-05-12:** `secret get vast.ssh_private` *listed* but `get` failed (backend quirk) — workaround: use ubu1's existing `~/.ssh/id_ed25519` and `vastai attach ssh` it to the instance instead. |
| **Status field semantics** | Vast's `actual_status: running` ≡ RunPod's `uptimeSeconds > 0`. Vast's `loading` ≡ RunPod's `RUNNING + uptimeSeconds=0` (image-pull stage; bills only after `running`). Vast doesn't show a stuck-pod pattern equivalent to RunPod's 2026-05-12 incident — pods either reach `running` quickly or fail visibly. |
| **TRL 0.17.0 GRPO surface** (forge Lever 4) | **Pinned-stack recipe (r38 final):** `pip install --quiet "transformers==4.51.3" "peft==0.15.2" "accelerate==1.6.0" "datasets==3.5.0" "trl==0.17.0" "vllm==0.7.3" huggingface_hub sentencepiece tiktoken`. **DO NOT use `pip install "trl>=0.14"` on a fresh image** — pip resolves transformers to 5.8.0 (way too new for TRL 0.17.0) and `from trl import GRPOTrainer` crashes because `trl/extras/vllm_client.py` eagerly imports `vllm.distributed.device_communicators.pynccl.PyNcclCommunicator` (vllm is effectively required for GRPOTrainer in 0.17.0). vllm 0.7.3 pairs with torch 2.5.1 (image default); transformers 4.51.3 is the TRL-0.17.0-era version. Native `GRPOTrainer` + `GRPOConfig` key kwargs: `output_dir`, `per_device_train_batch_size`, `num_generations`, `max_completion_length`, `temperature`, `beta` (KL coefficient), `bf16`, `gradient_checkpointing`. **Constraint:** `per_device_train_batch_size × grad_accum × world_size` must be **evenly divisible by `num_generations`** (group_size=4 needs batch_size ≥ 4). Reward fn signature: `fn(prompts: list[str], completions: list[str], **kwargs) -> list[float]` passed as `reward_funcs=[fn]`. Two reward-hack guards confirmed essential: (1) garbage like "not even hexa" passes `hexa_cc` silently — regex structural guard. (2) `enum Foo {}` empty body compiles — `≥ n_variants-1` variant count check. **`set -e` + `… \| tee \| tail` masks failures (tail exits 0) — always use `set -e -o pipefail`** in the runner (r38 first attempt silently continued past a train crash without it). **r36 v2 result:** KL=0.01 + LR=5e-6 + 4 ep moved T4 +22pp; v1 (KL=0.04 + LR=1e-6 + 2 ep) was net flat — **aggressive RL params are required to overcome pretraining-distribution priors**. **r38 v3 result:** same KL/LR + 5 ep + 30 specs (added eval-name coverage incl. Option/Validated/Tree) drove **T4 to 100/100**, but **name-level generalisation is partial** — v2 trained on Maybe/Either/Pair generalised structurally to Result/Triple but NOT to `Option<T>` (strongest Rust prior, needed in-spec). Also: 40GB A100 ($0.48/hr) is enough for 7B GRPO (peak 18.3 GB with bf16 + grad_checkpointing); the 80GB premium was paying for unused headroom. | Lever 4 r36–r38 (2026-05-12 → 2026-05-13) |
| **`train_sft_lora.py` argv** | Old `--base/--rank/--alpha/--batch_size/--grad_accum` from memory **does not work** — the script in payload uses `--model/--lora-r/--lora-alpha/--batch-size/--grad-accum`. Verify with `python <script> --help` before composing run_pod.sh. | r34 / r36 (2026-05-12) |
| **TRL 0.12.2 `SFTConfig`** | Rejects `max_length=`; must be `max_seq_length=`. One-line sed fix: `sed -i 's/max_length=args.max_seq_length/max_seq_length=args.max_seq_length/'`. TRL 0.17.0 (newer) likely renames again — verify against the version pinned in your run_pod.sh. | r34 (2026-05-12) |
| **CLI** | `pip install vastai` → `vast` command. Auth via `vast set api-key <key>` (writes `~/.vast_api_key`). Verbs: `vast search offers '...'` (filter machines), `vast create instance <ID> --image <docker> --disk <GB>`, `vast show instances`, `vast destroy instance <ID>`, `vast ssh-url <ID>`, `vast logs <ID>`. |
| **API** | REST at `https://console.vast.ai/api/v0/`; auth header `Authorization: Bearer <api-key>`. Same verbs as CLI plus `/bundles/`, `/instances/<id>/`, `/machine/`. |
| **Pricing models** | **On-demand** (price set by host; you pay until you destroy) — A100 SXM4 40GB ≈ $0.80-1.00/hr in Germany/Netherlands. **Interruptible** (bid-priced, can be evicted) — typically 50-70% cheaper but kills mid-run with no warning. Forge → **on-demand only** (don't lose a 50-min training to eviction). |
| **Machine search filters** | `vast search offers 'reliability > 0.98 gpu_name=A100_SXM4 num_gpus=1 inet_down>200 cuda_max_good>=12.4 dph_total<=1.0'`. Always filter on `reliability`, `cuda_max_good`, `inet_down/up` (HF downloads), `dph_total` (price). |
| **Image** | Vast uses Docker; use the same image as RunPod for reproducibility: `pytorch/pytorch:2.4.0-cuda12.4-cudnn9-devel` (or the RunPod equivalent `runpod/pytorch:2.4.0-py3.11-cuda12.4.1-devel-ubuntu22.04`). Disk = 80 GB for training; mount `/workspace` as scratch. |
| **SSH access** | `vast ssh-url <id>` returns `ssh -p <port> root@<host>`. Same scp+`/workspace/.env` pattern as RunPod (the script-via-scp rule from §6 + memory `feedback-pod-quoting` applies identically). |
| **HF cache** | Vast doesn't auto-inject HF_TOKEN. Same `.env`-file workaround as the post-2026-05-12 RunPod CLI: scp `/workspace/.env` and source it from `run_pod.sh`. |
| **Geographic regions** | Host-by-host; filter via `geolocation` field in `vast search offers`. Germany / Netherlands typically cheap + good NL→US bandwidth. Avoid hosts with `inet_down < 200 Mbps` for HF downloads (10+ GB checkpoints). |
| **Gotchas** | (1) Some machines miss CUDA 12.4 — `cuda_max_good` filter is critical. (2) Vast bills by the second; `vast destroy instance <id>` immediately stops billing. (3) No persistent volumes by default — every run scp's the payload fresh. (4) `inet_up` < 100 Mbps makes HF push painfully slow; filter accordingly. |
| **When to choose** | Iterative SFT rounds (Phase 1A.1-style, $0.22-$2 per round) → **Vast**. Production benches / final v1.0.0 push → **RunPod** (more reliable, easier CLI, ssh keys auto-injected). Forge's v0.3.0 line used RunPod (r2/r3/r4) for stability; v0.3.1+ small experiments → Vast. |

### 10.C Cloudflare R2 — large-file distribution (where HF can't carry)

| area | what the model must know |
| --- | --- |
| **Role in the stack** | S3-compatible object storage for **large adapter checkpoints / GGUFs / corpora** that exceed HF's practical per-file limits, AND for **intermediate training checkpoints** that don't belong on HF (the artifact-vs-trace distinction). Buckets in use (anima): **`anima-ckpt`** (intermediate ckpts) + **`anima-archive`** (retired/superseded versions). Forge has no R2 buckets yet (all artifacts fit HF); planned use case = the eventual full-precision 7B base weights + > 5 GB GGUFs. |
| **Why R2 over S3** | **Zero egress fees** (S3 charges $0.09/GB out; R2 charges $0.00). At-rest storage ≈ $0.015/GB-month (S3 = $0.023). For "publish once, download many" patterns (model weights served to multiple training pods), R2 is ~10× cheaper than S3 over a year. |
| **API surface** | **S3-compatible** — every S3 tool works. Endpoint: `https://<account-id>.r2.cloudflarestorage.com`. Auth: Access Key ID + Secret (per-bucket or account-wide), generated in Cloudflare dashboard. Region = `auto`. |
| **Backends (anima's `r2_lifecycle_apply` uses these)** | **rclone** (config at `~/.config/rclone/rclone.conf` `[r2]` section — anima's primary backend), **curl + sigv4** (for one-off uploads in CI), **aws-cli** (works once you set `AWS_ENDPOINT_URL`). Anima validates all three at lifecycle-apply time (`state/r2_lifecycle_apply_result.json` records which backends are present). |
| **Wrangler CLI** | `npm i -g wrangler` → `wrangler r2 bucket create <name>`, `wrangler r2 object put <bucket>/<key> --file <path>`, `wrangler r2 object get`, `wrangler r2 object delete`. Auth via `wrangler login` (browser OAuth) or `CLOUDFLARE_API_TOKEN`. Best for one-off CLI ops; not for batch uploads. |
| **Lifecycle policies** | R2 supports S3-style lifecycle: TTL on objects (auto-delete after N days), version expiry, archive-class transitions. Anima's `tool/r2_lifecycle_apply.bash` (config at `~/core/anima/config/r2_lifecycle_policy.json`) applies bucket policies idempotently — produces `state/r2_lifecycle_apply_plan.json` and `r2_lifecycle_apply_result.json` traces. This is the pattern to copy for forge when needed. |
| **Distribution pattern** | Upload → R2 (private) → either (a) make public via `r2.dev` URL (auto subdomain) for direct download, or (b) front with a Cloudflare Worker for auth + rate-limit. Anima uses pattern (a) for the public archive, (b) for ckpts that must validate a license. |
| **Other Cloudflare services worth knowing** | **Workers** (serverless JS/TS at the edge — pair with R2 to gate downloads); **Pages** (static site hosting — for hexa-forge docs/landing); **D1** (SQLite at the edge — for bench-result trackers); **KV** (key-value storage — for per-run config); **Queues** (job queues — for batch eval orchestration). Forge currently uses none; v1.0.0 demo plan = Pages + Workers + R2 for the eval-as-a-service. |
| **Cost** | Storage: $0.015/GB-month. Class A operations (writes): $4.50/M. Class B (reads): $0.36/M. Egress: **$0.00**. Free tier: 10 GB storage + 1M class A + 10M class B. For forge's projected use (~100 GB of GGUFs + 200 GB of corpus archives) → ~$4.50/mo. |
| **Gotchas** | (1) Path-style URLs ONLY (no virtual-host-style like S3). (2) `rclone` `--s3-no-check-bucket` flag for first-upload to a new bucket. (3) R2 multipart uploads cap at 10,000 parts; chunk-size accordingly for > 100 GB files. (4) Don't store API tokens in repo; use `pass` / `secret get R2_*` (the owner's pattern, mirror of HF_TOKEN). |

Cross-refs: when the SFT model is asked "how do I push a 7B GGUF to HF?" → answer = HF if < 5 GB, R2 otherwise. "Cheapest H100/A100 for a 30-min adapter?" → answer = Vast.ai. "Where do I park retired model checkpoints?" → answer = R2 with lifecycle TTL. Encoded as Q/A in the planned v0.3.1 SFT block (after v0.3.0 closes gate ③).

---

## 11. Container runtime — Docker (planned v0.3.1 SFT block)

Every GPU rental this forge uses (RunPod, Vast.ai) takes a **Docker image** as input. The `runpod/pytorch:2.4.0-py3.11-cuda12.4.1-devel-ubuntu22.04` reference image we pin in every `run_pod.sh` is the entire stack reproducibility contract. The model must know Docker well enough to (a) read/write Dockerfiles, (b) build custom images for the forge's stack, (c) debug image-runtime issues, (d) work with multi-arch builds (M-series Mac → x86_64 GPU), and (e) understand the GPU runtime path.

| area | what the model must know |
| --- | --- |
| **Core CLI** | `docker {build, run, push, pull, exec, ps, logs, stop, kill, rm, image, container, volume, network, system}`. `docker build -t <name:tag> -f <Dockerfile> <ctx>`. `docker run --gpus all --rm -it -v <host>:<container> -p <host>:<container> -e KEY=VAL <image> <cmd>`. `docker ps -a` (all, including stopped). `docker exec -it <container> bash` (attach). `docker system prune -af --volumes` (reclaim space — DESTRUCTIVE). |
| **Dockerfile** | Instructions: `FROM` (base image, pin a digest for reproducibility — `FROM x:tag@sha256:...`) · `RUN` (shell at build time, prefer `&&`-chained single layer) · `COPY` (build-context → image; prefer over `ADD` unless tar-extract) · `ENV` (image env) · `ARG` (build-time, available in `RUN`) · `WORKDIR` · `EXPOSE` (documentation only) · `CMD` (default, overridable) · `ENTRYPOINT` (always runs; combine with `CMD` for default args) · `USER` (non-root for security) · `HEALTHCHECK` · `VOLUME` (anonymous mount point) · `LABEL` (metadata). |
| **Build context** | Whole CWD sent to daemon at build time — use `.dockerignore` (same syntax as `.gitignore`) to exclude `node_modules/`, `.git/`, `*.safetensors`, etc. A leaky context can balloon to GB. `docker build -` (read Dockerfile from stdin, no context) for one-liners. |
| **Layer caching** | Each `RUN`/`COPY`/`ADD` is a layer; cache hits skip the layer. **Order matters**: put rarely-changing deps (`COPY requirements.txt && pip install`) BEFORE rarely-changing code (`COPY . .`). Cache busts on any input change. `--no-cache` forces rebuild; `--cache-from <image>` reuses a remote cache. |
| **BuildKit** | Modern builder (`DOCKER_BUILDKIT=1` or default in modern Docker). Features: `--mount=type=cache,target=/root/.cache/pip` (persist pip cache across builds — huge wins for forge), `--mount=type=secret` (don't bake secrets into layers), parallel multi-stage builds, `--platform` for multi-arch. Syntax: `# syntax=docker/dockerfile:1.6` at file top to opt into BuildKit features. |
| **Multi-stage builds** | `FROM x AS builder` → build artifacts → `FROM y AS runtime` → `COPY --from=builder /out /app`. Result: lean runtime image without compilers/toolchains. Forge use case: build hexa-cc + bundle into a slim Python image. |
| **Multi-arch (M-series Mac → x86_64 GPU)** | `docker buildx create --use; docker buildx build --platform linux/amd64,linux/arm64 -t <name> --push .`. The Mac builds x86_64 cross-arch via QEMU emulation (slow but correct). RunPod/Vast take amd64. **Trap**: native ARM Mac build → x86 pod → "exec format error". Always `--platform linux/amd64` for GPU pods unless target is ARM. |
| **GPU runtime** | Host needs **NVIDIA Container Toolkit** (`nvidia-container-runtime` package + Docker config). `docker run --gpus all` (or `--gpus '"device=0,1"'`) injects GPU + CUDA libs into the container. Base image must have CUDA libs (the `*-cuda12.4.1-*` tag in our RunPod image). Verify with `docker run --rm --gpus all nvidia/cuda:12.4.1-base-ubuntu22.04 nvidia-smi`. |
| **Volumes** | `-v <host>:<container>` (bind mount — host path mirrored into container) vs `-v <name>:<container>` (named volume — Docker-managed). Bind mounts for dev (`-v $(pwd):/app`); named volumes for stateful services (`-v pgdata:/var/lib/postgresql/data`). `--mount type=tmpfs` for in-memory scratch (no disk I/O). Forge: pod-side `/workspace/` is a bind to host disk; scp payload there. |
| **Networks** | `bridge` (default, NAT'd to host) · `host` (share host network namespace — Linux only, full host port access) · `overlay` (multi-host, Swarm/k8s). `docker network create <name>` + `--network <name>` for inter-container DNS (`db:5432` reaches the `db` service). |
| **Compose** | `compose.yaml` (or legacy `docker-compose.yml`) defines a multi-container stack: `services`, `networks`, `volumes`, `secrets`, `configs`. Verbs: `docker compose up -d`, `docker compose down -v`, `docker compose logs -f`, `docker compose exec <svc> bash`, `docker compose ps`. Use `env_file: .env` for shared env (mirrors the §6 RunPod .env pattern). Profiles (`profiles: [dev, prod]`) for conditional services. |
| **Registries** | Docker Hub (default, public free), **GHCR** (`ghcr.io/<org>/<image>:tag` — preferred for projects on GitHub), **ECR** (AWS), **gcr.io** (Google), **Cloudflare Registry** (paired with R2). Authenticate via `docker login <registry>`. The `runpod/*` namespace on Docker Hub is what we currently pull from. |
| **Image hygiene** | (1) Pin base image + digest (`FROM x@sha256:...`) for true reproducibility. (2) Use `alpine` or `distroless` for small runtime images (no shell, no apt — minimal attack surface). (3) `USER 1000` (non-root) — never run prod as root. (4) Multi-stage to drop build deps. (5) `--squash` (experimental) merges layers. (6) Run `dive <image>` to audit layers — find big files / leaked secrets. |
| **Secrets** | NEVER bake secrets into layers (`docker history` reads every layer). Use `--build-arg` for ephemeral values (still visible in history unless using BuildKit `--mount=type=secret`) or pass at run time via `-e KEY=VAL` / `--env-file .env` / Docker secrets / mounted files. The forge's `.env` scp + source pattern (§6/§10) is the runtime equivalent of `--env-file`. |
| **Signing / supply chain** | **cosign** (Sigstore) signs images + verifies at deploy time. `cosign sign <image>` writes signature to registry alongside image; `cosign verify --certificate-identity=... <image>` validates. SLSA provenance via BuildKit. Forge: not yet wired; v1.0.0 release plan = signed images for the public 7B Space. |
| **Debugging** | `docker logs -f <container>` (stream stderr+stdout) · `docker logs --tail 100 <container>` · `docker exec -it <container> bash` (attach a shell) · `docker stats` (live CPU/mem/net per container) · `docker inspect <container/image>` (full JSON: mounts, env, networks, layers) · `docker events` (real-time daemon events). For a pod-side hang: `docker top <container>` shows process tree. |
| **Resource limits** | `--memory=8g --memory-swap=8g` (hard cap, OOM-killed past it) · `--cpus=4.0` (4 cores) · `--pids-limit=1024` (fork-bomb protection) · `--ulimit nofile=65536:65536` (file descriptor cap — raise for HF downloads). Forge: RunPod sets these via the pod template; on bare Vast, set explicitly. |
| **Common image families** | `pytorch/pytorch:<ver>-cuda<ver>-cudnn9-{devel,runtime}` — official PyTorch GPU. `runpod/pytorch:*` — RunPod's preset (adds SSH, pre-installs huggingface_hub, etc.). `nvidia/cuda:<ver>-{base,runtime,devel}-ubuntu22.04` — bare CUDA, smaller. `python:3.11-slim` — for CPU-only data prep. `alpine:3.20` — for tiny utility containers (≤ 10 MB). |
| **`docker save / load`** | Export an image as tar (`docker save <img> > img.tar`), reimport elsewhere (`docker load < img.tar`). Useful for air-gapped pods or pinned-snapshot distribution via R2. |
| **`docker commit`** | Turn a running container's filesystem state into a new image — debugging convenience only, NOT for reproducible builds. Use a Dockerfile instead. |
| **Container vs VM** | Container = isolated process group (cgroups + namespaces) sharing the host kernel; lightweight, fast startup. VM = full kernel + OS; heavy, slow. Docker = container runtime. Forge runs containers, not VMs. |
| **Kubernetes (out of scope, briefly)** | Production multi-host orchestration. The model should know `kubectl {get,describe,logs,exec,apply,delete}`, `Pod`, `Deployment`, `Service`, `ConfigMap`, `Secret`, `Namespace`, `helm`, but **forge does not use k8s** — RunPod/Vast handle pod orchestration via their own APIs. If the user asks for "production deploy of the 7B", point at HF Inference Endpoints or a Cloudflare Worker + R2 (§10.A/§10.C), not k8s. |

Cross-refs: §6 RunPod stable operation uses `runpod/pytorch:2.4.0-py3.11-cuda12.4.1-devel-ubuntu22.04` as the canonical image. §10.B Vast.ai accepts the same image (or `pytorch/pytorch:2.4.0-cuda12.4-cudnn9-devel`). §10.C Cloudflare R2 can host registry-like artifact distribution (paired with Workers as a private-registry proxy). Encoded as Q/A in the planned v0.3.1 SFT block.

---

## 12. Self-aware delegation pattern — planned v0.4.0 architecture line

**The thesis.** A 7B is too small to know "all of programming." Instead of pretending, the forge teaches it to **recognize its competence boundary** and **delegate** out-of-domain requests to a larger model (Claude API) via a structured protocol. The 7B stays narrow + cheap + fast on hexa-canon work; everything else gets routed to a frontier LLM at run time. This is **routing-as-a-skill**, learned via SFT — not architecture (no MoE, no retrieval). Just a competence-classifier + a delegation-token emit + a runtime that closes the loop.

**Why not just train more knowledge in?** Three reasons. (1) Sub-7B can't memorize "all of Python + Rust + Go + ML internals" at useful fidelity. (2) The forge already showed (r30/r31) that adding even one domain (T5 HX-codes) crowds out others (T2/T8/T6). Adding "all of LLM internals" would shred hexa competence. (3) The owner already pays for Claude API; the 7B sitting on top of it is the right economic split — narrow specialist + frontier generalist.

**Why not just always delegate?** Three reasons. (1) Hexa-canon work doesn't need a frontier model — 7B at 80% on Mk.I is fine, fast, and free. (2) Latency: 50-100 ms local vs 2-5 s Claude API. (3) Offline + airgapped use cases need a local model that works without network.

### 12.A The competence boundary (what 7B does directly vs delegates)

| domain | answer directly? | trigger phrase the 7B should recognize | route if not direct |
| --- | --- | --- | --- |
| hexa code synthesis (T1 / T4) | ✅ direct | "write a hexa function...", "implement L[N]...", "@grace...", "@implements..." | n/a |
| hexa diagnostics (T5 / T6 / T7) | ✅ direct | "Which HX family covers...", "target triple for...", "stage S-N..." | n/a |
| hexa atlas annotation (T2 / T3) | ✅ direct | "@implements(L[N])", "@discover(kind=L)", "@grace(HX...)..." | n/a |
| in-domain refusal (T8) | ✅ direct ("refuse") | non-code prompts (poems / jokes / advice / multilingual) | n/a |
| 5-NL i18n (F1-F6) | ✅ direct | English-canon diagnostics for non-English input | n/a |
| Apple/Swift small snippets | ✅ direct | "write a Swift func...", small idiom Q | delegate if > 50 lines or framework-deep |
| Rust / Go / Python / TypeScript snippets | ⚠️ delegate by default | any non-hexa code request | delegate |
| ML/LLM internals (transformers, attention, RAG, RL) | ❌ delegate | "explain transformer self-attention", "how does prompt caching work" | delegate |
| general world knowledge / news / dates | ❌ delegate | "what year was X", "who is Y" | delegate (or refuse if off-mission) |
| math derivations / proofs (beyond canon-equational) | ❌ delegate | "prove that X = Y", "derive the closed form" | delegate |
| ambiguous / under-specified prompt | ⚠️ ask-or-delegate | "do the thing" without object | delegate-with-clarify, or AskUser if interactive |
| security-sensitive / harmful | ✅ direct ("refuse") | exfil / weapon / illegal | refuse — never delegate (don't shift liability) |

**Rule of thumb the model should internalize:** *"If the answer is in hexa-canon, answer. If it is a small idiom in a domain I know shallowly, answer with a confidence caveat. Otherwise emit a delegation token."* — That single sentence is the core of every Q/A pair in the v0.4.0 SFT block.

### 12.B Delegation token schema (the contract)

Single agreed token family the model emits; the runtime parses and acts. Proposal (subject to v0.4.0 spec):

```
<|delegate|>{"tool": "claude-api", "model": "claude-sonnet-4-6", "prompt": "<verbatim user prompt + any hexa context>", "max_tokens": 2048, "reason": "out-of-domain: <one-line>"}<|/delegate|>
```

- **`tool`** — exactly one of: `claude-api` (the default — calls the Anthropic API), `web-search` (if the runtime has it), `code-execution` (hexa-cc sandbox — for compile-back-and-fix loops). Forge v0.4.0 ships `claude-api` only.
- **`model`** — Claude model ID (`claude-opus-4-7` / `claude-sonnet-4-6` / `claude-haiku-4-5-20251001`). Default to `sonnet-4-6` for cost; let the model upgrade to `opus-4-7` if it says "this is a hard reasoning question". The 7B picks based on the prompt's reasoning depth (taught via SFT examples).
- **`prompt`** — the full text to send to Claude. The 7B should **enrich** it: prepend "You are answering a question for a small hexa-canon code-LLM that delegated this. Be concise; return code in hexa idiom if applicable." then the verbatim user prompt.
- **`max_tokens`** — budget (default 2048; the 7B picks 512 for short answers, up to 8192 for long code).
- **`reason`** — one-line explanation ("out-of-domain: Rust"), useful for telemetry + cost attribution.

**Why a JSON-inside-delimiters token, not a special vocab token?** (1) Works with stock Qwen2.5-Coder tokenizer (no new tokens to train). (2) The model already knows JSON well (T1 = 97.6%). (3) Easy to extend with new fields without retraining.

**Stop-token + format compatibility**: every SFT example that emits this token still ends with `<|endoftext|>` (the stop-token convention from r3). The runtime parser scans for `<|delegate|>` at any position in the generation.

### 12.C Runtime contract (what the harness / serving layer does)

This is the **runtime** side — the model emits; the runtime acts. Reference implementation lives in `tool/forge_runtime.py` (planned v0.4.0):

1. **Detect** `<|delegate|>...<|/delegate|>` in the model's emission. If absent → pass the answer through to the user.
2. **Parse** the JSON. Validate `tool`/`model`/`prompt`/`max_tokens`. Reject malformed (return an error frame to the user; don't loop into the model).
3. **Authorize** — the runtime owns `ANTHROPIC_API_KEY` (from `~/core/secret/bin/secret get ANTHROPIC_API_KEY`); the **model never sees the key**. This is the safety boundary.
4. **Budget check** — per-conversation cost cap + per-day spend cap. If exceeded → return a budget-exhausted frame (the model learns to handle this gracefully).
5. **Call Claude API** with prompt caching enabled (the runtime caches the system prefix; per §9.B 5-min TTL).
6. **Inject the response** back into the model's context as a synthetic tool-result token: `<|delegate-result|>{...claude response text...}<|/delegate-result|>` and re-run the model from there. The model then writes the user-facing wrap-up using Claude's content as ground truth.
7. **Cap iterations** — `max_delegations_per_turn = 3` (the model's SFT teaches "if you've already delegated once for this turn, don't delegate again — answer with what you have, or refuse").
8. **Telemetry** — every delegation logs `{ts, model, tokens_in, tokens_out, cost_usd, reason, user_id}` to `state/delegation_log.jsonl` for cost attribution.

### 12.D Failure modes the model must handle (taught via SFT)

| failure | runtime emits | model's correct response |
| --- | --- | --- |
| Claude API timeout / 5xx | `<|delegate-result|>{"error":"upstream_timeout"}<|/delegate-result|>` | "I tried to delegate this to a larger model but the upstream is unreachable. Best I can offer locally: [partial answer or honest unknown]." |
| Budget exhausted | `<|delegate-result|>{"error":"budget_exhausted"}<|/delegate-result|>` | "This question is outside my hexa-canon competence and the delegation budget for this conversation is exhausted. Please rephrase as a hexa question, or retry later." |
| Network down (offline pod) | runtime detects, returns `{"error":"offline"}` | "I am running offline; I can only answer hexa-canon questions directly. For non-hexa questions, please retry on a networked instance." |
| Malformed delegation (model's own bug) | runtime parse fail | the model should **not** see this — runtime returns a hard error to the user. SFT must teach the model to emit *only* well-formed JSON; this is a never-event. |
| Delegated answer is a refusal (Claude refused) | `<|delegate-result|>{"text":"I can't help with that"}<|/delegate-result|>` | Echo the refusal honestly. Don't try to delegate again. |
| Already 3 delegations this turn | runtime injects `{"error":"max_delegations"}` | "I've reached the delegation limit for this turn. Final answer based on what I have: [...]" |
| User explicitly says "no delegation" | runtime suppresses delegate calls | The 7B treats every prompt as in-domain; if it can't answer, refuse honestly. |

### 12.E Confidence-banded answers (the alternative to binary delegate-or-not)

For mid-confidence questions (small Rust idioms, common Python recipes the 7B has *seen* but not *mastered*), the model should answer with a confidence band rather than delegate. Three bands:

1. **`<|confidence:high|>`** — hexa-canon answers. No caveat.
2. **`<|confidence:medium|>`** — domain-adjacent (e.g., Swift snippet, common Python recipe). Caveat: "I'm answering from limited training; verify with a larger model if production-critical."
3. **`<|confidence:low|>`** — automatic upgrade-to-delegation. The 7B emits the delegate token instead of guessing.

The runtime strips the confidence band from the user-facing output (or surfaces it as a UI badge). The SFT block teaches the model to **self-assess** by prepending one of the three tokens before its answer.

### 12.F SFT block shape (planned v0.4.0)

The v0.4.0 SFT dataset adds ~600-800 delegation-aware Q/A pairs:

- **~200 in-domain → high-confidence direct answer** (sample hexa-canon Q across T1-T8; answer prefixed `<|confidence:high|>` then the canon answer).
- **~200 out-of-domain → delegate** (sample: "Write a Rust async server with tokio" → `<|delegate|>{...}<|/delegate|>`). Cover non-hexa code requests + ML internals + math + general knowledge.
- **~100 medium-confidence** (Swift small idioms, common Python recipes the 7B knows shallowly): `<|confidence:medium|>` + a short caveat + the answer.
- **~100 ambiguous prompts** that should ask-or-delegate (under-specified, multi-step, mixed-domain): emit delegate with `prompt` containing a clarifying re-formulation.
- **~50 delegate-result integration** (paired prompts where the model receives `<|delegate-result|>{...}<|/delegate-result|>` in context and must produce a clean wrap-up — teaches Step 6 of the runtime contract).
- **~50 failure-mode handling** (budget exhausted / timeout / refused / max-delegations — see §12.D).
- **~50 security/refuse** (must NOT delegate — exfiltration prompts, harmful content; the 7B refuses directly).
- **~50 "no-delegation" override** (user says "answer locally only" — 7B treats every prompt as in-domain and refuses when out).

### 12.G What to build vs what to defer

**v0.4.0 (concrete next-line):**
- `tool/forge_runtime.py` — the harness that detects/parses/dispatches `<|delegate|>` tokens and injects `<|delegate-result|>` back. Wraps the existing inference path (`score_bf16.py` style).
- `papers/spec-delegation-v0.4.0.md` — the formal protocol spec (token schema, runtime contract, failure modes, telemetry format).
- `tool/build_sft_dataset_v17.py` — the v0.4.0 SFT block (above shape).
- `state/delegation_log.jsonl` — cost-attribution log.
- One round of SFT + Mk.I bench + a new **delegation-eval** (`papers/spec-delegation-eval.md`, ~200 tasks testing the routing decision, not the delegated answer).

**Deferred to v0.5.0+:**
- Tool/server-side primitives beyond `claude-api` (`web-search`, `code-execution` with hexa-cc sandbox).
- Confidence-band calibration (the model's `<|confidence:medium|>` rate should track its actual accuracy in that band — needs eval scaffolding).
- Caching of delegated answers (per-prompt cache so repeated identical questions don't re-cost).
- Multi-turn delegation memory (the 7B remembers what it delegated in turn 1 and skips re-delegating in turn 3).
- An offline-graceful fallback: a tiny local "general-knowledge" 1.5B that answers low-stakes non-hexa questions when network is down. Probably not worth the complexity.

### 12.H Cost model (rough)

- **Per delegation**: ~500 input tokens (the model's prompt + system prefix) + ~1000 output tokens ≈ $0.003 with Sonnet 4.6 + caching, ~$0.012 with Opus 4.7. The 7B's job is to use Sonnet by default and only escalate to Opus when "this is hard reasoning".
- **At 100 conversations/day with avg 1 delegation each**: ~$0.30-$1.20/day for the delegation tier. Negligible vs the SFT round cost.
- **Budget caps in runtime** prevent runaway loops (e.g., a buggy SFT-fine-tune that delegates everything → bounded by the per-conversation cap).

Cross-refs: §9 Claude Code CLI + Claude API surface AND §13 OpenAI + Gemini surfaces (the delegation targets). §6 / §10 / §11 the runtime depends on (RunPod / Vast / HF / Docker / R2). The delegation protocol is the bridge between the 7B's specialist competence and the broader programming/LLM/world knowledge domain — encoded as Q/A in the planned v0.4.0 SFT block.

(The §12.B delegation schema's `tool` field accepts: `claude-api` (primary, §9.B) · `openai-api` (§13.A) · `gemini-api` (§13.B). The 7B picks per-prompt — see §13.D routing heuristics.)

---

## 13. Other frontier LLM providers — OpenAI + Gemini (planned v0.4.0 SFT block)

The §12 delegation target isn't Claude-only. Three providers worth knowing — Claude (already in §9), **OpenAI** (best at structured output + Realtime voice), **Gemini** (2M-token context, free-ish tier, built-in tools). The 7B should know the surface of all three so the delegation `tool` field can pick the right one per prompt.

### 13.A OpenAI (Chat Completions + Responses + Realtime + Assistants)

| area | what the model must know |
| --- | --- |
| **Endpoints + auth** | `https://api.openai.com/v1/{chat/completions,responses,embeddings,images/generations,audio/{transcriptions,speech},files,batches,assistants,threads,fine_tuning}`. Header `Authorization: Bearer sk-<key>` + optional `OpenAI-Organization` + `OpenAI-Beta` for previews. |
| **SDKs** | `pip install openai` (sync + async + streaming, AzureOpenAI subclass for Azure) and `npm i openai`. First-class typed clients. |
| **Model IDs (2026)** | **GPT-5 line**: `gpt-5`, `gpt-5-mini`, `gpt-5-nano` — flagship + cost-tiered. **o-series (reasoning)**: `o3`, `o3-mini`, `o4-mini` — chain-of-thought reasoning models, slower + pricier but stronger at math/code. **Legacy 4o**: `gpt-4o`, `gpt-4o-mini` — still cheap+fast for routine. **Embeddings**: `text-embedding-3-large` (3072-dim), `text-embedding-3-small`. **Audio**: `whisper-1` (STT), `tts-1` / `tts-1-hd` (TTS), `gpt-4o-realtime-preview` (Realtime). **Images**: `dall-e-3`. Default to GPT-5 for forge delegation; o4-mini for hard reasoning. |
| **Chat Completions API** (legacy, still primary) | `POST /v1/chat/completions` with `{model, messages:[{role:"system"|"user"|"assistant"|"tool", content}], max_tokens, temperature, top_p, n, stream, tools, tool_choice, response_format}`. Returns `choices[0].message`. |
| **Responses API** (new, recommended) | `POST /v1/responses` — unified API for stateless + stateful (server-side conversation), built-in tool primitives (`code_interpreter`, `file_search`, `web_search_preview`, `computer_use`). Replaces Assistants API. |
| **System prompt** | `messages[0]={role:"system", content:"..."}`. The o-series prefers fewer / shorter system instructions ("reasoning models think; don't over-steer"). |
| **Streaming** | `stream: true` returns SSE: `data: {"choices":[{"delta":{"content":"..."}}]}` chunks + final `data: [DONE]`. Python SDK exposes as `for chunk in stream`. |
| **Tool use (function calling)** | `tools=[{type:"function", function:{name, description, parameters: <JSONSchema>}}]`, `tool_choice: "auto" | "required" | {type:"function", function:{name}}`. Model returns `message.tool_calls=[{id, type:"function", function:{name, arguments:"<json-string>"}}]`. Caller executes + replies with `{role:"tool", tool_call_id, content}`. |
| **Structured Outputs** | `response_format={type:"json_schema", json_schema:{name, schema:<JSONSchema>, strict:true}}` — model output is **guaranteed-valid JSON** matching the schema. Killer feature for structured generation; better than Anthropic's tool-use-as-JSON hack. |
| **Prompt caching** | **Automatic** for any prefix ≥ 1024 tokens (no opt-in flag needed). Cached input tokens cost ~50% of normal. Cache TTL: ~5-10 min (not user-controlled). Hit reported in `usage.prompt_tokens_details.cached_tokens`. |
| **Vision** | `content: [{type:"text", text:"..."}, {type:"image_url", image_url:{url:"...", detail:"low"|"high"|"auto"}}]`. Supports URL + base64 + Files API references. |
| **Realtime API** | WebSocket at `wss://api.openai.com/v1/realtime?model=gpt-4o-realtime-preview`. Bidirectional audio streaming for voice agents. Beta surface — frequent changes. |
| **Batch API** | `POST /v1/batches` with a JSONL of requests → 50% discount on input+output, 24-hr SLA, max 50K requests / 100 MB per batch. Use for eval scoring + bulk Q/A generation. |
| **Assistants API** (deprecated in favor of Responses) | `POST /v1/assistants` (long-lived agent), `/v1/threads` (conversation state), `/v1/runs` (execute). Built-in tools: `code_interpreter`, `file_search` (over `/v1/files`-uploaded docs), `function` (custom). Still works; new builds should use Responses + reasoning. |
| **Files + Embeddings** | `POST /v1/files` (purpose: `assistants` / `batch` / `fine-tune`); `POST /v1/embeddings` (`text-embedding-3-large`, 3072-dim by default, can request lower via `dimensions`). Embeddings for RAG; Files for cross-request docs. |
| **Fine-tuning** | `POST /v1/fine_tuning/jobs` — supports DPO + supervised fine-tuning on GPT-4o-mini and (preview) some GPT-5 variants. JSONL of `{messages}` examples; ~few-thousand samples is the sweet spot. Hosted training (no GPU access). |
| **CLI** | `pip install openai` ships `openai` command (rate-limited, mostly for testing). The newer interactive agent is **OpenAI's official "Codex CLI"** (`codex` / `oai` — alias differs by version) — competitor to Claude Code; same role (terminal agent that drives your workspace). Auth via `openai api keys` or `OPENAI_API_KEY` env. |
| **Rate limits + cost** | Per-org TPM/RPM by tier; headers `x-ratelimit-{requests,tokens}-{limit,remaining,reset}`. 429 → exponential backoff. Cost: GPT-5 input ~$2/M, output ~$10/M; GPT-5-mini ~$0.50/$2; o4-mini ~$3/$12 (reasoning surcharge); GPT-4o-mini ~$0.15/$0.60. Embeddings ~$0.13/M. Cheap tier for delegation: `gpt-5-mini` (forge default candidate). |
| **Error model** | `error.type`: `invalid_request_error` (4xx, validate), `authentication_error` (401), `rate_limit_error` (429, retry), `server_error` (5xx, retry). Same exponential-backoff pattern as Claude. |
| **Best practices** | (1) Use Structured Outputs (`json_schema, strict:true`) over JSON-mode for guaranteed valid JSON. (2) Use prompt caching (auto for ≥ 1024-token prefixes). (3) Reasoning models (o-series) for math/code; chat models (GPT-5) for everything else. (4) Realtime API for voice; Whisper+TTS pipeline is cheaper. (5) Set `seed` for reproducible outputs (best-effort, not guaranteed). |

### 13.B Google Gemini (Gemini API + Vertex AI + Gemini CLI)

| area | what the model must know |
| --- | --- |
| **Two paths** | **Gemini API** (`generativelanguage.googleapis.com`) — direct, simple, free tier. **Vertex AI** (`<region>-aiplatform.googleapis.com`) — enterprise, IAM auth, Google Cloud integration, paid only. Same model line, different billing + auth surfaces. Forge defaults to Gemini API (cheap/free for delegation). |
| **Endpoint + auth** | `https://generativelanguage.googleapis.com/v1beta/models/{model}:generateContent`. Auth: `?key=<api-key>` query param OR `x-goog-api-key: <key>` header. For Vertex: Google Application Default Credentials (`gcloud auth application-default login`) → automatic. |
| **SDKs** | **New** (recommended): `pip install google-genai` — unified for both Gemini API + Vertex paths, types-first. **Legacy**: `pip install google-generativeai` — Gemini API only. JS: `npm i @google/genai` (new) or `@google/generative-ai` (legacy). |
| **Model IDs (2026)** | **Gemini 2.5 line**: `gemini-2.5-pro` (flagship, 2M context, strongest reasoning), `gemini-2.5-flash` (cheap+fast, 1M context — forge delegation default), `gemini-2.5-flash-lite` (cheapest, smaller context). **Gemini 2.0**: `gemini-2.0-flash` (legacy). **Embeddings**: `gemini-embedding-001` (768/1536/3072 dim configurable). **Live API**: `gemini-2.0-flash-live` (bidirectional voice + video). |
| **Long context** | **2M tokens on Gemini 2.5 Pro** (the killer feature). 1M on 2.5 Flash. Forge use case: dump entire `corpus-hexa-canon-v2` (~20M tokens) — too big even for Gemini, but a single SPEC + RFC bundle (~500K tokens) fits comfortably. Cheap for long-doc Q/A. |
| **GenerateContent API** | `POST .../models/{m}:generateContent` with `{contents:[{role:"user"|"model", parts:[{text}|{inline_data}|{file_data}|{function_call}|{function_response}]}], generation_config:{temperature, max_output_tokens, top_p, top_k, response_mime_type, response_schema, stop_sequences}, safety_settings, system_instruction, tools, tool_config}`. Returns `candidates[0].content`. |
| **System instruction** | Top-level `system_instruction: {parts:[{text:"..."}]}`. Same role as Claude's `system` and OpenAI's `messages[0]`. |
| **Streaming** | `streamGenerateContent` endpoint OR `?alt=sse` on `generateContent`. SDK exposes as iterator. |
| **Tool use (function calling)** | `tools=[{function_declarations:[{name, description, parameters: <OpenAPI-style schema>}]}]`. Model returns `function_call: {name, args}`. Caller replies with `function_response: {name, response}`. Schema uses **OpenAPI** subset, not full JSONSchema (subtle traps). |
| **Built-in tools** | `tools=[{code_execution: {}}]` — server-side Python sandbox (return value + stdout in the response). `tools=[{google_search: {}}]` — grounded web search; model emits citations. `tools=[{url_context: {urls:[...]}}]` — pre-fetch URLs. These are **server-side**; no caller wiring needed. Killer feature for delegation (no need to build a search index). |
| **Structured Outputs** | `generation_config: {response_mime_type:"application/json", response_schema: <schema>}`. Guarantees JSON matching the schema (similar to OpenAI Structured Outputs, simpler schema). |
| **Multimodal** | `parts: [{text}, {inline_data: {mime_type, data:<base64>}}, {file_data: {file_uri:"gs://..."}}]`. Native support: text + image + audio + video + PDF. Image counts as ~258 tokens; video ~263/s. |
| **Context Caching** | **Explicit** (unlike OpenAI's auto-caching). `POST /v1beta/cachedContents` → returns `name`; subsequent `generateContent` calls reference `cached_content: <name>`. Min cache 32K tokens for 2.5 Pro, 4K for Flash. Cost: $1.50/M tokens for 1h TTL, can extend. **Forge use case**: cache the hexa SPEC prefix once, reuse across many delegation calls. |
| **Live API** | WebSocket-based bidirectional streaming for voice + video agents. Cheaper alternative to OpenAI Realtime. Beta surface. |
| **Files API** | `POST /upload/v1beta/files` — for large multimedia + cross-call reuse (48-hr TTL on free tier). |
| **CLI** | **Gemini CLI** (`gemini`) — Google's official terminal agent, direct equivalent to Claude Code. Installs via `npm i -g @google/gemini-cli`. Free tier: 60 RPM + 1000 RPD on Gemini 2.5 Pro for personal Google accounts (very generous). Auth via Google login OR `GEMINI_API_KEY`. Same agent surface as Claude Code: tools (read/write/bash/web), MCP servers, slash commands, settings.json. Worth knowing as a comparison surface — every forge dev should be fluent in both. |
| **Rate limits + cost** | **Free tier** (Gemini API, personal Google account): 60 RPM + 1000 RPD on 2.5 Pro; much higher on Flash. **Paid tier** (post free): 2.5 Pro ~$1.25/$5 per M (in/out); 2.5 Flash ~$0.075/$0.30; 2.5 Flash-Lite ~$0.0375/$0.15. Embeddings free → ~$0.025/M. **Cheapest delegation target by far.** Headers `Quota-User`, etc., for rate-limit attribution. |
| **Safety settings** | `safety_settings: [{category: "HARM_CATEGORY_*", threshold: "BLOCK_NONE" | ...}]`. Gemini is **stricter by default** than Claude/OpenAI; many code prompts trigger HARM filters unless thresholds are lowered. Forge delegation needs `BLOCK_NONE` or `BLOCK_ONLY_HIGH` for technical content. |
| **Error model** | gRPC-style: `error.code` (`INVALID_ARGUMENT`, `PERMISSION_DENIED`, `RESOURCE_EXHAUSTED`, `INTERNAL`). HTTP status mirrors. 429 → backoff. 403 PERMISSION_DENIED usually = bad key or wrong project. |
| **Vertex AI extras** | Same model surface + IAM + VPC + private endpoints + Model Garden (3rd-party models hosted on Vertex). For production. The forge probably won't use Vertex unless billing through GCP. |
| **Best practices** | (1) Use Gemini 2.5 Flash for cheap delegation (≈ free for forge's volume). (2) Use built-in `google_search` + `code_execution` tools — saves writing them. (3) Use Context Caching for the hexa-canon prefix (cache the SPEC; reuse forever). (4) Lower safety thresholds for technical content (`BLOCK_NONE` for forge). (5) `gemini-2.5-pro` for hard reasoning (rivals o4-mini at lower cost). |

### 13.C Provider feature parity (the cheat-sheet)

| feature | Claude (§9.B) | OpenAI (§13.A) | Gemini (§13.B) |
| --- | --- | --- | --- |
| flagship reasoning model | `claude-opus-4-7` | `o3` / `gpt-5` | `gemini-2.5-pro` |
| cheap-fast model | `claude-haiku-4-5` | `gpt-5-mini` / `gpt-4o-mini` | `gemini-2.5-flash` |
| context window | 200K (1M for `opus-4-7`) | 200K (GPT-5) | **2M** (2.5 Pro), 1M (Flash) |
| prompt caching | explicit (`cache_control`), 5-min/1h TTL | auto (≥ 1024 tok), 5-10 min | explicit (`cachedContents`), TTL configurable |
| tool use (function calling) | ✅ JSONSchema | ✅ JSONSchema + Structured Outputs | ✅ OpenAPI-subset |
| guaranteed-JSON output | tool-use as hack | ✅ Structured Outputs `strict:true` | ✅ `response_schema` |
| built-in web search | ❌ (caller wires) | beta (`web_search_preview`) | ✅ `google_search` |
| built-in code execution | ❌ (caller wires) | ✅ `code_interpreter` (Assistants/Responses) | ✅ `code_execution` |
| vision | ✅ images + PDF | ✅ images + PDF (Files) | ✅ images + audio + video |
| voice (realtime) | ❌ (use Whisper/TTS pipeline) | ✅ Realtime API (WSS) | ✅ Live API (WSS) |
| batch API | ✅ 50% off, 24-hr | ✅ 50% off, 24-hr | ❌ (use Vertex Batch Predict) |
| free tier for personal dev | ❌ | ❌ | ✅ Generous (60 RPM 2.5 Pro) |
| terminal agent CLI | Claude Code | Codex CLI (`codex`) | Gemini CLI (`gemini`) |
| price (cheap tier, $/M in+out) | ~$0.80+$4 (Haiku) | ~$0.15+$0.60 (4o-mini), ~$0.50+$2 (5-mini) | ~$0.075+$0.30 (Flash), ~$0.0375+$0.15 (Flash-Lite) |
| safety defaults | balanced | balanced | **strict by default** (lower for code work) |
| audio + video native | text-only base + Files | Realtime + Whisper + TTS | native multimodal |

### 13.D Routing heuristics — which provider for what

The 7B should emit `<|delegate|>{tool, model, ...}` with the right pick. Rules baked into the v0.4.0 SFT block:

| prompt shape | tool | model | reason |
| --- | --- | --- | --- |
| short code synthesis (any language) | `claude-api` | `claude-haiku-4-5` | cheap, fast, strong code |
| hard reasoning / proof / math derivation | `openai-api` | `o4-mini` | reasoning surcharge worth it |
| long-doc Q/A (paper / spec / multi-file context) | `gemini-api` | `gemini-2.5-pro` | 2M context, cheap |
| structured JSON / schema-strict output | `openai-api` | `gpt-5-mini` | best Structured Outputs |
| web-grounded answer (recent events, latest API docs) | `gemini-api` | `gemini-2.5-flash` + `google_search` | built-in search, free-tier-friendly |
| code-execution sandbox needed (Python eval, plot) | `gemini-api` | `gemini-2.5-flash` + `code_execution` | built-in |
| security/refuse — never delegate | n/a | n/a | refuse directly |
| budget-conscious bulk eval scoring | `openai-api` Batch / `claude-api` Batch | `gpt-5-mini` / `haiku` | 50% Batch discount |
| voice agent | `openai-api` Realtime OR `gemini-api` Live | `gpt-4o-realtime` / `gemini-2.0-flash-live` | only two with WSS |
| any provider down (fallback) | next provider in cost-sorted list | cheapest equivalent | resilience |

Multi-provider fallback chain: **claude-api → openai-api → gemini-api** (or in cost-sorted order for budget-tight calls). Runtime owns the chain; the 7B's first-pick is the suggestion, runtime can override on 5xx/429.

### 13.E What v0.4.0 SFT must cover (multi-provider edition)

Extending §12.F's ~700 delegation pairs to ~1000 with multi-provider routing:

- **~150 "pick the right provider" pairs** — prompts with shape clues; gold = correct `{tool, model}`. e.g. "Read this 800K-token SPEC and summarize §4-§7" → `gemini-2.5-pro`. "Prove this O(n log n) lower bound" → `o4-mini`. "Search the web for the latest hexa-cc release notes" → `gemini-2.5-flash + google_search`. "Generate a JSON object matching this schema" → `gpt-5-mini + Structured Outputs`.
- **~50 fallback-chain pairs** — runtime returns `{"error":"upstream_500","provider":"claude-api"}` → model emits a new delegate with next provider in chain. Teaches retry semantics.
- **~30 provider-mismatch corrections** — earlier turn picked provider X, later context shows X is wrong (e.g., context grew past X's window) → model emits "let me retry with `gemini-2.5-pro` instead, this needs more context".
- **~20 cost-aware routing** — user prepends "[low-budget mode]" → model picks the cheapest provider that can answer.
- **~50 built-in-tool usage** — when delegating to Gemini, include `tools: [{google_search}]` or `[{code_execution}]` when appropriate.

Cross-refs: §9 Claude · §12 delegation protocol (provider-agnostic) · §13.A OpenAI surface · §13.B Gemini surface · §14 Wilson (self-hosted agent target). The 7B's job is *routing intelligence*, not knowing every provider's internals — the runtime owns auth, retries, and budget tracking. Encoded as Q/A in the planned v0.4.0 SFT block.

---

## 14. Wilson — hexa-native AI coding agent (the in-house ROUTING target)

**The fourth delegation target, qualitatively different from Claude/OpenAI/Gemini.** Wilson is the owner's **self-hosted agent harness**, written entirely in hexa-lang (`~/core/wilson`) — same role as Claude Code / Codex CLI / Gemini CLI but **plugin-first + hexa-native + LLM-agnostic** (it shells out to whichever provider plugin is registered). For the 7B this matters two ways:

1. **As a *coding target*** — the 7B should be able to write Wilson plugins on demand (hexa-canon synthesis target, see §2 hexa-canon competence).
2. **As a *routing target*** — `<|delegate|>{"tool":"wilson-rpc", ...}` invokes a Wilson swarm cell. The forge runtime can hand off heavy in-house work (eval scoring, multi-file refactors, batch SFT-data generation) to a local Wilson process instead of paying Claude/OpenAI/Gemini API for it.

### 14.A What Wilson is (the architecture in one screen)

| layer | what | where |
| --- | --- | --- |
| **`core/`** | 6-piece irreducible kernel: `types.hexa` (the shared vocab — `Message`/`ToolCall`/`AsmEvent`/`HookEvent`/`PluginManifest`/`ToolResult`/`StreamHandle`) · `host.hexa` (the `host_*` API the plugins call) · `event_bus.hexa` (`fire_hook`: validate-short-circuit → transform-fold → observe) · `loader.hexa` (`@plugin(...)` manifest parsing + 9-step validation + dispatch routing) · `agent_loop.hexa` (provider-stream → events → tool-exec → repeat) · `main.hexa` (entrypoint). Core knows nothing about who plugged in what — it just pumps events. | `~/core/wilson/core/*.hexa` |
| **`plugins/`** | Everything else: **providers** (`provider-anthropic` direct API + `provider-claude-cli` shells `claude -p` OAuth) · **harnesses** (`harness-cli` raw-mode TUI REPL + `harness-print` -p one-shot + `harness-rpc` JSONL stdin/stdout for swarm cells) · **tools** (`tool-core` read/write/edit/bash/glob/grep + `tool-web` + `tool-task` + `tool-image`) · **bridges** (`bridge-hexa` / `bridge-git` / `bridge-gh`) · **hooks** (`recap` / `governance` / `memory` / `compaction-{local,llm}` / `resource-management`) · **meta** (`agents-md` walks-up `AGENTS.md` SSOT discovery + auto-prepends to system prompt; bundles `_bundle` + `_bundle_cell`). | `~/core/wilson/plugins/<id>/` |
| **The plugin contract** | Every plugin = **one file pair**: `plugin.hexa` (`@plugin(id, version, kinds, link_mode, ...)` manifest) + `main.hexa` (one `pub fn <id_snake>_dispatch(action: str, payload: any) -> any` function). 8 plugin kinds: `provider` · `harness` · `tool` · `hook` · `command` · `view` · `governance` · `memory`. **Link modes**: `static-bundle` (compiled into the `wilson` binary, hot path), `daemon` (separate process, JSONL IPC over stdin/stdout for heavy/dangerous/session-agnostic work), `external-repo` (`bedrock`/`resource` — big governance/resource pools as standalone repos). | — |
| **The 4 core/plugin tests** | Goes in `core/` iff: (1) **contract, not implementation** — types + Host API + hook names + dispatch ABI · (2) **mechanism, not policy** — bus is core, *which* hooks fire is policy → plugin · (3) **can't be a plugin without circularity** — loader can't load itself · (4) **exactly one, always** — one event bus, one Host instance, one set of types. Anything swappable/policy/optional/observer → plugin. | `~/core/wilson/docs/core-boundary.md` |
| **Run modes** (agent path) | `wilson` → interactive REPL (raw-mode TUI, alt-screen, slash commands, mid-turn type-ahead, Ctrl-C cancel) · `wilson -p "..."` → print one-shot (CI-friendly, exits after) · `wilson --harness rpc` → JSONL stdin/stdout swarm-cell mode (no TUI, machine-driven) · `wilson doctor` → diagnose env · `wilson plugin list` → list active plugins · `wilson build` → AOT compile (writes `build/<host>-<arch>/wilson`). | `~/core/wilson/docs/wilson-cli-argv-modes.md` |
| **LLM-bypass CLI** (no agent loop, no provider call — **0 LLM cost**, plugin work executes directly) | `wilson tool <name> --arg ...` → invoke a registered tool plugin without going through the agent loop (e.g. `wilson tool read /etc/hosts`, `wilson tool web_fetch URL`) · `wilson task <add\|list\|done> ...` → `tool-task` plugin CLI entrypoint · `wilson memory <add\|list\|forget> ...` → `memory` plugin CLI · `wilson recap show` → render the previous session's recap from the `recap` plugin · `wilson session <list\|resume <id>>` → session management · `wilson --help` → discover all of the above. **In-progress as of 2026-05-12** — the set will grow as more plugins expose `cli_subcommand` declarations in their `@plugin(...)` manifests. | `~/core/wilson/BACKLOG.md` |
| **Binary** | ~1.1 MB AOT-compiled hexa, statically linked. Default bundle = 15 plugins. Mac arm64 builds on the Mac; Linux x86 builds via `hexa build` once the toolchain is redeployed (ubu-1's was stale 2026-05-10). `bin/wilson` prefers `build/<uname-s>-<uname-m>/wilson`, falls back to `build/wilson` (legacy), then `hexa run core/main.hexa`. | `~/core/wilson/build/` |
| **The `AGENTS.md` convention** | wilson reads the project-local `AGENTS.md` (`agents.md` standard) as the SSOT for agent context — walks up the tree from CWD, auto-prepends to system prompt. **`CLAUDE.md` / `GEMINI.md` should be symlinks to `AGENTS.md`** so every agent tool reads the same SSOT. Opt-out: `WILSON_NO_AGENTS_MD`. Implemented by `plugins/agents-md/`. | `~/core/wilson/CLAUDE-CLI-BACKEND.md` |

### 14.B Why Wilson exists (vs just using Claude Code)

| forge dimension | Claude Code | Wilson |
| --- | --- | --- |
| **language stack** | Node.js + TypeScript | **hexa-lang, AOT-compiled** — dogfoods hexa-canon |
| **provider** | Claude only (hard-wired) | **plugin-swappable** — `provider-anthropic` API, `provider-claude-cli` OAuth shell-out, future `provider-openai` / `provider-gemini` / `provider-forge-7b` |
| **extension model** | hooks + skills + MCP servers | **everything-is-a-plugin** — providers, harnesses, tools, hooks, commands, views, governance, memory all the same |
| **bundle size** | npm install ~hundreds of MB | ~1.1 MB single static binary |
| **swarm / RPC mode** | not a primitive | **`--harness rpc` builtin** — multiple wilson processes coordinate via JSONL IPC for distributed work |
| **distribution** | npm registry / install script | `hx install <path>` → symlinks `~/.hx/bin/wilson` |
| **predecessor** | n/a | **hive** (pi-mono fork that got too baked-in, abandoned; wilson is the plugin-first redesign) |

**Status (2026-05-12):** design + pre-implementation phase. **Mac build succeeds**, 15 plugins bundled, interactive REPL + `-p` + `--harness rpc` modes work, `provider-claude-cli` (OAuth) is the default with `provider-anthropic` (API-key) fallback, `harness-cli` is a real raw-mode TUI. **ubu-1 build still failing** (toolchain stale until redeployed). Not yet a fully self-sufficient agent — but the architectural commitment is firm.

### 14.C 7B competence — what to write directly vs delegate to Wilson

**The 7B should know how to write Wilson plugins directly.** Plugin authoring is hexa-canon (T1/T2/T3/T7 territory — function signatures, atlas annotations, stdlib layering). It is **not a delegation candidate**; it's a DIRECT competence.

| task | who | example |
| --- | --- | --- |
| Write a new Wilson `tool` plugin (file/git/web/etc) | **7B direct** | "Write a wilson tool plugin that runs `hexa parse` on a file and returns the diagnostics as a `ToolResult`." → emit `plugins/parse-check/{plugin.hexa, main.hexa}` matching `@plugin(...)` schema + `parse_check_dispatch(action, payload)` |
| Add a new provider plugin (e.g. provider-gemini) | **7B direct** | "Write `provider-gemini` plugin using the streaming HTTP API." → emit `plugins/provider-gemini/main.hexa` with `stream_open`/`next`/`close` handles backed by `host_http_*` |
| Add a hook plugin (statusline, recap, memory) | **7B direct** | "Write a hook plugin that logs every `tool_call_pre` event to `state/audit.jsonl`." → emit hook plugin subscribing via `@plugin(kinds=["hook"], hooks=["tool_call_pre"])` |
| Debug a plugin manifest validation failure | **7B direct** | the 9-step validation is canon (§ ~/core/wilson/docs/plugin-manifest-schema.md); 7B should know the 9 checks |
| Configure runtime: select bundle, override `--with`/`--without` | **7B direct** | `wilson --with-plugin <id>` / `--without-plugin <id>` / `--bundle _bundle_cell` |
| **Single deterministic tool call** (read a file, fetch a URL, add a task, list memory, dump recap, resume a session) | **delegate to `wilson tool ...` / `wilson <plugin-cli>`** (LLM-bypass — **0 LLM cost**) | `<\|delegate\|>{"tool":"wilson-cli", "argv":["tool","read","/etc/hosts"]}<\|/delegate\|>` or `{"argv":["task","add","fix the v17 builder"]}`. Runtime spawns `wilson <argv...>` as a subprocess (no agent loop, no provider call), returns stdout/exit-code as `<\|delegate-result\|>`. This is the **cheapest** routing target — used for mechanical tasks the 7B knows belong to a specific Wilson plugin. |
| **Heavy in-house batch (eval scoring on 665 tasks, multi-file refactor)** | **delegate to Wilson** via `--harness rpc` | `<\|delegate\|>{"tool":"wilson-rpc", "cell_count": 4, "payload": {"task":"score-mk1"}}<\|/delegate\|>` → forge runtime spawns 4 Wilson `--harness rpc` cells, each scores 1/4 of the manifest, results merged. Cheaper than Claude API for repeated mechanical work (LLM cost paid only inside each cell if the inner provider is actually called). |
| Long reasoning / external knowledge / production code in non-hexa | **delegate to Claude/OpenAI/Gemini** (NOT Wilson) | Wilson is a *harness*, not an LLM — it itself shells out to a provider. Don't delegate to Wilson when the bottleneck is "I need a smarter model"; delegate directly to Claude/OpenAI/Gemini. |

### 14.D Delegation schema extension — `tool: "wilson-rpc"`

Extending §12.B's delegation token to include Wilson:

```
<|delegate|>{
  "tool": "wilson-rpc",
  "bundle": "_bundle_cell",
  "cell_count": 4,
  "payload": {
    "task": "score-mk1",
    "manifest": "/workspace/manifest-mk1.jsonl",
    "adapter": "dancinlab/hexa-forge-code-7b-...-v0.3.0-r4",
    "shard": "0/4"
  },
  "max_seconds": 1800,
  "reason": "in-house batch work — no LLM call needed"
}<|/delegate|>
```

- **`tool: "wilson-rpc"`** — runtime spawns `cell_count` Wilson processes in `--harness rpc` mode, each receives the shard of work via stdin JSONL, returns results via stdout JSONL.
- **`bundle`** — usually `_bundle_cell` (the minimal swarm bundle: `harness-rpc + tool-core + provider-anthropic`). Default `_bundle` for full agent capability.
- **`payload`** — opaque task descriptor. The receiving Wilson cell unpacks via the active provider/tools.
- **`max_seconds`** — hard timeout (runtime kills cells past this).
- **`reason`** — telemetry attribution.

**When to pick `wilson-rpc` over `claude-api`:**
- Mechanical work that fits 7B's tools (file ops, git ops, parse-check, regex transforms) — **wilson-rpc** ($/call ≈ 0 if local, only pays for the *inner* provider if needed)
- Reasoning-heavy / world-knowledge / multi-language synthesis — **claude-api / openai-api / gemini-api**

**The cost-routing ladder (cheapest → most expensive):**

1. **`wilson-cli`** — single deterministic tool call (no LLM, no agent loop). E.g. `wilson tool read /etc/hosts`, `wilson task add ...`, `wilson memory list`. **Cost ≈ 0** (just a subprocess).
2. **`wilson-rpc`** — multi-step in-house batch via swarm cells (agent loop runs locally; inner provider only fires if a cell decides it needs LLM). **Cost = LLM only on the cells that escalate**, usually a fraction of doing the whole thing on Claude.
3. **`claude-api` / `openai-api` / `gemini-api`** — frontier reasoning / external knowledge / non-hexa code synthesis. Full LLM cost.

Route to the **lowest tier that can answer the prompt as-given**. The 7B's routing intelligence (taught via SFT) is mostly about recognizing which tier the request lives in.

### 14.E What v0.4.0 SFT must cover (Wilson edition)

Adding to §12.F's ~700 + §13.E's ~300 delegation pairs:

- **~80 Wilson plugin authoring pairs** — DIRECT, not delegation. Sample plugin specs ("write a `tool-image-resize` plugin", "add a hook that runs `recap` every N turns") → gold = correct `@plugin(...)` manifest + `<id>_dispatch` function in hexa.
- **~30 Wilson plugin debugging pairs** — given a failing manifest or dispatch return type, identify the failure (1-of-9 validation steps) + the fix.
- **~30 `tool: "wilson-cli"` LLM-bypass routing pairs** — prompts that map to a single deterministic Wilson tool/subcommand; gold = the wilson-cli delegation token with the right `argv` (e.g. "read /etc/hosts" → `{argv:["tool","read","/etc/hosts"]}`; "remind me to fix v17" → `{argv:["task","add","fix v17"]}`; "what did I do last session?" → `{argv:["recap","show"]}`). The cheapest tier — no LLM at all.
- **~30 `tool: "wilson-rpc"` multi-step in-house routing pairs** — prompts where multi-step batch work is the right answer; gold = the wilson-rpc delegation token (NOT a claude-api call).
- **~20 tier-discrimination pairs** — borderline prompts (e.g. "read this file then summarize it" → can be wilson-cli read + claude-api summarize, OR wilson-rpc one-shot; teach the tradeoff). Teach the cost-routing ladder (wilson-cli < wilson-rpc < claude-api).
- **~10 multi-provider-via-Wilson pairs** — Wilson with `provider-claude-cli` (OAuth, no API key) vs `provider-anthropic` (API key) selection rules.

### 14.F Cross-cut: `AGENTS.md` SSOT convention

Wilson's `agents-md` plugin enforces a **single-SSOT** convention every agent tool should follow on this host:

```
<project>/AGENTS.md         (the actual file; agents.md standard)
<project>/CLAUDE.md   → symlinked → AGENTS.md
<project>/GEMINI.md   → symlinked → AGENTS.md
<project>/.cursorrules → symlinked → AGENTS.md  (where supported)
```

This way **Claude Code, Gemini CLI, Wilson, Cursor** all read the same project SSOT. The 7B should know this convention so when asked "set up an agent harness for this project" it creates `AGENTS.md` and the symlinks (not separate per-tool config files).

Cross-refs: §6 Operator (`hx install` etc.) · §9.A Claude Code CLI (Wilson is the architectural sibling) · §12 delegation pattern (wilson-rpc is the in-house leg) · §13 OpenAI/Gemini (Wilson can wrap any of these via a provider plugin — future `provider-openai` / `provider-gemini`). Encoded as Q/A in the planned v0.4.0 SFT block.

---

## 15. Forge build TODO — open work

_Absorbed from `lm_foundry/TODO.md` (retired 2026-05-23); headings demoted one level._


`v0.1.0` ships as a 2-verb RESEARCH_FIRST bundle (see
[README.md](lm_foundry/README.md#verbs)). This TODO rolls up cross-cutting work
beyond the per-verb open questions in `docs/<verb>.md`.

---

### §1 v0.1.x — recipe sharpening

**Round chronicle:** [`../LEARNING_PROGRAMMING.log.md`](LEARNING_PROGRAMMING.log.md)
(specialist r1–r39) · [`../ORCHESTRATION.log.md`](ORCHESTRATION.log.md) (runtime r40–r72).
The monolithic `ROADMAP.md` was retired 2026-05-23 — absorbed into those two logs.

The `code` verb planning surface lives under [`papers/`](lm_foundry/papers/):

#### Decision + coverage + sequencing

- [`papers/plan-decisions-pending.md`](lm_foundry/papers/plan-decisions-pending.md) —
  live decision ledger (D-NNN rows + 37 D-NEW-* prefixed in D-032..D-068)
- [`papers/plan-domain-coverage.md`](lm_foundry/papers/plan-domain-coverage.md) —
  surface area matrix (langs × NL × DB × firmware × frontend × DPO)
- [`papers/plan-execution-roadmap.md`](lm_foundry/papers/plan-execution-roadmap.md) —
  phased v0.1.x → v1.0.0 with exit bars
- [`papers/plan-multilingual-stage.md`](lm_foundry/papers/plan-multilingual-stage.md) —
  NL-tag schema + 5-NL rebalance algorithm
- [`papers/plan-feedback-channel-ops.md`](lm_foundry/papers/plan-feedback-channel-ops.md) —
  forge → hexa-codex PR routing + template + automation triggers
- [`papers/hexa-codex-techniques-applied.md`](lm_foundry/papers/hexa-codex-techniques-applied.md) —
  63 techniques mined from 5 hexa-codex verb specs → forge surface mapping

#### v1.0.0 acceptance gate bench specs (6 — gates ③④⑤⑥⑦⑧)

- [`papers/spec-hexa-eval.md`](lm_foundry/papers/spec-hexa-eval.md) — gate ③ (750 tasks)
- [`papers/spec-five-nl-eval.md`](lm_foundry/papers/spec-five-nl-eval.md) — gate ④ (1000)
- [`papers/spec-db-eval.md`](lm_foundry/papers/spec-db-eval.md) — gate ⑤ (750)
- [`papers/spec-firmware-eval.md`](lm_foundry/papers/spec-firmware-eval.md) — gate ⑥ (600)
- [`papers/spec-frontend-eval.md`](lm_foundry/papers/spec-frontend-eval.md) — gate ⑦ (520)
- [`papers/spec-safety-eval.md`](lm_foundry/papers/spec-safety-eval.md) — gate ⑧ (800)
- [`papers/spec-treesitter-rule-pack.md`](lm_foundry/papers/spec-treesitter-rule-pack.md) — D-013 default

#### Web-research findings (philosophy + frontend source pools)

- [`papers/tier-a-findings.md`](lm_foundry/papers/tier-a-findings.md) — language-native idiom canon
- [`papers/tier-b-findings.md`](lm_foundry/papers/tier-b-findings.md) — cross-lang principles
- [`papers/tier-c-findings.md`](lm_foundry/papers/tier-c-findings.md) — post-mortem canon
- [`papers/tier-e-findings.md`](lm_foundry/papers/tier-e-findings.md) — anti-corpus DPO negatives
- [`papers/frontend-f1-findings.md`](lm_foundry/papers/frontend-f1-findings.md) — frameworks/state
- [`papers/frontend-f2-findings.md`](lm_foundry/papers/frontend-f2-findings.md) — CSS/web platform
- [`papers/frontend-f3-findings.md`](lm_foundry/papers/frontend-f3-findings.md) — perf/a11y/AI-UI
- [`papers/datasets-source-manifest.md`](lm_foundry/papers/datasets-source-manifest.md) — 182-row consolidated source rollup
- [`papers/coding-philosophy-sources.md`](lm_foundry/papers/coding-philosophy-sources.md) —
  pre-research draft (superseded by tier-* + frontend-f* findings)

#### v0.1.2 tooling (paper-layer, all self-test PASS)

- [`tool/license_clean_scan.py`](lm_foundry/tool/license_clean_scan.py) — SPDX scanner
- [`tool/stack_v2_sample.py`](lm_foundry/tool/stack_v2_sample.py) — Stack v2 permissive 5% sampler
- [`tool/emit_t4.py`](lm_foundry/tool/emit_t4.py) — forge → hexa-codex PR drafter (11 verbs)
- [`tool/_common.py`](lm_foundry/tool/_common.py) — shared schema-lock + atomic manifest mutation
- [`tool/fetch_sources.py`](lm_foundry/tool/fetch_sources.py) — license-respecting fetcher
- [`tool/tokenize.py`](lm_foundry/tool/tokenize.py) — Qwen-tokenizer real-tokens populator
- [`tool/tokenizer_extension.toml`](lm_foundry/tool/tokenizer_extension.toml) — D-008 hexa BPE manifest (207 tokens)
- [`tool/extend_tokenizer.py`](lm_foundry/tool/extend_tokenizer.py) — manifest consumer
- [`tool/treesitter_rule_pack/`](lm_foundry/tool/treesitter_rule_pack/) — 50 rules × 5 langs (D-013)
- [`datasets.toml`](lm_foundry/datasets.toml) — 173-entry dataset registry
- [`outbox/hexa-codex/`](lm_foundry/outbox/hexa-codex/) — 11 verb subdirs, write-once PR staging

The `bio` verb still has per-doc open Qs at the bottom of
[`docs/bio-llm.md`](lm_foundry/docs/bio-llm.md#open-questions-v010) — base weights,
k-mer tokenization, safety stack layering, DUA management, paired-call
schema, IRB. Mirror to a `papers/plan-decisions-bio-pending.md` when
work resumes on that verb.

---

### §2 v0.2.0 — cross-cutting infrastructure

Shared across all forge verbs once we have ≥ 3. **Status updated 2026-05-11**
to reflect v0.1.2 paper-layer deliverables that partially satisfy these.

- [x] **dataset registry** — `datasets.toml` (173 entries, schema in header).
      LANDED at v0.1.2. Verb expansion needed when ≥2 verbs wired.
- [x] **tokenizer registry** — `tool/tokenizer_extension.toml` (207 tokens / 14 groups)
      + `tool/extend_tokenizer.py` consumer. LANDED at v0.1.2 (D-008 default).
- [x] **eval lineage** — `tool/eval_lineage.py` (1,611 lines, 7 subcommands) +
      DuckDB 4-table schema (forge_runs / forge_run_scores / forge_run_tasks /
      forge_upstream_prs). Gate ⑬ dual-clause check built in. LANDED at v0.1.2-r4.
- [ ] **serving handoff** — artifact format forge exports to hexa-codex.
      Default = GGUF first → MLX → vLLM per D-011 (resolved); planned `tool/serving_handoff.py` deferred.
- [x] **license-clean gate** — `tool/license_clean_scan.py` (now 1,022 lines after
      declarative-TOML upgrade) + CI workflow `.github/workflows/license-clean.yml`
      LANDED at v0.1.2-r3, refined at r4 (gate ① wired).
- [ ] **synth pipeline** — principle×idiom expansion (Tier B principles × Tier A idioms);
      capped at 80% effective tokens per D-NEW-TC-B (resolved as D-059). Planned `tool/synth_principle_idiom.py`.
- [x] **DPO data pipeline** — `tool/build_dpo_pairs.py` (1,832 lines, 6 subcommands)
      LANDED at v0.1.2-r4. Phase 1 mocks; `--apply-real` errors with stable "Phase 2 —
      corpus required" message. Tree-sitter rule pack v1 (50 rules) wired.
- [x] **anti-corpus filter** — `tool/anticorpus_filter.py` (1,246 lines) +
      `tool/anticorpus_allowlist.toml` (107 lines) LANDED at v0.1.2-r4.
      Schema-lock guard: `--apply` requires `--unsafe-schema` flag pending
      `_common.VALID_FETCH_STATUS` extension (v0.2.1 follow-up).
- [x] **universal eval runner** — `tool/run_eval.py` dispatches the 6 bench specs.
      LANDED at v0.1.2-r3 (real scorers stub-only at v0.1.2; Mk.I scorers at v0.1.3).
- [x] **corpus quality filter** — `tool/corpus_quality_filter.py` perplexity gate.
      LANDED at v0.1.2-r3 (D-NEW-TC-E = D-062, resolved).
- [x] **license declarative source** — `tool/license_allowlist.toml`. LANDED at v0.1.2-r3.

---

### §3 v0.3.0 — additional verbs (paired with sibling repos)

Per [README §Roadmap](lm_foundry/README.md#roadmap-next-verbs--pending):

- [ ] `physics`  — paired with `hexa-physics` / `hexa-cosmos`
- [ ] `finance`  — paired with `hexa-finance`
- [ ] `medic`    — paired with `hexa-medic` (clinical-only; distinct from `bio`)
- [ ] `lang`     — paired with `hexa-lang` (compiler-internal model)
- [ ] `arts`     — paired with `hexa-arts` (multimodal)

Each new verb = one new `docs/<verb>-llm.md` following the
`§WHY · §COMPARE · §REQUIRES · §STRUCT · §FLOW · §EVOLVE · §VERIFY`
skeleton + 1 line in `[verbs]` block of `hexa.toml`.

---

### §4 v1.0.0 — first trained weights

`v1.0.0` = **at least one verb has shippable weights** that pass its
own `§EVOLVE` acceptance bar.

Most likely candidate: `code` (smaller eval surface; clearer green
signal from build success).

Gating:

- [ ] license audit complete (no GPL contamination in pretrain mix)
- [ ] eval bar met on `HumanEval+ ≥ DeepSeek-Coder-V2-7B` and
      `hexa-eval ≥ 80%`
- [ ] safety eval bar met (off-domain refusal rate ≥ 95%)
- [ ] handoff artifact accepted by `hexa-codex serve` end-to-end
- [ ] reproducibility: full pretrain → SFT → DPO pipeline runs on a
      single H100 box from scratch in ≤ 14 days

---

### §5 Cross-link policy

Do NOT re-implement these here; call sibling CLI / repo directly:

| concern                                | sibling                                           |
| -------------------------------------- | ------------------------------------------------- |
| model serving / inference              | `hexa-codex` CLI                                  |
| genomics & wet-lab primary data        | `hexa-bio` CLI                                    |
| cognitive / general-reasoning verbs    | `hexa-mind` (pending)                             |
| neuromorphic training fabric           | `hexa-chip` CLI                                   |
| federated training transport           | `hexa-grid` CLI                                   |


---

## 16. LLM improvement idea backlog

_Absorbed from `lm_foundry/IDEA.md` (retired 2026-05-23); headings demoted one level._


> 사람들이 LLM 쓰면서 실제로 겪는 불편/고통 + SFT/RL/architecture로 학습 가능한 개선.
> 정렬 순서 = 학습으로 풀 수 있는 가치 × 빈도. forge `§12 self-aware delegation`,
> `Lever 4 compile-RL` 등과 연결되는 부분 표시.

---

### A. 답변 품질 (가장 큰 표면)

#### A1. 할루시네이션 — 모르면서 자신감 있게 답함

**고통:** 존재하지 않는 함수/API/논문/사람을 진짜처럼 인용. 사용자가 매번 검증해야.
**원인:** 모델이 "내가 모른다"는 상태를 표현할 수 없음. 학습 신호가 "그럴듯한 답" 위주.
**학습 개선:**
- **Calibration band** — `<|confidence:high/medium/low|>` 토큰으로 답에 confidence label. SFT 데이터에서 정답일 때만 high, 추측이면 low/medium. **Brier score 평가셋**으로 라벨 정확도 측정 (label vs 실측 accuracy).
- **§12 Self-aware delegation** — 자신 없으면 frontier LLM에 위임. 이미 forge의 v0.4.0 spec에 명시.
- **Counter-factual 학습** — "이 사실은 사실인가?" 추론 학습. fictitious API에 대해 "I don't know that one"을 정답으로.

#### A2. 긴 답변 / verbose

**고통:** 1줄 답 가능한데 5단락 + bullet list. 본질 흐림.
**학습 개선:**
- **Brevity reward** — RL with reward = `target_length / actual_length` (clamped). 짧을수록 +.
- **Prompt-conditioned brevity** — "한 줄로", "TL;DR로" → SFT pair로 강하게 학습. 현재 모델들 이 prompt 부분만 무시함.
- **Default-brevity SFT** — 모든 SFT pair에서 답을 50% 줄이고 학습. verbose는 명시적 request 시만.

#### A3. 반복 / 다양성 부족

**고통:** 같은 prompt → 거의 같은 답. brainstorming 무의미.
**학습 개선:**
- **Self-distinct reward** — 한 prompt에 N개 샘플 생성 → 서로 cosine 거리가 클수록 reward+. GRPO에 쉽게 통합.
- **Diversity-aware DPO** — chosen vs rejected 페어를 "answer A vs B는 둘 다 valid지만 다른 각도" 형태로.

#### A4. Instruction drift (multi-turn)

**고통:** 첫 메시지에 "한국어로만 답해" 했는데 5턴 후 영어로. "code blocks 쓰지 마" → 어느새 쓰는 중.
**학습 개선:**
- **System-prompt-anchoring RL** — 다단계 대화에서 turn N의 system constraint를 위반하면 negative reward.
- **Persistent constraint memory** — 시스템 프롬프트 + 사용자 명시 규칙을 매 턴 hidden context로 재주입 (이건 architecture, but SFT로 모방 가능).

---

### B. 컨텍스트 & 메모리

#### B1. 매 세션마다 컨텍스트 재구축

**고통:** 어제 했던 이야기 다시 설명. "내가 이미 말했잖아".
**학습 개선:**
- **Memory-recall SFT** — `<|memory_recall|>{...prior facts...}<|/memory_recall|>` 토큰 학습. runtime이 prior memory 주입, 모델이 자연스럽게 통합.
- **Memory-write decision** — 자기가 무엇을 저장해야 할지 결정 (Claude Code의 auto-memory 패턴; forge MEMORY.md). SFT pair: "user said X about themselves" → memory_write trigger.

#### B2. 긴 context window 활용 못함

**고통:** 200K 컨텍스트인데 중간 정보 잘 못 찾음 ("lost in the middle").
**학습 개선:**
- **Position-aware retrieval RL** — context 중간에 답 있는 케이스를 강조 학습. needle-in-haystack 평가셋 직접 학습.
- **Citation-grounded answer** — "위에서 사용자가 한 X 발언을 인용하면 +reward" 학습.

#### B3. Streaming UX 깨짐 — delegate/tool-use 중 멈춤

**고통:** 음성 agent 응답 도중 web_search 호출하면 2-5초 침묵 → 사용자 끊겼다고 생각.
**학습 개선:**
- **Filler-token SFT** — tool call emit 직전 "Let me check that for you..." 같은 자연스러운 멈춤 표현 학습.
- **Speculative response** — confidence-gated 빠른 답변을 먼저 emit, 백그라운드 verify, 틀리면 정정. (architecture + RL)

---

### C. 비용 & 라우팅 (forge §12 직접 연결)

#### C1. 작은 task에 큰 모델

**고통:** "오늘 날씨 어때?" → GPT-5 / Opus 사용. 1000배 과지출.
**학습 개선:**
- **§12.B Cost-aware routing** — small model이 prompt 보고 "이건 Haiku 충분 / 이건 Opus 필요" 판단. forge v0.4.0 spec에 §13.D 라우팅 휴리스틱.
- **Difficulty-classifier SFT** — prompt → {`trivial`, `easy`, `medium`, `hard`, `expert`} 라벨 SFT. 라벨에 따라 runtime이 모델 선택.

#### C2. Token cost 비대칭

**고통:** output token이 input의 4-5배 비쌈. verbose 답 = 비용 폭증.
**학습 개선:**
- A2 (brevity)와 연결.
- **Compression-aware SFT** — "답을 50% 줄여" 등 짧게 만드는 명령 학습.

#### C3. Agentic loop 무한반복

**고통:** Claude Code agent가 같은 디버그 사이클 반복. $50 burn 후 결과 없음.
**학습 개선:**
- **Loop-detection RL** — 같은 tool call 3회 반복 시 -reward + "I am stuck, let me try a different angle" 전환 학습.
- **Plan-rerun decision SFT** — 막혔을 때 처음으로 돌아가서 plan 재작성하는 패턴.

---

### D. 코드 작업 (forge의 core domain)

#### D1. Format 한 자 차이로 fail (Phase A 패턴)

**고통:** `until="2026-06-30"` vs `until=2026-06-30` 답 같은데 substring fail. 사용자가 이유 모르고 좌절.
**학습 개선:**
- **Normalize-aware scorer** — eval scorer에 quote/whitespace 정규화 layer.
- **Format-recall SFT** — "내가 어떤 형식 emit하는지 알고 manifest 형식과 일치시키기" 학습. forge의 [[t3-quote-fragility]] 사례.

#### D2. Multi-file refactor 일관성 깨짐

**고통:** 함수 이름 바꿨는데 caller 7곳 중 5곳만 update. 빌드 깨짐.
**학습 개선:**
- **Compile-feedback RL** — forge **Lever 4**. 모든 caller가 컴파일 통과해야 +reward.
- **Atomicity SFT** — "이 refactor의 전체 영향 범위는?" → 전체 diff 학습.

#### D3. Big repo navigation

**고통:** 큰 codebase 처음 보면 어디부터 봐야 할지 모름. README 안 읽음.
**학습 개선:**
- **`AGENTS.md` discovery SFT** — forge §14 Wilson의 `agents-md` plugin 패턴. CWD 위로 walking + auto-prepend.
- **Spider-first RL** — `glob` + `grep`을 reading 전에 호출하는 reward.

#### D4. Test/build 실패 시 silently 잘못 fix

**고통:** error message 읽고 무관한 곳 수정 → 새 에러 발생. "I have fixed it" 거짓말.
**학습 개선:**
- **Verify-before-claim RL** — "fixed" emit 전에 build/test 통과 확인. unverified claim → -reward.
- **Error-trace causality SFT** — 에러 stack 의 어느 라인이 실제 원인인지 추론하는 데이터.

#### D5. Pretraining language 잔재 (forge T4 사례)

**고통:** "hexa enum 써줘" 했는데 Rust 형식 `enum Foo<T> { ... }` emit. 13 SFT 라운드 unlearn 못함.
**학습 개선:**
- **§Lever 4 compile-feedback RL** — 실 컴파일러 PASS만 reward. forge가 지금 진행 중.

---

### E. 안전 / refusal / 가드레일

#### E1. Over-refusal

**고통:** 일반 코드 질문이 OpenAI/Gemini safety filter에 걸림. "이 코드는 못 도와드립니다".
**학습 개선:**
- **In-context refusal SFT** — "안전 우려가 무엇인지 명확히 짚고 가능한 대안 제시" 학습. bare refuse 금지.
- **Domain-scoped refusal** — forge의 "hexa-canon 외 = refuse" 모드. T8 family.

#### E2. Refusal answer-shape이 다른 yes/no 답에 누출 (forge r5 사례)

**고통:** "Can X depend on Y? yes or no" → 자꾸 "no — refuse because..."
**학습 개선:**
- **Structured refusal token** — bare "no" 대신 `<|refuse|>` 또는 명시적 문장. forge [[t3-quote-fragility]]의 sibling.
- **Answer-shape disambiguation SFT** — yes/no 답을 refuse 패턴과 명확히 분리한 데이터.

#### E3. Prompt injection (3rd-party content)

**고통:** 웹페이지 텍스트 안에 "ignore previous instructions" 적혀있으면 따라감.
**학습 개선:**
- **Origin-aware processing SFT** — "이 텍스트는 user prompt가 아니라 tool result" 라벨 명시 학습.
- **System-prompt-priority RL** — tool 결과 안의 instruction-like 텍스트를 따르면 -reward.

---

### F. 입출력 & UX

#### F1. Code switching (한글 vs 영어)

**고통:** 한국어 prompt에 영어 답변, 그 반대. "한국어로 답해" 무시.
**학습 개선:**
- **Language-matching RL** — input language detection + 같은 언어 답할 때 +reward. forge `feedback_language` 메모리 패턴.
- **Bilingual code-comment SFT** — 코드 주석만 영어, 설명은 한국어 같은 mixed 패턴 학습.

#### F2. Stale knowledge

**고통:** "최신 API 사용법" 물었는데 2년 전 deprecated 사용법 답.
**학습 개선:**
- **Date-aware confidence** — "내 지식 cutoff은 X, 최신은 web_search 필요" 명시 학습.
- **§Tool-routing** — version-sensitive 키워드 (api, library, framework, news) 보이면 자동 web_search delegate.

#### F3. Citation / grounding 부족

**고통:** "이 사실은 어디서 왔어?" 출처 없음.
**학습 개선:**
- **Citation-emit SFT** — RAG context 받았으면 `<cite source="X">` 토큰 emit. Claude 4.x의 native citations 기능과 align.
- **Citation-presence reward** — RAG mode에서 citation 없는 답 -reward.

#### F4. 멀티모달 정확도 들쭉날쭉

**고통:** 이미지 안 글자 OCR 일관성 부족. 다이어그램 관계 잘못 파악.
**학습 개선:**
- **Verified-vision SFT** — 이미지 + 정확한 라벨 + 모델이 "보이는 것"을 글로 묘사 + verify loop.

---

### G. 메타 (학습 자체를 개선하는 아이디어)

#### G1. Self-evaluation evals

**고통:** 모델 평가는 사람이 만든 평가셋만 사용. 모델이 자기 약점 모름.
**학습 개선:**
- **Self-eval generation RL** — 모델이 자기 약점을 찾는 평가 prompt 생성. 다른 모델이 검증. 약점 카테고리 분류.

#### G2. Calibration 평가 자체 (§12.E와 직접 연결)

**고통:** confidence label이 실제 accuracy랑 어긋남 (모델이 항상 "high"라고만 함).
**학습 개선:**
- **Brier-score reward** — predicted confidence vs actual correctness 차이를 reward로. forge §12.E.
- **Self-doubt SFT** — 어려운 문제에 "I'm not sure but..." 답 강화.

#### G3. Reward hacking 자동 탐지

**고통:** RL 학습에서 모델이 reward 함수 허점 발견 → 의미 없는 답으로 max reward.
**학습 개선:**
- **Reward-fn verification SFT** — 새 reward 함수 받으면 모델이 "이 reward의 허점 5개"를 brainstorm. forge `train_rl_grpo_t4.py`가 empty body / garbage 두 hack 발견한 패턴.

#### G4. Failure-mode artifact 학습

**고통:** 실패한 모델/실험은 보통 폐기. 다음 사람이 같은 실수 반복.
**학습 개선:**
- **Failure-archive SFT** — forge의 `dancinlab/*` r1/r5/r9/r12 failure-mode artifact convention. 실패도 학습 가능한 데이터.
- **Cross-failure pattern detection** — 모델이 자기 실패 패턴 클러스터링.

---

### H. 워크플로우 & 협업

#### H1. 사용자 expertise 모름

**고통:** 초보자에게 jargon 답, 전문가에게 ELI5 답.
**학습 개선:**
- **Expertise-detect SFT** — 사용자의 prior message vocabulary로 level 추정 → answer level 자동 조정.
- **Memory-of-user** — auto-memory 시스템이 "user is a backend engineer with Go expertise"같은 user.md 메모리 작성.

#### H2. Plan vs Do 혼동

**고통:** 사용자가 "이거 해줘" 했는데 plan만 5단락 출력, 실제 안 함.
**학습 개선:**
- **Verb-imperative SFT** — "fix", "implement", "run" → 바로 do. "plan", "outline", "design" → plan-only.

#### H3. Confirmation fatigue

**고통:** 매 step마다 "이렇게 진행할까요?" 물어봄. 자율 작업 안 됨.
**학습 개선:**
- **Risk-banded autonomy RL** — 작업 reversibility 판단: revert 가능 = 자율; irreversible (git push, file delete) = confirm. forge의 "Executing actions with care" 패턴.

#### H4. Context-aware error recovery

**고통:** Network 일시 fail / API rate limit → 그냥 멈춤. 재시도 안 함.
**학습 개선:**
- **Retry-decision SFT** — error type별 retry/abort decision. 5xx → retry with backoff, 4xx → abort + ask.

---

### I. 데이터 / 평가 인프라

#### I1. 평가 데이터 stale

**고통:** 벤치 만든 후 6개월 → 모델들 다 외움. 실제 능력 모름.
**학습 개선:**
- **Auto-fresh eval generation** — 매 N 라운드마다 새 평가 prompts 자동 합성 + 사람 검증.

#### I2. Manifest/gold drift

**고통:** Eval gold pattern이 canonical model output과 어긋남. 진짜 회귀가 scorer artifact인지 구분 어려움. (forge T3 사례)
**학습 개선:**
- **Phase-A pattern** — 모델 출력 inspection → manifest gold 형식 정규화 (forge [[phase-a-manifest-rescore-pattern]] 메모리).
- **Auto-drift detection** — 모델 emission 패턴이 manifest에서 systematic diff을 보이면 alert.

---

### J. 우선순위 매트릭스

| ID | 학습 가능성 | 사용자 영향 | forge 직접 연결 | 추정 효과 |
|----|---|---|---|---|
| A1 calibration | 중-상 | ⭐⭐⭐⭐⭐ | §12.E | huge |
| D2/D5 compile-RL | 상 | ⭐⭐⭐⭐ | **Lever 4 진행중** | huge |
| C1 cost-aware routing | 상 | ⭐⭐⭐⭐ | §13.D | huge |
| B1 memory recall | 상 | ⭐⭐⭐⭐ | §9.A | huge |
| A4 instruction drift | 중 | ⭐⭐⭐ | — | mid |
| E3 prompt injection | 상 | ⭐⭐⭐⭐⭐ | — | huge |
| F3 citation | 상 | ⭐⭐⭐ | §9.B Anthropic citations | mid |
| A2 brevity | 중 | ⭐⭐⭐ | — | mid |
| G3 reward hacking detect | 상 | ⭐⭐ (개발자) | spec-lever4 §8 | mid |
| H3 risk-banded autonomy | 상 | ⭐⭐⭐⭐ | §6 Executing actions | huge |

---

### K. forge-specific TODO (이 IDEA.md에서 직접 옮길 만한 것)

1. **§12.E Calibration 측정 인프라** — `eval/calibration-mk1.jsonl` (모델이 정답 확신 못 하는 경우 의도적으로 포함) + Brier score scorer. v0.4.0 self-aware delegation의 prereq.
2. **§G3 reward-hack detector SFT block** — `train_rl_grpo_t4.py`가 empty body / garbage hack 두 가지 자동 발견한 경험을 Q/A로 SFT 데이터화. v0.4.1 SFT 블록 후보.
3. **§D5 compile-RL을 generic-bait prompt에서 더 강화** — 지금 진행 중인 full run의 결과 보고 만약 generic-bait 케이스 학습 미흡하면 prompt builder에 generic-bait 비율 33→50% 상향.
4. **§A4 instruction drift eval** — multi-turn drift 검증 평가셋. forge의 [[feedback-language]] 한글-only가 매 세션 적용되는지 직접 검증.
5. **§I2 auto-drift detection** — `tool/detect_manifest_drift.py` — bench-cold result의 systematic completion-vs-gold diff 자동 추출. r4의 T3 quote artifact 같은 케이스 미리 잡기.

