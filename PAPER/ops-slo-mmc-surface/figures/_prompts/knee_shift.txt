A clean technical schematic diagram for a computer-systems queueing-theory paper, white background, flat vector style, no photorealism, no people. Title at top: "M/M/c SLO surface — the latency knee shifts right with parallel slots". The main panel is a 2D line chart: x-axis labeled "offered arrival rate lambda (queries per second)" with ticks 1, 2, 5, 10, 20, 40 on a log scale; y-axis labeled "p99 latency (ms)". Three smooth curves, each flat-and-low then bending sharply upward into a vertical asymptote (a queueing "knee" / pole), color-coded and labeled: a red curve "c = 1 channel (-np 1)" whose knee is leftmost near 5 qps; an orange curve "c = 2 channels (-np 2)" whose knee is in the middle near 15 qps; a green curve "c = 4 channels (-np 4)" whose knee is rightmost near 20 qps. Draw a dashed vertical line under each knee labeled "throughput ceiling c·mu". A curved arrow spanning the three knees labeled "knee shifts RIGHT with more channels c". Below the chart, a thin secondary strip showing an "accuracy cliff" — a green region on the left (100% correct) dropping to a red region on the right past the knee, labeled "saturation -> accuracy collapse". Minimal, crisp, publication-quality, monochrome-friendly, axis labels legible, no clutter.
