# status=executed
# milestone=M3.ECON (F-CODEX-2 latency conjunct — substrate's own measured context-latency curve)
# verifier=verify/numerics_economics_empirical_landing.hexa  (check 9 + check 10, LATENCY_MS now LIVE)
# bench=bench/sandbox_stage4_context_scaling.hexa  (real llama-server, $0 local M3 Metal, cycle-16)
# model=Qwen2.5-1.5B-Instruct-Q4_K_M  host=mac-mini-m3 (brew llama.cpp + Metal, 24GB UMA)  port=8091  -np 1 -cb
# falsifier=F-CODEX-2: inference_cost ∝ context^τ = context^4  (n=6 lattice TAU_INFER=4)
# fit_method=closed-form log-log OLS slope of (context_len, mean_wall_ms); residual=|slope − τ|; PASS gate ≤ 0.10
# verdict=🔴 FALSIFIED  (10/10 structural checks PASS · F-CODEX-2 residual 3.47602 ≫ ε=0.10)
# m3_econ_checkbox=STAYS [ ]  (F-CODEX-2 does NOT fit the substrate's measured latency curve)
# tier=🟢 SUPPORTED-NUMERICAL bench + closed-form recompute (the FALSIFICATION itself is numerically supported)
# cost_usd=0
# ════════════════════════════════════════════════════════════════════════
#  MEASURED CONTEXT-LATENCY CURVE — bench TSV verbatim
#  .verdicts/sandbox/stage4_context_scaling.tsv (Qwen2.5-1.5B, 20 tasks × 4 rungs)
# ════════════════════════════════════════════════════════════════════════
# context_len   mean_wall_ms   mean_input_tokens   n_correct   throughput_tok_per_s
#       1024            569                 987       17/20                  22.80
#       2048            670                1947       17/20                  17.60
#       4096           1005                3867       17/20                  11.98
#       8192           1668                7647       17/20                   7.31
#
# accuracy is FLAT 17/20 across all 4 rungs → no long-context collapse; the
# latency claim stands cleanly with no accuracy-floor caveat needed (the
# bench's honest_note_2 caveat does not apply here). 8k rung booted fine on
# the 24GB UMA M3 (no OOM / UNAVAILABLE row — k=4 full grid, not k=3 partial).
#
# ════════════════════════════════════════════════════════════════════════
#  PREDICTED vs MEASURED — F-CODEX-2
# ════════════════════════════════════════════════════════════════════════
# F-CODEX-2 PREDICTS:  wall_ms ∝ context^τ  with  τ = 4   (n=6 lattice; (2ctx)→16×, (4ctx)→256×)
# SUBSTRATE MEASURES:  log-log OLS slope τ̂ = 0.52398      (R²≈0.956 — clean power-law fit)
# RESIDUAL:            |τ̂ − τ| = |0.52398 − 4.0| = 3.47602   ≫  ε = 0.10
#
# predicted wall_ms (τ=4, anchored to the 1k point A=569ms):
#   ctx=1024 → 569 ms (anchor)
#   ctx=2048 → 569·2^4   = 9 104 ms     (MEASURED 670 ms  — 13.6× over-prediction)
#   ctx=4096 → 569·4^4   = 145 664 ms   (MEASURED 1005 ms — 145× over-prediction)
#   ctx=8192 → 569·8^4   = 2 330 624 ms (MEASURED 1668 ms — 1397× over-prediction)
# The context^4 law over-predicts the substrate's 8k latency by ~1400× — it is
# not a marginal miss, it is the wrong functional class for modern serving.
#
# WHY (honest, real-limits-first per LATTICE_POLICY.md §2):
#   The substrate's wall_ms rises only ~2.9× (569→1668 ms) across an 8× context
#   sweep — a SUB-LINEAR (τ̂≈0.52) curve. This is exactly the modern-serving
#   regime documented in infer_cost/ai-inference-cost.md §S7.6: prefill (TTFT)
#   scales ~linearly in input length while per-request total wall is dominated
#   by the FIXED max_tokens=64 decode (memory-bandwidth-bound, context-invariant
#   weight-load per token, Theorem I-1 wall) + the cached-boilerplate prefill.
#   Quartic context^4 would require O(n^2) attention to dominate AND the prefill
#   to dwarf decode at these context lengths — neither holds for a 1.5B Q4 model
#   with llama.cpp's KV-cache scheduler at ≤8k tokens. The honest failure mode
#   the bench preregistered (τ≈2 paged-attention sub-quartic) is even GENTLER
#   here (τ̂≈0.5), because the fixed 64-tok decode floor flattens the curve further.
#
# IS THIS A LATTICE_POLICY EXTERNAL-ENVELOPE EXEMPTION?  NO.
#   Unlike F-CODEX-1 (which the cycle-15 lift made disclosure-only because the
#   N^σφ training-cost claim is a lattice number asserted against an EXTERNAL
#   entity, Qwen 2.5, that has no obligation to follow n=6), F-CODEX-2 is a
#   claim about the SUBSTRATE'S OWN INTERNAL latency-vs-context scaling — the
#   substrate provides the curve directly (verify/numerics_economics_empirical_landing.hexa
#   check-10 rationale, L544-548). τ=4 is therefore a REAL falsifiable prediction
#   ABOUT this substrate, and the substrate's own measurement decisively rejects
#   it. This is a genuine 🔴 FALSIFIED, not a g25/g26 self-imposed-ceiling artifact.
#
# CONSEQUENCE:
#   M3.ECON checkbox STAYS [ ] (honest residual, per task directive "honest
#   residual over forcing a fit"). M5.ECON v1.3.0 release gate accordingly
#   records 🔴 FALSIFIED on the F-CODEX-2 conjunct: the n=6 τ=4 context-cost
#   law does not describe the SANDBOX substrate's measured latency. The HONEST
#   landing is that the substrate's empirical context exponent is τ̂≈0.52, not 4.
#
# ════════════════════════════════════════════════════════════════════════
#  hexa verify (numerical recompute) — RAW STDOUT VERBATIM  (exit code 1)
# ════════════════════════════════════════════════════════════════════════
========================================================================
  hexa-codex ECONOMICS empirical-fit harness — F-CODEX-1 + F-CODEX-2
  M3.ECON closed-form residuals vs n=6 lattice (24/25 + 4)
  cycle-16: k_active=4 LIVE · F-CODEX-2 latency LIVE — checkbox [ ] iff F-CODEX-2 residual ≤ ε
========================================================================
  N6_EXP_TRAIN=0.96  TAU_INFER=4.0  EPS_RESIDUAL_THRESHOLD=0.1

  scale ladder live-row count: k_active=4 / 4
    0.5B    : LIVE (cycle-6)
    1.5B    : LIVE
    3B      : LIVE
    7B      : LIVE

  [PASS] SCALE_GRID_PARAMS strictly monotone-increasing (0.5B < 1.5B < 3B < 7B)
         · first_violation_at=-1
  [PASS] SCALE_GRID_PARAMS length == 4 (ladder rungs)
         · len=4  expected=4
  [PASS] STAGE2_ACCURACY_0_5B is live (no -1 sentinel) and all entries ∈ [0, 1]
         · live=1  in_band=true
  [PASS] STAGE2_ACCURACY_0_5B mean reconciles to cycle-6 per-stratum-max sum (≈ 0.3667)
         · computed_mean=0.36668  expected=0.36668  drift=0.0
  [PASS] STAGE2_ACCURACY_0_5B[wc_5_15] = 27/30 = 0.9 (cycle-6 best persona = max)
         · value=0.9  ref=0.9  drift=0.0

  [PASS] n=6 theoretical exponents: N6_EXP_TRAIN = 24/25 + TAU_INFER = 4
         · N6_EXP_TRAIN=0.96  TAU_INFER=4.0  exp_drift=0.0  tau_drift=0.0
  [PASS] k_active scale-points == 4 (cycle-14: 0.5B/1.5B/3B/7B all LIVE; F-CODEX-1 fit active)
         · k_active=4  expected_this_commit=4  fit_status=LIVE
  [PASS] F-CODEX-1 empirical slope disclosed (LATTICE_POLICY-lifted: lattice residual is descriptive, not gating)
         · k_active=4  measured_slope=0.17207  lattice_ref=0.96  lattice_residual=0.78793  threshold_unused=0.1
  [PASS] F-CODEX-2 measured slope LIVE (context-scaling bench) — closed-form OLS recompute on (context, wall_ms)
         · measured_tau=0.523982  lattice_tau=4.0  residual=3.47602  threshold=0.1  source=stage4_context_scaling.tsv
  [PASS] verdict-line consistency — today expected 'FALSIFIED' (k_active == 4, F-CODEX-2 measured τ̂ rejects lattice τ=4, residual > ε)
         · computed_label=FALSIFIED  expected_today=FALSIFIED  k_active=4

========================================================================
  10/10 checks passed
  🔴 FALSIFIED — F-CODEX-2 residual exceeds ε=0.1
  · F-CODEX-1 measured slope=0.17207 (lattice residual=0.78793, disclosure-only)
  · F-CODEX-2 residual=3.47602 (gate=0.1)
__HEXA_CODEX_NUMERICS_ECONOMICS_EMPIRICAL_LANDING__ FALSIFIED
