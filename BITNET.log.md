# BITNET — log

Append-only history sister of `BITNET.md`. Each entry starts with `## <ISO timestamp> — <header>` (newest on top); body = `- [x]` (done) / `- [ ]` (pending) checkbox tasks.

## 2026-05-25 — M5 BT-77 recompute 정직한 partial: 🟡 SUPPORTED-BY-CITATION (atlas 등록은 있되 in-repo recompute path 없음), M5 [ ] 유지 + 언블록 레시피 명시

- [x] **BT-77 atom provenance 완전 추적 — 2줄 헤드라인만, sub-row 0개.** `hexa atlas lookup R 'n6-atlas-breakthrough-theorems-tecs-l-cross-domain-**bt-77**'` → `atlas.n6:40221-40222` 2줄: `@R ... = BitNet Quantization n=6 n6 :: n6atlas [10*]` + `"**BT-77** — 2B4T 25/26 EXACT, 3 models × 2 teams, 40/41 total"`. **벤치마크 파일 / 모델 리스트 / 채점 코드 / 소스 페이퍼 = 0건**. sub-row 추가 probe: `grep -E "bitnet-quantization\|atlas-bitnet\|bt-77-" atlas.n6` → **0 matches**. 형제 BT-71(7/7, sub-row 8개) · BT-72(7/7, sub-row 8개) · BT-73(6/6, sub-row 6개) · BT-75(HBM exponent, 다수 sub-row)는 모두 `@R n6-atlas-<domain>-—-table-bt-XX-⭐⭐-<n6-identity> = <value>` 형태로 sub-row 등록되어 있는데, BT-77만 헤드라인 only — atlas 등록 자체가 비대칭.
- [x] **"25/26 EXACT, 3 models × 2 teams, 40/41 total"의 정체 = n=6 numerology fact-pattern, ML 벤치마크 아님.** 형제 BT들 텍스트 envelope을 전부 대조한 결과: BT-86 "NaCl/TiO₂/Al₂O₃/MgO/Perovskite all CN=6=n, 23/24" / BT-72 "EnCodec 8 codebooks 1024 entries 24kHz 6kbps 7/7" / BT-71 "L=σ-φ=10, layers=σ-τ=8, width=2^(σ-τ)=256, SH=n/φ=3, 7/7" — 전부 **K개 실제 시스템 파라미터가 n=6 divisor identity(n, σ=12, σ-φ=10, σ-τ=8, τ=4, σ·τ=48, sopfr=5, n-φ=3, μ=2, J₂=24)에 정수 매치하는지 카운트**(TECS-L cross-domain pattern-match) 컨벤션. BT-77 "25/26 EXACT"도 동일하게 — BitNet 2B4T architecture parameter 26개 중 25개가 n=6 lattice에 정수 매치, "3 models × 2 teams = 40/41"는 BitNet 700M+1.3B+3B (Microsoft + community/HF team 변종)까지 확장한 누적 카운트. **MMLU/IFEval/HumanEval류 ML 벤치마크가 아님 — "EXACT"는 served-accuracy가 아니라 integer-identity-match.**
- [x] **honest tier = 🟡 SUPPORTED-BY-CITATION, M5 flip 안 함.** `hexa verify BT-77`는 rubric으로 fall-through(n=6 fact-pattern atom용 `--expr` closed-form 없음 — 형제 atom들도 마찬가지). 🔵/🟢 불가능(in-repo recompute path가 없음 — 26개 BitNet 파라미터 + n=6 lattice match 코드를 담은 `.hexa` table이 아직 없음, 외부 지식으로 지어내면 LLM-self-judge → cx_claim_verify 위반). 🔴 불가능(측정값이 헤드라인을 반박하지 않음). 🟠 DEFERRED 부적합(hw/data 의존이 아니라 **recompute PATH 자체가 비어있음**; M1/M2는 served-accuracy라는 scoring fn이 있어서 DEFERRED가 맞았던 것 — BT-77은 그게 없음). rubric상 정확히 🟡 — "atlas/literature registered, NO hexa recompute (external sympy/citation carry — never auto-🔵)". M1/M2/M2+가 같은 모델(BitNet b1.58 2B4T)에 T4 contact을 이미 land했지만 그 30%/RSS 숫자는 BT-77의 25/26 fact-pattern과 다른 measure이므로 conflate 금지. **M5는 [ ] 유지 + honest residual 노트** — flip하려면 in-repo recompute가 필요한데 그게 없는 게 정직.
- [x] **X-kind sibling `n6-bt-77` 네임스페이스 분리 확인.** `hexa atlas lookup X 'n6-bt-77'` → `atlas.n6:58332` "Cross-Vendor HBM Capacity Convergence to n=6" — **HBM 도메인, BitNet 아님**. firmware/boards/chip/papers/n6-advanced-packaging-*.md의 "BT-77 HBM 12-Hi sigma σ=12 stack depth" 교차 참조도 X-kind 쪽 — packaging-paper context. M5 scope = R-kind만 (BitNet quantization).
- [x] **언블록 레시피 (next-cycle M5 seed, $0 / <1일).** (a) BitNet 2B4T `config.json`(이미 디스크: `/Users/ghost/Models/bitnet-bf16/config.json`)에서 26개 architecture 필드(`hidden_size, num_attention_heads, num_kv_heads, num_hidden_layers, intermediate_size, vocab_size, max_position, rope_theta, ...`)를 `bench/bitnet_bt77_n6_lattice_table.hexa` 26-row 테이블로 커밋. (b) `bench/bitnet_bt77_n6_lattice_scorer.hexa`가 (a) 테이블 + n=6 lattice {n, σ=12, σ-φ=10, σ-τ=8, τ=4, σ·τ=48, sopfr=5, n-φ=3, μ=2, J₂=24}로부터 K/26 + per-row `{param, n6_identity, match:bool}` 반환 (closed-form). (c) BitNet 700M+1.3B+3B 형제 사이즈 config로 확장 → 40/41. (d) `hexa atlas append-witness --kind R --id '**bt-77**' --raw '<K/26>'`로 등록. 모델·엔진(brew llama.cpp + Microsoft bitnet.cpp 둘 다 M1/M2+에서 build/verify 완료) 전부 READY — .hexa table + scorer만 부재. 새 hw/data 의존성 0.
- [x] **verdict persisted + discoveries tape valid.** `.verdicts/bitnet/m5_bt77_recompute.txt`(요약 + `hexa atlas lookup R/X` verbatim + `hexa verify BT-77` rubric verbatim + provenance probe + tier-justification + 5단계 reproduce 레시피 + 4단계 unblock 레시피). `.discoveries/bitnet-m5-bt77-citation-only.tape`(7 entries / 0 malformed, `hexa tape` 검증 통과). cx_claim_verify · cx_discovery_log 준수, cx_empirical_contact는 M1/M2/M2+가 underlying MODEL에 대해 이미 만족.

## 2026-05-25 — M2+ TRUE 1.58-bit i2_s 측정 (bitnet.cpp 빌드, /gap ROOT-B 종결, ROOT-A와 합쳐 M1/M2 verdict 두 축 모두 견고)

- [x] **엔진 빌드(load-bearing).** Microsoft `bitnet.cpp` 공식 인퍼런스 엔진을 mac-mini-m3 로컬에 빌드 (`~/BitNet`, github.com/microsoft/BitNet, recursive clone). 토치/이슈: (a) brew `ccache` 깨짐(`libfmt.11.dylib` not found) → `cmake -DGGML_CCACHE=OFF`로 우회. (b) arm64 i2_s는 `codegen_tl1.py --model bitnet_b1_58-3B --BM 160,320,320 --BK 64,128,64 --bm 32,64,32`로 LUT 커널 헤더 생성. (c) `cmake -DBITNET_ARM_TL1=OFF -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++` + `cmake --build build --target llama-cli llama-quantize -j 8`로 빌드 성공. 결과: `~/BitNet/build/bin/llama-cli` (Microsoft 공식 I2_S 삼진 커널, arm64 NEON+ACCELERATE).
- [x] **GGUF 정합성 — 공식 i2_s 다운로드는 토큰화 deadcase, self-convert가 정답.** 디스크의 `~/Models/gguf/bitnet/ggml-model-i2_s.gguf`는 SHA256(`4221b252...884c2162`)이 공식 `microsoft/BitNet-b1.58-2B-4T-gguf` HF blob과 **bit-for-bit 일치**(검증됨), 그러나 metadata는 arch=`bitnet-b1.58`, name=`bitnet2b`, chat_template=`Human:/BITNETAssistant:` — bitnet.cpp 엔진에서 디코딩이 garbage("on enc cliente...")나 token-0 collapse(`!!!!`)로 무너짐. 해결: bf16(`~/Models/bitnet-bf16`, 4.83GB, 이미 디스크) → `utils/convert-helper-bitnet.py`(preprocess-huggingface-bitnet → convert-ms-to-gguf-bitnet → `build/bin/llama-quantize ... I2_S 1`)로 engine-matched i2_s GGUF 생성 → `~/Models/bitnet-bf16/ggml-model-i2s-bitnet.gguf` (1.19GB, 1187801216 bytes, arch=bitnet-b1.58, file-type=40, ggml-type=36=I2_S, 332 tensors). 공식 2B4T `User:/Assistant:` raw-prefix 모드에서 coherent decoding 확인("4. Response: 4. Response: ...").
- [x] **serving mode = raw User:/Assistant: prefix (`-cnv`는 token-0 collapse).** `-cnv` 모드(임베디드 chat_template 적용)는 모든 입력에 대해 `!!!!` 출력 — 임베디드 템플릿/엔진 mismatch. raw `-p "User: <q>\\nAssistant:"` --temp 0 -ngl 0 모드가 fair serving mode. M1 TQ2_0 bench의 `assistant_prefix` parse 모드와 동일한 의미.
- [x] **bench M2+ = 6/20 = 30.0% (TRUE i2_s, 1.58 bpw).** `bench/bitnet_i2s_true_1_58bit.hexa` 작성 — TASKS manifest + byte_exact_subset 스코어러는 `bench/bitnet_m1_accuracy_floor.hexa`와 **byte-identical**(verbatim copy), engine 호출만 bitnet.cpp llama-cli + raw User/Assistant prefix로 교체. 결과: **6/20 = 30.0% (TQ2_0와 정확히 동일)**, peak_rss=1527 MB(TQ2_0 1568 MB 대비 −2.6%, i2_s가 약간 더 타이트 — 1.58 vs 2.06 bpw 차이 부합 + 직전 ROOT-A 엔트리의 "true 1.58-bit RSS ≤ TQ2_0이므로 memory-holds는 보수적" 보수 가정을 **직접 확정**), total_wall=87,727 ms(TQ2_0 221,772 ms 대비 **2.53× 빠름 — bitnet.cpp 논문의 1.37×–5.07× ARM 헤드라인을 실측 확인**). usable floor(≥15/20) 미달, 동일.
- [x] **/gap ROOT-B = answered + ROOT-A와 합쳐서 두 축 모두 견고.** ROOT-B 가설("진짜 1.58-bit 테스트 안 했으니 30%는 TQ2_0 아티팩트일 수 있다") REFUTED. TRUE i2_s 삼진 커널도 같은 manifest에서 정확히 6/20. **직전 PR #40의 ROOT-A(스코어러 confound)가 이미 "30%는 verbose-but-correct가 아니라 사실이 틀린 답"임을 입증**했으므로, 이번 M2+ ROOT-B는 그 결론을 quantization 축으로 확장: BitNet b1.58 2B4T의 floor 실패는 (i) ternary kernel 변종이 아니라 + (ii) 스코어러 formulation도 아니라 → 모델의 진짜 quality 한계. joint claim "1.58-bit ≈ full quality at a fraction of memory"는 두 축 모두에서 FALSIFIED — 메모리 절반은 HOLD(1527 MB < Qwen-1.5B-Q4-CPU 2878 MB → 0.53×, 그리고 진짜 i2_s에서 측정 — ROOT-A의 보수 가정이 확정됨), 품질 절반은 FAIL.
- [x] **honest sub-finding: arm64 i2_s 커널 stability artifact.** 20개 failure 중 10개가 token-0 반복(`!!!!`)으로 collapse — 특정 입력 시퀀스에서 i2_s LUT 경로가 NaN/inf logit을 내는 결정론적 패턴(--temp 0, warmup/threads와 무관하게 재현). TQ2_0의 14개 wrong은 verbose-prose miss였지 degenerate token이 아님 → joint accuracy는 같아도 i2_s의 failure mode는 정성적으로 kernel-stability driven. bitnet.cpp arm64 i2_s on M3의 추가 falsifier surface로 기록.
- [x] **verdict persisted.** `.verdicts/bitnet/m2plus_i2s_true_1_58bit.txt`(summary) + `.tsv`(20-row per-task) + `.verdicts/bitnet/m2plus_i2s_vs_tq2_0_comparison.txt`(i2_s↔TQ2_0 헤드라인 비교 + reproduce 레시피). `hexa verify BT-77` 프로브는 M1과 동일하게 rubric으로 fall-through(empirical atom, --expr closed-form 없음). discovery: `.discoveries/bitnet-m2plus-i2s-true-1-58bit.tape`(hexa tape 검증 통과, 7 entries / 0 malformed). cx_empirical_contact T4. 모두 $0 local.

## 2026-05-25 — M1 측정 confound 해소 (the /gap ROOT-A): 30%는 진짜 ternary floor, 스코어러 아티팩트 아님

- [x] **INSTRUCT-FAIR 스코어러 family 작성** (`bench/bitnet_m1_scorer_ablation.hexa`, deterministic, LLM-judge 금지 g5/R6). 기존 `byte_exact_subset` 옆에 (a) **whitespace-lenient**(대소문자 fold + 양쪽 공백 전부 제거 후 substring), (b) **answer-extraction**("Answer:" 뒤 / 첫 문장 / 첫 줄 span을 먼저 뽑고 그 span에서만 gold 매칭). 케이스 fold는 기존 스코어러와 byte-identical하게 shell `tr 'A-Z' 'a-z'` + `grep -qF` 사용.
- [x] **저장된 completion 재채점**(re-serve 안 함 — `m1_accuracy_floor.tsv` col 7). **byte_exact 6/20=30.0%(원본 정확 재현) · lenient 7/20=35.0%(Δ+1) · extraction 6/20=30.0%(Δ+0).** lenient가 회복한 단 1건 = idx14(`1,2,3` vs gold `1, 2, 3`, 순수 공백 아티팩트). 나머지 실패는 전부 **사실이 틀린** 답("not square of 7", "ASCII is 1", "1101 = 11") — verbose-but-correct 가설 기각.
- [x] **template/prompting ablation**(Task 3, 실패 5개 task만 재serve, $0 local). 2-shot 프리픽스는 `--jinja` 챗 템플릿이 few-shot 블록을 한 user turn으로 감싸 모델이 예시만 반복 → 0/5 회복(오히려 해로움). strict "answer only" 지시는 1/5만 회복(원래 verbosity-only miss인 gold=2). 나머지 4개는 prompting으로 안 고쳐지는 진짜 사실 오류. → **prompting은 floor를 의미있게 못 움직임.**
- [x] **정직한 relabel (ROOT-B honesty 합침).** served/measured 값은 **TQ2_0 2.06bpw** — 광고된 i2_s **1.58-bit 아님**(stock llama.cpp에서 로드 불가). BITNET.md M1/M2 + M2 summary + discovery tape에서 "1.58-bit를 측정했다"고 읽히는 문구를 전부 "TQ2_0 2.06bpw"로 정정. (메모리 결론은 보수적: 진짜 1.58-bit RSS는 ≤ TQ2_0이므로 memory-holds는 유지.)
- [x] **verdict 영향:** 30%는 **진짜 TQ2_0 2.06bpw floor** — 스코어러 아티팩트 성분은 +5pp(14개 실패 중 1개)뿐. **M1/M2 "quality FAIL" verdict 유지** (M2 silent flip 안 함, surface만 함). `.verdicts/bitnet/m1_scorer_ablation.txt`(per-scorer raw + hexa verify --fence verbatim + ablation 블록).
- [x] **cross-domain FLAG (g58, 실행 안 함):** 같은 `byte_exact_subset`가 SANDBOX 21개 파일(canonical 16/20 stage-0 floor 포함) 채점에 쓰임 → SANDBOX-active cycle용 discovery seed로만 등록(`.discoveries/sandbox-byte-exact-blast-radius.tape`, hexa tape 검증 통과). 여기서 SANDBOX verdict는 안 건드림.
- [x] discovery: `.discoveries/bitnet-m1-ternary-floor.tape`에 `d_bitnet_m1_scorer_artifact_ruled_out` 추가 + floor-negative/M3-seed claim relabel(7 entries, 0 malformed). M3 seed의 스코어러 절반은 RESOLVED, Q4/Q8/fp16 Pareto knee만 남음.

## 2026-05-25 — M1 Surface + M2 Memory 닫음 (BitNet 2B4T 1.58-bit, $0 local, 정직한 음성 결과)

- [x] **M1 엔진 언블록 (load-bearing).** 공식 `microsoft/bitnet-b1.58-2B-4T-gguf`의 i2_s GGUF는 stock brew llama.cpp 9150에서 **로드 불가** — ggml type-id 36이 이 빌드의 `TYPE_IQ4_NL_4_4 REMOVED`와 충돌(i2_s는 bitnet.cpp 전용, 미설치). bitnet.cpp 빌드 대신 llama.cpp 네이티브 삼진(ternary) 경로 **TQ2_0**(2.06 bpw)를 택함: bf16 safetensors(`microsoft/bitnet-b1.58-2B-4T-bf16`, 4.83 GB) 다운로드 → brew `convert_hf_to_gguf.py`로 변환. 2B4T 변종 때문에 로컬 one-off 패치 3개 필요(커밋 안 함): (1) config arch `BitNetForCausalLM`→`BitnetForCausalLM`(컨버터 레지스트리 키가 소문자 n, 대소문자 구분), (2) tensor_mapping에 `self_attn.attn_sub_norm`·`mlp.ffn_sub_norm` source 이름 추가(2B4T의 subln 레이어 — 구형 bitnet_b1_58 컨버터엔 없음), (3) `BitnetModel.set_vocab` BPE fallback(2B4T는 SentencePiece가 아니라 GPT-2/BPE `tokenizer.json` 사용). 결과 GGUF(332 tensors, 210 tq2_0, file-type 37) 로드+실행 확인.
- [x] **Metal 커널 부재 → CPU-only.** 변환된 TQ2_0는 로드되지만 brew Metal 백엔드가 **type 35에서 assert**(TQ2_0 GPU 커널 없음) → `-ngl 0` CPU 추론(NEON+ACCELERATE, ~7.7 tok/s) + `--jinja`("User:/Assistant:" 2B4T 챗 템플릿은 llama.cpp 빌트인 아님). 엔진 동작 검증 = M1 surface 충족.
- [x] **M1 정확도 floor = 6/20 = 30.0% (정직한 음성).** SANDBOX stage-0과 **동일한 20-task manifest**(verbatim) + **동일한 byte_exact_subset 스코어러**로 측정. clean Qwen2.5-0.5B-Q4 baseline은 같은 manifest에서 16/20 = 80%. BitNet은 usable floor(≥15/20)는 물론 4-bit 0.5B baseline에도 못 미침. 실패 양상: instruct 모델이 "reply with X only"를 무시하고 장황·반복 산문 생성("The digit only is 4. The digit only is 4. …") → 사실은 맞아도 정확 키워드를 놓침. raw completion(템플릿 없이)은 **빈 출력** → `-st --jinja`가 공정한 serving 모드이며 30%는 진짜 served-instruct 수치(템플릿 결핍 아님). 비교 가능성을 위해 스코어러는 일부러 그대로 둠.
- [x] **M2 메모리 = 0.55× (same-backend) — 메모리는 통과, 품질은 실패 → joint claim FALSIFIED.** 같은 serving 세션에서 `/usr/bin/time -l` RSS 캡처. BitNet-TQ2_0 peak RSS = 1568 MB(CPU) vs Qwen2.5-1.5B-Q4 = 1944 MB(Metal, as-served) / 2878 MB(CPU, same-backend control). "1.58-bit ≈ full quality at a fraction of memory"의 메모리 절반은 HOLD(RSS가 Q4 baseline의 실제 fraction); 품질 절반은 FAIL(정확도 격차 30% vs 80%). falsifier "accuracy gap > ε" 발화, "memory not below fraction" falsifier는 미발화. honest note: TQ2_0 Metal 커널 부재로 BitNet은 CPU 측정 — same-backend CPU control로 백엔드 교란 제거.
- [x] hexa verify: M1/M2는 T4 EMPIRICAL(cx_empirical_contact) — served-accuracy를 재계산하는 `--expr` closed-form 없음 → rubric상 🟠 DEFERRED(external hw/data dep). 재계산 경로 = 커밋된 `bench/bitnet_m1_accuracy_floor.hexa` 재실행. `hexa verify BT-77` verbatim 프로브를 M1 summary에 기록(BT-77은 empirical benchmark atom이라 rubric로 fall-through).
- [x] discovery: `.discoveries/bitnet-m1-ternary-floor.tape`(hexa tape 검증 통과, 6 entries / 0 malformed) — 엔진 언블록·M1 음성·M2 FALSIFIED + M3 Pareto/스코어러-템플릿 confound seed.
- [ ] **다음 사이클 seed (M3.BITNET):** 30%가 진짜 ternary floor인지 스코어러/템플릿 아티팩트인지 분리. (a) 같은 2B4T를 Q4/Q8/fp16로 변환($0 local, bf16 이미 디스크에 있음)해 bit↔quality knee 측정; (b) verbosity-tolerant 스코어러 또는 strict "answer only" system-prompt로 30%가 들리는지 확인(M1 TSV에 사실은 맞지만 정확 키워드를 놓친 답이 다수).

