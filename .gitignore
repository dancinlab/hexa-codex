# runtime / cache
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
.ipynb_checkpoints/

# editor
.DS_Store
.vscode/
.idea/
*.swp

# Claude Code local state (scheduled tasks, agent caches)
.claude/*
!.claude/skills/

# hexa-codex runtime state — state/ tracked per commons c5 (cli/hexa-codex.hexa appends to state/)
# narrow: ephemeral run-markers + logs stay untracked; real artifacts under state/ ARE tracked
state/markers/
state/*.log
lm_foundry/state/*.jsonl

# build artifacts
build/out/
build/artifacts/
*.pdf
# EXCEPTION (cx_paper_pdf_committed): shipped paper PDFs ARE tracked — viewable on GitHub
!PAPER/**/*.pdf

# LaTeX paper build cruft (PAPER/*/main.{aux,bbl,blg,log,out} + legacy papers/)
PAPER/**/*.aux
PAPER/**/*.bbl
PAPER/**/*.blg
PAPER/**/*.log
PAPER/**/*.out
PAPER/**/*.fls
PAPER/**/*.fdb_latexmk
PAPER/**/*.synctex.gz
PAPER/**/*.toc
PAPER/**/build/
ENGINE/build/
papers/**/*.aux
papers/**/*.bbl
papers/**/*.blg
papers/**/*.log
papers/**/*.out
papers/**/*.fls
papers/**/*.fdb_latexmk
papers/**/*.synctex.gz

# === lm_foundry/ (absorbed from hexa-forge, 2026-05-13) ===
# model weights / large artifacts — NEVER commit; use HF Hub or external storage
*.safetensors
*.bin
*.gguf
*.pt
*.ckpt
checkpoints/
weights/
# training logs + per-round bench pulls (SoT is HF: dancinlab/hexa-forge-bench-cold-v0.1.3)
lm_foundry/runs/
lm_foundry/logs/
lm_foundry/bench-cold/
wandb/
# local idea memo (not for publication)
# manifest backups
lm_foundry/eval/**/*.bak
.harness/logs/
.harness/handoff/
