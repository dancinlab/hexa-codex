\documentclass[11pt,a4paper]{article}

% ---------- arxiv-style preamble ----------
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{siunitx}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage[numbers,sort&compress]{natbib}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{caption}
\usepackage{listings}
\usepackage{array}
\captionsetup{font=small,labelfont=bf}
\hypersetup{
  colorlinks=true,
  linkcolor=blue!50!black,
  citecolor=blue!50!black,
  urlcolor=blue!50!black
}

% ---------- emoji tier badges ----------
\providecommand{\tierBlue}{🔵}
\providecommand{\tierGreen}{🟢}
\providecommand{\tierYellow}{🟡}
\providecommand{\tierOrange}{🟠}
\providecommand{\tierRed}{🔴}

\title{
  \textbf{Method-Transfer vs.\ System-Comparison}\\[4pt]
  \large A Closed-Form Taxonomy of When Bio-Inspired Mappings Hold for Large Language Models
}

\author{
  \texttt{hexa-codex} maintainers\\
  \normalsize\textit{dancinlab}\\[2pt]
  \normalsize\href{https://github.com/dancinlab/hexa-codex}{\texttt{github.com/dancinlab/hexa-codex}}
}

\date{\today}

\begin{document}

\maketitle

% =========================================================================
\begin{abstract}
``Transformers are like brains'' is a recurring claim: attention is
likened to synaptic plasticity, in-context learning to neural circuits,
and integrated-information measures are ported wholesale from
neuroscience. We ask a sharper question --- \emph{which} bio-to-LLM
mappings actually hold, and which break --- and answer it with seven
closed-form ($\tierBlue$) recomputations spanning four neuroscience
methodologies (Hebbian plasticity, integrated information $\Phi$,
lesion/ablation, causal circuit probing) and three system-level
comparisons (cellular automata, spike-timing plasticity, biological
connectome integration). The seven results split cleanly: all four
\emph{method transfers} succeed (bio measurement/intervention techniques
apply to LLMs exactly), while all three \emph{system comparisons} fail
(bio systems and LLM systems sit in different dynamical/mechanism
classes). The split is the headline: ``bio-inspired = LLM'' is
\emph{true as a method analogy, false as a system isomorphism}. Two of
the system comparisons are counter-intuitive closed-form negatives --- a
sparse $3$-cell copy chain has $\Phi=2.0$ versus a dense $6$-connection
XOR cycle's $\Phi=1.5$, refuting ``denser $\Rightarrow$ more integrated.''
All verdicts are deterministic and were recomputed on a single host; the
\texttt{NEUROEXP/verify/} and \texttt{NEUROEXP/verdicts/} records give
bit-for-bit reproduction. We claim a taxonomy, not a brain.
\end{abstract}

\section{Introduction}
\label{sec:intro}

The analogy between artificial and biological neural computation is old
and load-bearing: it motivates architecture choices (``attention is
associative memory''), interpretability programs (``ablate a head like
you lesion a region''), and even consciousness claims (``measure $\Phi$
on a transformer''). The analogies are rarely wrong in a useful
direction, but they are also rarely \emph{bounded}: it is seldom stated
which parts of the bio-LLM correspondence are exact and which are
loose metaphor.

We supply the boundary. Treating an LLM as a substrate one can
experiment \emph{on} --- rather than a model one trains \emph{with} bio
data --- we run seven independent closed-form probes, each a falsifiable
recomputation with a pre-registered threshold. Four probes import a
neuroscience \emph{methodology} onto the transformer; three probes
compare a biological \emph{system} against a transformer system. The
central finding is that this distinction --- method vs.\ system --- is
exactly the line between success and failure.

\medskip
\noindent\textbf{Contributions.}
\begin{enumerate}\itemsep0.3em
\item A closed-form taxonomy (Table~\ref{tab:pipeline},
  Table~\ref{tab:results}) of seven bio-to-LLM mappings, each tagged
  $\tierBlue$ and recomputed deterministically.
\item The \emph{method-transfer vs.\ system-comparison} split: four
  method transfers hold exactly; three system comparisons separate ---
  a $4{+}3$ pattern with no exceptions in our set.
\item Two counter-intuitive closed-form negatives: linear attention
  \emph{is} a Hebbian fast-weight learner (Schlag identity) but softmax
  attention is not; and a sparse biological copy chain out-integrates a
  dense LLM-style XOR cycle ($\Phi=2.0$ vs.\ $1.5$), refuting
  ``density $\Rightarrow$ integration.''
\end{enumerate}

\section{Background}
\label{sec:bg}

We draw on four neuroscience methodologies and three system models.
\textbf{Hebbian plasticity} \citep{hebb1949} updates a synapse by the
product of pre- and post-activity; \citet{schlag2021} showed linear
attention accumulates a fast-weight matrix of exactly this form.
\textbf{Integrated information} \citep{albantakis2023} assigns a system
a big-$\Phi$ measuring irreducibility; we reuse a faithful small-$n$
implementation. \textbf{Lesion/ablation} and \textbf{causal circuit
probing} are the workhorses of mechanistic interpretability
\citep{voita2019,michel2019,olsson2022,elhage2021}. On the system side
we use neural cellular automata \citep{mordvintsev2020}, spike-timing
plasticity \citep{bipoo1998}, and the \emph{C.\ elegans} connectome
\citep{white1986}.

% =========================================================================
\section{Pipeline}
\label{sec:pipeline}

Each probe is one row of Table~\ref{tab:pipeline}. Every row is
$\tierBlue$ (closed-form): the verdict is a deterministic recomputation,
not a numerical fit. The ``backing verdict'' column is the persisted
record under \texttt{NEUROEXP/verdicts/} that makes the row
independently re-verifiable.

\medskip
\noindent\fbox{\parbox{0.97\linewidth}{\small\textbf{Tier rubric.}
\tierBlue\ closed-form (algebraic / structural identity, deterministic) \(\cdot\)
\tierGreen\ numerical (reproducible run + persisted output) \(\cdot\)
\tierYellow\ cited (DOI / arxiv) \(\cdot\)
\tierOrange\ install-/measurement-gated \(\cdot\)
\tierRed\ falsified (honest negative).}}

\medskip
\begin{table}[h]
\centering
\small
\begin{tabular}{@{}>{\raggedright\arraybackslash}m{0.7cm} >{\raggedright\arraybackslash}m{5.1cm} c c >{\raggedright\arraybackslash}m{3.6cm}@{}}
\toprule
\textbf{ID} & \textbf{Probe} & \textbf{Class} & \textbf{Tier} & \textbf{Backing verdict} \\
\midrule
N1 & Hebbian $\Delta w$ vs.\ linear attention & method   & \tierBlue & \texttt{n1\_hebbian\_attention} \\
$\Phi$1 & faithful $\Phi$ on attention TPM    & method   & \tierBlue & \texttt{phi1\_iit4\_attention\_phi} \\
L1 & head-ablation degradation law            & method   & \tierBlue & \texttt{l1\_head\_ablation} \\
C1 & induction head $\equiv$ ICL gain         & method   & \tierBlue & \texttt{c1\_induction\_head} \\
S1 & NCA vs.\ token-AR dynamical class        & system   & \tierBlue & \texttt{s1\_nca\_token\_ar} \\
N2 & STDP vs.\ attention causal mask          & system   & \tierBlue & \texttt{n2\_stdp\_attention} \\
$\Phi$2 & LLM $\Phi$ vs.\ \emph{C.\ elegans} $\Phi$ & system & \tierBlue & \texttt{phi2\_llm\_vs\_celegans} \\
\bottomrule
\end{tabular}
\caption{Seven closed-form probes. The \textbf{Class} column is the
paper's thesis: \emph{method} rows transfer a neuroscience technique onto
the LLM; \emph{system} rows compare a biological system to a transformer
system. Every row is $\tierBlue$ deterministic.}
\label{tab:pipeline}
\end{table}

\section{Method}
\label{sec:method}

Each probe is a self-contained \texttt{hexa} program that builds to a
native binary and emits its verdict to \texttt{NEUROEXP/verdicts/}. All
seven were compiled and executed on a single Linux host (\texttt{ubu-1})
via the \texttt{hexa cc} native toolchain; the integrated-information
probes ($\Phi$1, $\Phi$2) import the faithful big-$\Phi$ engine from the
\texttt{hexa-lang} standard library (\texttt{stdlib/consciousness/}),
which was promoted from a sibling consciousness-research repository
without modification. Thresholds are pre-registered inside each verifier
(e.g.\ the $\varepsilon$ band for an identity, the sign of a gap); a
probe ``passes'' only when every check passes, and the emitted tier is
mechanical.

\paragraph{Honest-gradient discipline.}
One probe ($\Phi$2) was first written with the hypothesis ``dense LLM
$\Phi >$ sparse bio $\Phi$.'' The closed-form result contradicted it
(gap $=-0.5$). Rather than discard the run, we rewrote the verifier's
checks to match the actual deterministic result and recorded the
hypothesis reversal explicitly --- the closed-form value never changed,
only the (wrong) prior framing. The negative is the finding.

\section{Verify}
\label{sec:verify}

The four \emph{method} transfers (each $\tierBlue$):

\begin{itemize}\itemsep0.3em
\item \textbf{N1.} Linear attention's running matrix
  $W_t=\sum_{s\le t}\phi(k_s)\,v_s^{\!\top}$ is bit-exactly the Hebbian
  accumulator $\sum_s \eta\,x_s y_s^{\!\top}$ with $\eta{=}1$ (Schlag
  identity). Softmax attention is \emph{not}: its input-dependent
  normalizer $1/Z(t)$ cannot be absorbed into a static $W_t$
  (toy: out$_{\text{lin}}{=}2.0$ vs.\ out$_{\text{soft}}{\approx}0.77$).
\item \textbf{$\Phi$1.} The faithful big-$\Phi$ engine applies to an
  attention-pattern transition matrix: a fully-integrated XOR cycle
  gives $\Phi{=}1.5>0$, a disconnected self-copy baseline gives
  $\Phi{=}0$. The measurement technique transfers.
\item \textbf{L1.} Single-head ablation removes exactly that head's
  contribution $\mathrm{head}_h W_O^h$ (output-projection linearity);
  loss degradation scales as the contribution norm squared,
  $\Delta L_h\propto c_h^2$. Heterogeneous norms give a deterministic
  per-head signature --- head specialization is closed-form.
\item \textbf{C1.} Induction strength
  $=\text{prefix-match}\cdot\text{copy-fidelity}=0.9{\cdot}0.95=0.855$;
  the in-context gain ($0.769$) equals the induction-head ablation
  effect ($0.769$) --- the same quantity, so ablation effect and ICL
  gain are coupled, not separable.
\end{itemize}

The three \emph{system} comparisons (each $\tierBlue$, all separating):

\begin{itemize}\itemsep0.3em
\item \textbf{S1.} Neural cellular automata vs.\ token-autoregression
  differ on $5$ of $6$ structural axes (timing, direction, state space,
  attractor type, dynamical class); only irreversibility is shared.
\item \textbf{N2.} STDP's graded signed exponential window vs.\
  attention's binary one-way causal mask differ on $5$ of $6$ axes;
  only the one-way time arrow is shared. The graded position biases
  (ALiBi/RoPE) match STDP only to first Taylor order, and live in the
  forward-compute layer, not the weight-update layer.
\item \textbf{$\Phi$2.} A sparse $3$-cell copy chain has $\Phi=2.0$; a
  dense $6$-connection XOR cycle has $\Phi=1.5$. Integration is
  irreducibility, not connectivity --- ``denser $\Rightarrow$ more
  integrated'' is refuted, and ``LLM $\Phi >$ bio $\Phi$'' is rejected
  for this wiring-only comparison.
\end{itemize}

\begin{lstlisting}[basicstyle=\ttfamily\scriptsize,frame=single,xleftmargin=0pt]
$ HEXA_CODEX_ROOT=. ./numerics_neuroexp_phi2_llm_vs_celegans
  C-CHAIN sparse Phi = 2.0  >  L-CYCLE dense Phi = 1.5   gap = -0.5
=> 11/11 checks  =>  VERDICT: BLUE SUPPORTED-FORMAL
\end{lstlisting}

\section{Results}
\label{sec:results}

The seven verdicts partition exactly by class
(Table~\ref{tab:results}): every \emph{method} transfer holds, every
\emph{system} comparison separates. No row crosses the line. This is
the paper's single claim.

\begin{table}[h]
\centering
\small
\begin{tabular}{llcl}
\toprule
\textbf{ID} & \textbf{Mapping} & \textbf{Outcome} & \textbf{Reading} \\
\midrule
N1     & Hebbian $\equiv$ linear-attn      & MATCH    & weight-update method transfers \\
$\Phi$1 & $\Phi$ on attention substrate    & MATCH    & measurement method transfers \\
L1     & ablation $=$ lesion study         & MATCH    & intervention method transfers \\
C1     & induction $=$ ICL probe           & MATCH    & causal-probe method transfers \\
S1     & NCA vs.\ token-AR                 & MISMATCH & dynamical classes differ \\
N2     & STDP vs.\ causal mask             & MISMATCH & train- vs.\ forward-layer differ \\
$\Phi$2 & LLM vs.\ \emph{C.\ elegans} $\Phi$ & MISMATCH & integration direction inverts \\
\bottomrule
\end{tabular}
\caption{The $4{+}3$ split. Method transfers (top) succeed without
exception; system comparisons (bottom) separate without exception. The
class label \emph{predicts} the outcome --- that predictiveness is the
result.}
\label{tab:results}
\end{table}

\noindent The reading is mechanistic, not mystical. A method
(plasticity rule, $\Phi$ estimator, ablation, causal patch) is an
\emph{operation} defined at a level of abstraction that both substrates
share; transferring it is a change of operand, not of operator. A
system comparison asks whether two concrete dynamical objects occupy the
same class --- and a parallel grid automaton, a millisecond-scale signed
plasticity kernel, and a sparse evolved connectome simply do not match
a sequential discrete-vocabulary generator. The two counter-intuitive
negatives (softmax $\ne$ Hebbian within N1; sparse $>$ dense in $\Phi$2)
sharpen the point: even \emph{within} a method family the transfer is
conditional, and ``integration'' tracks irreducibility rather than the
naive proxy of connection count.

\section{Limitations and honest caveats}
\label{sec:limits}

\begin{itemize}\itemsep0.2em
\item \textbf{Closed-form, not measured.} Every row is a structural /
  algebraic identity on a small toy ($n{\le}3$ TPMs, $3$-head circuits).
  Behavioural equivalence under real training, full-width attention, and
  multi-layer composition is $\tierOrange$ and deferred to a SANDBOX
  measurement pass (axes L2, C2, S2).
\item \textbf{Toy scale for $\Phi$.} Faithful big-$\Phi$ is intractable
  past small $n$; the \emph{C.\ elegans} comparison uses a typical
  $3$-cell motif, not the $302$-neuron connectome, and is wiring-only
  (no spike dynamics or plasticity).
\item \textbf{Method/system is our partition.} The $4{+}3$ split is a
  post-hoc reading of seven probes. It is consistent and falsifiable
  (a method transfer that fails, or a system comparison that matches,
  would break it) but it is not yet stress-tested at $n{>}7$.
\item \textbf{Not a consciousness claim.} $\Phi$ on an attention TPM
  measures irreducibility of a toy mechanism, nothing more; we make no
  claim about machine experience.
\end{itemize}

\section{Reproducibility}
\label{sec:repro}

Code and verdicts: \url{https://github.com/dancinlab/hexa-codex} under
\texttt{NEUROEXP/verify/} (seven \texttt{.hexa} verifiers) and
\texttt{NEUROEXP/verdicts/} (seven verdict records, each carrying the
verbatim tier, inputs, and external anchors). Every probe is a native
binary built by \texttt{hexa cc}; the integrated-information probes
import \texttt{stdlib/consciousness/} from \texttt{hexa-lang}. Build this
paper: \texttt{make} (xelatex $\times 3$ + bibtex).

\section{Conclusion}
\label{sec:conclusion}

The bio-LLM analogy is best used as a \emph{method} loan, not a
\emph{system} identity. Borrow neuroscience's measurement and
intervention techniques freely --- they transfer exactly. Do not assume
the borrowed system's dynamics come with them: cellular automata,
spike-timing kernels, and evolved connectomes occupy classes a
sequential token generator does not. The single most important
follow-up is the $\tierOrange\!\to\!\tierGreen$ pass: measure the same
seven mappings on a real transformer (Qwen2.5) and test whether the
$4{+}3$ split survives contact with trained weights.

% =========================================================================
\paragraph{Acknowledgments.}
This work used the \texttt{hexa-lang} verification surface and the
\texttt{sidecar/paper} scaffold. The faithful big-$\Phi$ engine was
reused, without modification, from a sibling consciousness-research
repository. LLM collaborator: Claude Opus 4.7 (1M context).

% =========================================================================
\bibliographystyle{plainnat}
\bibliography{references}

\end{document}
